---
tags: []
---
# 하네스 시스템 통합 스크립트 상세 가이드 (v9.3.1 — 하네스 업그레이드 로드맵 P1+P2 완료)

이 문서는 박사님의 Mac Studio 지능화 프로젝트의 **누적된 기술 자산**과 **통합 하네스 에이전트(Harness Agent)**의 유기적 연결 상태를 기록한 최종 매뉴얼입니다.

---

## 0. 핵심 시스템 디렉터리 구조 및 파일 위치 안내

시스템의 주요 파일과 스크립트가 저장된 위치 요약입니다. 스크립트나 문서의 이름만 알고 있을 때 어디서 찾아야 할지 헤맬 경우 이 섹션을 참고하세요.

### `/Users/bluesea/Applications/Mjauto/Scripts/` (메인 스크립트 폴더)

**메인 엔트리 포인트**
- `hermes_local.py`: 헤르메스 텔레그램 봇 메인 실행 파일 (Launchd로 자동 실행)
- `harness_agent.py`: 통합 하네스 에이전트 관제탑 — **319줄 얇은 오케스트레이터** (🔄 2026-06-09 3차 분할 완료: 887→494→319줄, 64% 총 감소)
  - **현재 역할**: /confirm, /cancel, 모드전환 UI, @단축명령어, Logic Engine 우선처리, 진행 애니메이션, SAVE 태그 처리, 응답 전송. 실제 컨텍스트 조립/에이전틱 루프는 아래 두 모듈에 위임.
  - **위임 구조**: `assemble_context()` (context_assembler.py) → `get_llm_response()` → `run_agentic_loop()` (agentic_loop.py)
  - **에이전틱 루프 안전 시스템** (agentic_loop.py로 이관됨, 구조만 기록):
    - Mayor 감독, ToolResult 샌드박스, CoVe 파일시스템 자기검증, PermissionBridge RUN_CMD 승인
- `agent_harness.py`: ACID 기반 에이전트 제어 하네스 (Harness V2.5 전용)

**라우터 계층**
- `hybrid_router.py`: Action Realization Layer v2.7 — v9.2 Multi-Model Load Balancing 탑재
- `natural_language_router.py`: 자연어 의도 분류기 v1.0

**파일/메모리 유틸리티**
- `hermes_file_ops.py`: 파일 안전 조작 핸들러 (확인 시스템 포함)
- `file_ops_agent.py`: 🆕 **파일 작업 레이어 (2026-06-09 신규)**
  - **책임**: `harness_agent.py`에서 분리된 파일 작업 확인 대기열/경로 검증/실행 (L466~L584)
  - **주요 함수**: `handle_file_op()` — DELETE/MOVE/COPY/RENAME/CREATE 작업 수행, `is_path_allowed()` — 보안 경로 검사, `safe_remove()` — Trash로 안전 이동
  - **설계의도**: 파일 작업을 별도 모듈로 격리하여 harness_agent의 메인 로직 단순화. Permission Bridge 통합.
- `hermes_harness.py`: ACID 에이전트 제어 하네스 핸들러 (Harness V2.5)
- `hermes_memory_patch.py`: Bio-Memory Patch — 3계층 통합 파사드

**설정/인덱스**
- `config.py`: 포트, 토큰 등 환경 변수와 경로를 정의하는 설정 파일
- `harness_config.py`: Mac Studio 환경 전용 하네스 설정
- `index_db.py`: 통합 인덱스 DB 헬퍼 모듈 v1.0
- `migrate_to_index_db.py`: 통합 인덱스 DB 마이그레이션 스크립트 v1.0
- `migrate_json_to_db.py`: JSON→DB 마이그레이션
- `update_index.py`: 인덱스 업데이트 스크립트
- `setup_family_bot.py`: 패밀리 봇 설정 스크립트

**에이전트/메시징**
- `web_agent_module.py`: 대화형 웹 검색 및 읽기 에이전트 모듈
- `send_telegram_msg.py`: 터미널 단독 텔레그램 알림 발송 유틸리티

**테스트/임시**
- `test_wiki_linker.py`: wiki_linker 단독 테스트
- `_run_fulltest_graphify.py`: graphify 전체 테스트
- `_run_graphify_test.py`: graphify 테스트

**쉘 스크립트 (8개)**
- `check_services.sh`: 서비스 상태 점검
- `clear_memory.sh`: 16GB 시스템 램 캐시 확보용 최적화 스크립트
- `fswatch-indexer.sh`: 파일 변경 감지 인덱서 (launchd 연동) — 🆕 2026-06-23 `wiki_auto_stamper.py` 연동 추가. wiki/ 전체 감시로 확장. Hermes·Claude Code가 생성한 .md 파일에도 태그(최대8개)·링크제거·타임스탬프 자동 적용. 제외: `.obsidian/`, `.smart-env/`, `99_Archive/`
- `hermes_cli.sh`: 터미널 전용 봇 디버거 스크립트
- `recovery_system.sh`: 시스템 복구 스크립트
- `clear_webui_cache.sh`: WebUI 모델 캐시 삭제 스크립트 (구버전 캐시 강제 재로드)
- `check_venv_patches.sh`: venv 내 주요 파치 검증 스크립트 (harness_agent, run.py, config.yaml 검증)
- `model_endpoint_check.sh`: LLM 엔드포인트 가동 여부 확인 스크립트 (Gemma4, DeepSeek, NVIDIA NIM)
- `check_file_sizes.sh`: 🆕 2026-06-11 핵심 스크립트 비대화 감시 — 관제탑(harness_agent/hermes_local) 500줄, 일반 .py 800줄 초과 시 텔레그램 경고. launchd `com.hermes.sizewatch`로 매주 월 09:00 자동 실행. 진단: `~/.hermes/runtime/sizewatch.log`
- `check_bot_alive.sh`: 🆕 2026-06-17 / 🔄 2026-06-23 Hermes1 봇 워치독 — `~/.hermes/runtime/bot_heartbeat` mtime이 3분 이상 정지 시 SIGTERM(→최대 5초 대기→SIGKILL fallback) 후 재시작. **2026-06-23 개선**: 프로세스 완전 소멸(PID=0) 케이스 즉시 재시작 로직 추가, launchctl 실패 시 nohup fallback, 복구 성공/실패 텔레그램 알림 추가. launchd `com.hermes.botwatch`로 5분마다 자동 실행. **주의**: botwatch plist 존재해도 launchd bootstrap 안 되면 미동작 — 재부팅 후 `launchctl list com.hermes.botwatch`로 PID 확인 필수. 진단: `~/.hermes/runtime/botwatch.log`

**데이터 파일**
- `harness_memory.json`: 시스템 내 에이전트들이 공유하는 단기 대화 버퍼 메모리 파일
- `memory.json`: 추가 메모리 버퍼
- `consolidator_state.json`: 메모리 엔진 통합 상태 파일 (`~/Applications/Mjauto/Scripts/hermes/memory_engine/consolidator_state.json`)
- `harness_graph.json`: 하네스 그래프 데이터
- `HERMES.md`: 헤르메스 설명 파일
- `antigravity_history.json`: 안티그래비티 히스토리
- `codex_history.json`: Codex 히스토리
- `v9_0_완성_요약.md`: v9.0 core_reducer 개요/액션 현황/v9.1 계획 정리 문서

**하위 디렉터리**
- `handlers/` (12개 핸들러 모듈) — 아래 상세
- `modules/` (61개 전문 모듈 + dialectic_layer/ 6파일 + graph_memory/ 3파일) — 아래 상세
- `tests/` (10개 테스트 파일) — 아래 상세
- `MacBot/` — MacBot 관련 파일 (레거시)
- `_archive/` — 보관 (dream_scheduler.py, weekly_pulse.py, start_bots.sh, mac_bot_v2.py 등)
- `inbox/` — 수신 대기 파일
- `outbox/` — 발신 대기 파일
- `_backup/` — 백업
- `backup_plists/` — plist 백업
- `working/` — 작업 중 파일

---

### `/Users/bluesea/Applications/Mjauto/Scripts/handlers/` (텔레그램 명령어 핸들러 — 20개 모듈)

> 🔄 **2026-06-09 대규모 분할 완료** — 기존 13개 → 17개 (4개 신규 분리)
> 🆕 **2026-06-10** — `_stock.py` 추가 → 18개
> 🛡️ **2026-06-11** — 전체 핸들러 255곳 `safe_reply`/`safe_edit` 적용 완료 (결함 #4 완전 해소)
> 🔄 **2026-06-30** — `_stock.py` SRP 분할 → `_stock_mjstock.py`·`_stock_coin.py` 신규 분리 → 20개

| 파일 | 설명 | 주요 명령어 |
|:---|---|:---|
| `__init__.py` | 패키지 초기화 — 모든 핸들러 import 집계 | — |
| `_approval.py` | 승인/태그 명령어 | `/tag`, 승인 콜백 |
| `_base.py` | 베이스 핸들러 — 공통 인스턴스/함수 + 🆕 `safe_reply()`/`safe_edit()` Markdown 폴백 헬퍼 (결함 #4 방어) | 공통 logger, router, verifier |
| `_callbacks.py` | 🆕 **InlineKeyboard 콜백 라우터** (2026-06-09 _base.py에서 분리) | mem_purge, kanban, perm, verify_harness 버튼 + 🆕 `/retry` (낙관적 응답 재시도) |
| `_file.py` | 파일/웹 명령어 — 🆕 낙관적 응답 엔진 통합 | `/ingest` (즉시 피드백+백그라운드), `/file`, `/web` |
| `_grill.py` | 문서 기반 대화 | `/grill-with-docs` |
| `_kanban.py` | 칸반 명령어 (v2) | `/kanban` — heartbeat + zombie detection + multi-worker |
| `_memory.py` | 메모리/지식 명령어 | `/memory`, `/knowledge` |
| `_meta.py` | **메타 문서 핸들러** | **`/claude_brief`** — 4대 메타 문서 브리핑 생성. 🆕 **`/save_wiki [경로] [--overwrite]`** — 대화 분석 결과를 wiki 페이지로 저장 (index.md 자동갱신). 🆕 **`/wiki_lint [일수]`** — wiki 고아/오래된/깨진링크/빈 페이지 탐지 |
| `_orchestrator.py` | Multi-Agent 오케스트레이션 | `/orchestrate`, `/agent` |
| `_paper.py` | **논문 명령어** (2026-06-09 분리, 🔄 2026-06-30 SRP 리팩터 → 449줄) — 9개 서브명령 핸들러 함수 분리. `cmd_paper`는 54줄 dispatch 라우터. `_RESEARCH_HELP` 상수는 `_research.py`로 이전 | `/paper humanize/draft/review/list/bundle/claims/compare/save/show` |
| `_research.py` | 연구 명령어 PKM_2 Knowledge Mesh (🔄 272줄, `_RESEARCH_HELP` 상수 추가) | `/research` (지식 메쉬 파이프라인) |
| `_stock.py` | **주식 분석 핸들러 V_FINAL** (🔄 2026-06-30 SRP 분할 후 713줄) — `/stock` 수동조회 시 `record_manual_lookup()` 호출. 하단에서 `_stock_mjstock`·`_stock_coin` import + STOCK_COMMANDS 조립 | `/stock`, `/scan`, `/market`, `/watchlist`, `/positions`, `/result` + 차트분석 |
| `_stock_mjstock.py` | 🆕 **MJstock 핸들러** (2026-06-30 신규, 🔄 2026-06-30 갱신) — `_stock.py`에서 분리. `_handle_mjstock_chart`에서 `record_manual_lookup()` 호출. `safe_reply`/`safe_edit` 로컬 정의. **🆕 신규 함수**: `_handle_mjstock_scan_list()` — `mjstock_scan_list:{screener_key}:{date_str}` 콜백 처리, CSV 읽어 신호 먼저 + 점수 순 정렬, 각 종목 버튼 `mjstock_chart:{ticker}:{screener_key}:{date_str}`. `_handle_mjstock_chart()` — `date_str=None` 파라미터 추가(date_str 있으면 back 버튼 scan_list로, 없으면 mjstock_all로). `callback_mjstock()` — `mjstock_scan_list:` prefix 처리 + `mjstock_chart:` 4-part 파싱 추가. `_MJSTOCK_SCREENER_NAMES` 딕셔너리 추가(screener_key → 표시명 매핑). | `/backtest`, `/mjscan`, `/mjstock`, `/mjbuy`, `/mjsell`, `/mjpositions` |
| `_stock_coin.py` | 🆕 **코인 핸들러** (2026-06-30 신규, 🔄 2026-06-30 대규모 갱신) — `_stock.py`에서 분리. **🔄 3단계 네비게이션 신설**: 스캔 요약 → `coin_scan_list:{ts_key}:{screener_key}` → 종목 클릭 → `coin_all:{symbol}:{ts_key}:{screener_key}` (4-part) → 검색기 버튼 → `coin_chart` → 뒤로가기 → coin_scan_list. **신설 함수**: `_handle_coin_scan_list()`, `_handle_coin_scan_back()`, `_handle_coin_results()`. **결과 포맷 변경**: `삼돌이: N종 (A0/B6/C3)` → `삼돌이: 신호(N개), 점수(N개)`. **coin_all 뒤로가기 추가**: `_handle_coin_all()`에 `← 검색기 목록으로` 버튼. `_coin_scan_cache` 딕셔너리: ts_key → scan_out (최대 3개 유지). `safe_reply`/`safe_edit` 로컬 정의. **중요**: 이 파일 수정 시 `launchctl kickstart -k gui/$(id -u)/com.hermes.stockbot` 필수 — hermes_local 재시작만으로는 미반영. | `/coin` (scan/all/chart/list/back/results) |
| `_system.py` | 시스템 핵심 명령어 (🔄 521줄로 축소) | `/exec`, `/status`, `/delegate`, `/audit`, `/model`, `/caveman` |
| `_system_ops.py` | 🆕 **세션/검색/재시작** (2026-06-09 분리, 🔄 2026-06-23 /restart_bot 방식 변경) | `/handoff`, `/search`, `/restart_bot` — **주의**: `/restart_bot`은 launchctl unload/load 방식 제거됨. 현재: Popen(start_new_session=True)로 새 프로세스 먼저 기동 → 기존 SIGTERM. launchctl load는 이 환경에서 Exit code 5로 실패. |
| `_ui.py` | UI 명령어 | `/start`, `/help`, `/verify_harness` (InlineKeyboard 버튼 포함) |
| `_vault.py` | Vault 진단 명령어 | `/vault check`, `/vault graph` (graphify 연동) |

> 실제 20개 모듈. `_stock.py`는 hermes_local.py에서 try/except 감싸 등록 (Python 3.14 __init__.py 호환성 우회). `_stock_mjstock.py`·`_stock_coin.py`는 `_stock.py` 하단에서 import — hermes_stock_bot.py는 변경 없음.

---

### `/Users/bluesea/Applications/Mjauto/Scripts/modules/` (전문 기능 모듈 — 67개 + 서브패키지 9파일)

**지식/파일 관리 (7개 — PKM_2 Knowledge Mesh 4개 포함)**
- `wiki_manager.py`: 위키 저장소 관리 — 🆕 **2026-06-21**: `write_wiki(rel_path, content, overwrite)` 신규 — 대화 분석 결과를 wiki 마크다운 페이지로 저장 (경로 탈출 방어, atomic write, 타임스탬프 자동 삽입, `index.md` 자동 갱신). `lint_wiki(stale_days)` 신규 — wiki 건강 점검 4종 (고아 페이지·오래된 페이지·깨진 내부 링크·빈 페이지) 탐지 후 텔레그램 요약 반환
- `wiki_linker.py`: 위키 링크 자동화
- `file_manager.py`: 파일 관리
- `ingest_engine.py`: 지식 이관 및 정원(Gardening) 자동화 — PDF→텍스트 파이프라인, Inbox deferral 탑재
- `knowledge_indexer.py`: HybridKnowledgeIndexer (v9.1.2) — FTS5 + 의미 검색 통합
- `vault_scanner.py`: Vault 스캐너
- `knowledge_mesh_orchestrator.py`: KnowledgeMeshOrchestrator (v1.0) — PKM_2 중앙 제어기. DecisionAgent JSON 레시피 기반 DAG 실행. web_search_multi(arXiv/Semantic Scholar), local_semantic_search, merge_timeline, cross_reference, summarize_insights 오퍼레이션 제공. LRU 캐시 탑재.
- `timeline_builder.py`: 통합 타임라인 빌더 (v1.0) — 웹 논문+로컬 노트 시간순 정렬, arXiv ID/DOI 기반 중복 병합
- `cross_reference_analyzer.py`: 교차 참조 분석기 (v1.0) — TF-IDF 코사인 유사도 + 시간 감쇠, alignment_type(predict_and_realize / retrospective_match), 임계값 0.7 이상만 인사이트
- `auto_topic_manager.py`: 자동 주제 분류기 (v1.0) — TF-IDF 기반 클러스터링, 5개 기본 주제 템플릿(AI/ML, 법학/AI 규제, 연구 방법론, 자동화/시스템, 기타/미분류), 자동 저장/로드

|**기억/인지 (11개)**|
|- `bio_memory_engine.py`: 생체 기억 엔진 — **v9.3 하이브리드 트리거**: L2→L3 이관 조건에 `개수(>200개)` 외에 `용량(>1MB)` 메트릭 신설. 둘 중 하나라도 초과 시 즉시 L3 전이 트리거. `L2_MAX_BYTES = 1MB` 상수 도입. 🔄 **2026-06-23 SRP 분할**: 955줄 → 627줄. 벡터 계층(`TurboVecLight`, `VectorIndexManager` 등) → `vector_engine.py`로 분리. `ImportanceScorer`, `ForgettingCurve` 중복 정의 제거 → `deriver_layer.py` import로 교체. **에러 진단**: `from modules.vector_engine import *` 실패 시 `modules/vector_engine.py` 존재 확인.|
|- `vector_engine.py`: 🆕 **벡터 인덱스 계층 (2026-06-23 bio_memory_engine.py에서 분리)** — `TurboVecLight`(경량 벡터 검색), `VectorIndexManager`(인덱스 생명주기 관리), `get_vector_backend()`, `set_vector_backend()`, `init_vector_index()`, `_auto_bind_vim()` 포함. bio_memory_engine의 벡터 관련 책임 전담. **에러 진단**: `init_vector_index()` 실패 → FAISS/annoy 패키지 설치 여부 확인.|
|- `cove_engine.py`: CoVe 팩트체크 엔진 (경로: `~/Applications/Mjauto/Scripts/cove_engine.py`) — 4단계 팩트체크 파이프라인. 🆕 2026-06-08: **Step 5 파일시스템 검증 추가** — 답변 내 파일명(.py/.sh) 주장을 grep으로 실존 확인, 미존재 시 ⚠️ 경고 출력. LLM 환각(미사용 기술 스택 오인) 방지.
|- `cognitive_engine.py`: 인지 인텔리전스|
|- `memory_engine.py`: 메모리 엔진|
|- `history_manager.py`: 히스토리 관리 — 🆕 2026-06-09 **Context Compaction 추가**: 30턴 초과 시 오래된 대화를 요약 1개로 압축(`COMPACT_THRESHOLD=30`, `KEEP_RECENT=10`). `get_history_for_llm()`이 자동 압축 적용, `compact_and_save()`로 강제 저장 가능. 목적: 장기 대화 시 LLM 컨텍스트 윈도우 절약 + 핵심 맥락 보존. LLM API 호출 없이 텍스트 요약(무비용).|
|- `deriver_layer.py`: 기억 파생/승격 엔진 (Harness V2.5)|
|- `dreamer_layer.py`: L3 통합/꿈꾸기 엔진 (Harness V2.5) — **v9.3 용량 기반 증류**: `offline_consolidation()`에서 500자 이상 & 중요도 4.0 이상, 혹은 800자 초과 원문을 L3 패턴으로 강제 이관 후 L2 캐시 다이어트. 기존 에빙하우스 망각 공식($R = e^{-t/S}$) 기반 단순 삭제를 넘어 용량 기반 압축 전이 결합.|
|- `dreaming_v2.py`: SKILLOPT & FluxMem Intelligence Engine (v8.9) — **v9.3 장기 시맨틱 코어 직접 영구화**: `deep_distill()`이 `semantic_memory.json`의 `patterns` 배열에 정형화된 지식 객체 직접 누적. Obsidian 기록과 병행 수행. **안전장치: Early Exit Guard Bypass** — `raw_events`가 비어도 L2 용량 초과 시 조기 종료 우회하여 다이어트 루틴 강제 구동. **안전장치: Atomic Write** — `os.replace(tmp_path, real_path)` 원자적 파일 교체로 Race Condition 방지. 🔄 **2026-06-23 결함 #5(PEMS 고착화) 수정**: `random.random()` 기반 PEMS 조기 종료 제거 → 실제 미처리 이벤트 수 체크 + 1시간 최소 간격(`_last_distill_time`) 체크로 교체. 검증 게이트 확률 비교 → `len(distilled_knowledge.strip()) > 20` 교체. `_append_to_hot_md` 경로 수정: `Mjobsidian/hot.md` → `Mjobsidian/wiki/00_Meta/01_hot.md`. **에러 진단**: Dreaming 조기 종료 반복 → `_last_distill_time` 값 확인 (0이면 1시간 간격 첫 실행 통과).|
|- `subconscious_layer.py`: 잠재의식 계층 엔진|
|- `memory_refinement.py`: 🆕 **메모리 정제 엔진 (Phase 2, 2026-06-09)** — bio_memory_engine(Lock Stack) 래퍼. 4대 갭 해결: (1) `auto_forget()` 에빙하우스 보유율 15% 미만 L2 자동 정리(dry-run/confirm), (2) `check_conflict()` 저장 전 L2/L3 키워드 충돌 검사, (3) `should_store()` "7일 후에도 쓸모있나?" 자동 판단(중요도/일시적 표현/중복), (4) `hybrid_recall()` **3소스 통합검색** → harness LLM 컨텍스트 자동 주입. 🔄 **2026-06-11 P2-④ 업그레이드**: `hybrid_recall()`이 L2 에피소딕 + knowledge_indexer FTS5 + **semantic_index.db FTS5** (00_Meta 82개 문서) 3소스 병합으로 확장. `/memory forget`, `/memory health` 서브커맨드.|

**시스템/감시 (6개)**
- `system_monitor.py`: 시스템 감시
- `audit_engine.py`: 로그 감사 엔진
- `monitoring_engine.py`: MonitoringEngine v1.0 (v9.2) — 액션별 메트릭(지연시간/성공률) 기록, 에러율 추적, 3축 임계 경보. SQLite 기반.
- `weakness_miner.py`: 🆕 **Self-Harness Weakness Mining (2026-06-10)** — 반복 실패 패턴 자동 감지. 실패 트레이스를 (action, error_signature) 쌍으로 클러스터링. 동일 패턴 3회↑ 시 텔레그램 알림 + `06_에러보고서.md` 자동 append. `get_weakness_miner()` 싱글턴. DB: `~/.hermes/runtime/weakness.db`. **커버리지**: `_stock.py`(cmd_stock/scan/chart) + `_vault.py`(duplicates/graph) + `_file.py`(web/recent) + `_system.py`(exec/status/audit) + `_grill.py`(grill) + `_research.py`(local) — 총 6핸들러 14포인트. **에러 진단**: `python3 -c "from modules.weakness_miner import get_weakness_miner; [print(r) for r in get_weakness_miner().get_top_failures()]"`
  - **2026-06-24 추가** `record_skill_invocation(skill_name)` — 스킬 호출 시 `skill_invocations` 테이블에 기록 (arXiv 2606.20659 Skill Coverage 적용)
  - **2026-06-24 추가** `get_skill_coverage()` — `~/.hermes/skills` + `~/.claude/skills` 전체 스캔 → 스킬별 호출률 dict 반환. `/status` 명령어에서 미호출 스킬 목록 노출
  - **2026-06-24 추가** `validate_skill_safety()` — `SKILL.md` 안전 키워드 검사, 안전 제약 없는 스킬 목록 반환 (arXiv 2606.20636 SkillHarness 적용)
  - **에러 진단**: `skill_invocations` 테이블 미생성 시 `weakness.db` 삭제 후 재기동
- `proposal_validator.py`: 🆕 **Self-Harness Proposal Validation (2026-06-10)** — 수정 전 회귀 체크리스트 자동 생성. `pre_check(target_file, desc)` → held-in(weakness DB 알려진 실패) + held-out(정상 케이스) 표시. `record_result(file, passed, detail)` → SQLite 이력 + 거부시 에러보고서 append. `HANDLER_FUNCTIONS` 딕셔너리로 파일→기능 매핑. `get_validator()` 싱글턴. DB: `~/.hermes/runtime/proposal_validation.db`. **에러 진단**: `python3 -c "from modules.proposal_validator import get_validator; print(get_validator().pre_check('handlers/_stock.py','테스트'))"`
- `optimistic_response.py`: 🆕 **낙관적 응답 엔진 (Phase 1, 2026-06-09)** — Linear 아키텍처 패턴. 즉시 "⏳ 진행 중" 피드백 → `asyncio.create_task()`로 백그라운드 실행 → 성공/실패 시 메시지 편집. 3회 자동 재시도(2초 간격), 에러 유형별 복구 힌트(권한/네트워크/파일/메모리). `get_engine()` 모듈 싱글턴. `/ingest` 3분기(기본/scan/interrogate) + `/retry` 명령어에 통합.
- `model_scanner.py`: 모델 스캐너
- `modules_info.py`: 모듈 정보 제공

**LLM/라우팅 (11개)**
- `command_router.py`: 🆕 **명령어 라우터 (2026-06-09 harness_agent.py L239~345에서 분리, ~135줄)**
  - **주요 함수**: `route_command(update, context, user_text, *, file_op_pending, safe_remove, get_current_mode, ALL_MODES, MODE_LABELS, sys_mon, memory, history, ingest_inst, get_semantic, logic, get_llm_response) -> bool`
  - **처리 항목**: `/confirm`(파일작업 승인), `/cancel`(파일작업 취소), 🔄 모드전환 키보드, `@검색/@상태/@정리/@ingest` 단축명령어, Logic Engine 우선처리
  - **반환**: `True` = 처리완료(상위 return), `False` = LLM 라우팅 필요
  - **에러 진단**:
    - `/confirm` 동작 안 됨 → `file_op_pending` dict 키 확인 (chat_id 기준)
    - `@검색` 응답 없음 → `get_semantic()` 반환 None 여부 확인
    - Logic Engine 우선 처리 안 됨 → `logic` 객체 None, `_FILE_KEYWORDS` 오탐 확인
  - **연결**: `harness_agent.handle_message()` → `route_command()` 호출. `modules/file_ops_agent`, `handlers/_base._reply_long` 의존.

- `response_handler.py`: 🆕 **LLM 응답 파이프라인 (2026-06-09 harness_agent.py L347~443에서 분리, ~130줄)**
  - **주요 함수**: `handle_llm_response(update, context, user_text, *, history, wiki, memory, config, get_llm_response, file_m, ctx_builder, get_sys_prompt) -> None`
  - **처리 항목**: 진행 애니메이션(_animate_msg task), assemble_context() 호출, get_llm_response() 호출, run_agentic_loop() 호출, [SAVE] 태그 처리(PermissionBridge 포함), 히스토리 저장, _reply_long 최종 전송, 예외 처리
  - **에러 진단**:
    - "🤔 생각 중..." 뜨고 멈춤 → `get_llm_response` 내부 예외 확인, Circuit Breaker 상태 확인
    - [SAVE] 차단됨 → `permission_bridge.guard_external_tool` import 및 경로 허용 여부 확인
    - `anim_task` cancel 에러 → `anim_task is None` 체크 (예외 처리에서 None 가드 추가됨)
  - **연결**: `harness_agent.handle_message()` → `handle_llm_response()` 호출. `modules/context_assembler`, `modules/agentic_loop`, `modules/permission_bridge`, `handlers/_base._reply_long` 의존.

- `context_assembler.py`: 🆕 **컨텍스트 조립 엔진 (2026-06-09 harness_agent.py L372~500에서 분리, 254줄)**
  - **책임**: LLM에 전달할 `messages` 리스트 조립 전담. wiki ctx 5분 캐시, style_profile, 히스토리 15턴, L2/L3 메모리 hybrid_recall, Resolver 작업 컨텍스트, 시스템 팩트(briefing.md), 날씨 실시간 스크래핑 순서로 조립.
  - **주요 함수**: `assemble_context(sys_prompt, user_text, history, wiki, memory, config, update_progress_fn)` → `list[dict]` 반환
  - **내부 헬퍼**: `_inject_system_fact_context()` — "내 시스템/하네스" 키워드 감지 시 briefing.md 팩트 주입. `_inject_weather_context()` — "날씨" 감지 시 네이버 날씨 BS4 스크래핑 실시간 주입.
  - **연결**: `harness_agent.py` → `assemble_context()` 호출. `memory_refinement.hybrid_recall`, `hermes_context_builder.resolve_task_type`, `handlers/_base._get_style_profile` 의존.
  - **에러 진단**: 날씨 미주입 → BS4/urllib 설치 확인. 메모리 미주입 → `modules/memory_refinement.py` 존재 확인. 각 블록 독립 try/except이므로 하나 실패해도 나머지는 정상 동작.
- `agentic_loop.py`: 🆕 **에이전틱 루프 엔진 (2026-06-09 harness_agent.py L503~694에서 분리, 276줄)**
  - **책임**: 도구 태그 파싱·실행(LIST/READ/WEB_READ/SEARCH/RUN_CMD/CREATE/DELETE/MOVE/COPY/RENAME), ToolResult 샌드박스 래핑, Mayor 루프 감독, CoVe 파일시스템 자기검증.
  - **주요 함수**: `run_agentic_loop(initial_ans, messages, get_llm_response, update, context, file_m, ctx_builder, update_progress_fn)` → 최종 응답 문자열 반환
  - **내부 헬퍼**: `_exec_cmd()` — PermissionBridge 경유 RUN_CMD 실행. `_apply_cove_grounding()` — 루프 종료 시 파일 환각 검증.
  - **연결**: `harness_agent.py` → `run_agentic_loop()` 호출. `modules/tool_result.ToolResult`, `modules/mayor_agent.mayor`, `modules/file_ops_agent.handle_file_op`, `modules/permission_bridge.guard_external_tool`, `cove_engine.CoVeEngine` 의존.
  - **에러 진단**: 도구 미실행 → 각 `_RE_*` 정규식 패턴 확인. Mayor 조기 종료 → `modules/mayor_agent.py` DEFAULT_MAX_ITERS/TOKEN_BUDGET 확인. RUN_CMD 차단 → PermissionBridge 승인 상태 확인.
- `llm_engines.py`: 🆕 **LLM 엔진 레이어 (2026-06-09 신규)**
  - **책임**: `harness_agent.py`에서 분리된 LLM 모드/Circuit Breaker/엔진 호출 (L176~L463)
  - **핵심**: 모드 관리 (`MODE_QWEN14B`, `MODE_DEEPSEEK`, `MODE_NVIDIA`), Circuit Breaker 패턴 (연속 3회 실패 → 60초 격리), 자동 폴백 체인
  - **주요 함수**: `get_current_mode()`, `set_mode()`, `call_primary_with_fallback()`, `_call_nvidia()`, `_call_local()`, `_call_deepseek()`
  - **설계의도**: 모드/회로차단 상태를 단순히 노출하기 위해 분리. harness_agent는 LLM 엔진 구현 상세를 몰라도 됨 (의존성 역전)
- `llm_interface.py`: LLM 인터페이스
- `hybrid_router.py` (루트에도 존재): 모듈 버전
- `action_realization_layer.py`: Action Realization Layer — Life-Harness Layer 3
- `adapters.py`: UnifiedChannelAdapter (WeKnora 패턴) — v9.0
- `context_manager.py`: 컨텍스트 관리
- `hermes_context_builder.py`: 코드베이스 컨텍스트 하네스

**실행/제어 (4개)**
- `executor.py`: 자율 에러 복구형 Bash 실행기 (v2: Trajectory Regulation Layer)
- `core_reducer.py`: Hermes3 v9.0 Stateless Reducer & Micro-Agent Pipeline (🔄 2026-06-09 분할됨 — 에이전트 클래스 core_agents.py로 분리, 313줄)
- `core_agents.py`: 🆕 **마이크로 에이전트 레이어 (2026-06-09 core_reducer.py에서 분리, 505줄)**
  - **책임**: `SourceChannel`, `AgentContext`, `ArchitecturalJudgmentGate`, `DecisionAgent`, `ExecutionAgent`, `KnowledgeAgent` 6개 클래스
  - **설계의도**: 에이전트 정의와 파이프라인 오케스트레이션 분리. core_reducer는 흐름 제어만 담당
- `permission_bridge.py`: 2-tier 도구 인가 게이트웨이 — Internal(자동) vs External(Tier 2, Telegram 승인)

**스킬/진화 (6개)**
- `skill_evolver.py`: 스킬 자율 진화 (🔄 2026-06-09 분할됨 — L333 이후 큐레이션 기능 분리 완료)
  - **핵심 클래스**: `SkillEvolver` — Dreaming 중 에러 복구 시 SKILL.md 자동 생성
  - **re-export 함수들**: `find_similar_skill`, `record_exec_recovery`, `record_learning_interaction`, `get_skill_stats`, `prune_old_skills` 등 — 모두 `skill_curator_ext.py`에서 forward import됨
- `skill_curator_ext.py`: 🆕 **스킬 큐레이션 레이어 (2026-06-09 신규)**
  - **책임**: `skill_evolver.py`에서 분리된 큐레이션/통계/검증 (L334~918)
  - **주요 함수**: `find_similar_skill`, `sync_to_vault`, `record_exec_recovery`, `record_learning_interaction`, `get_skill_stats`, `prune_old_skills`, `get_weekly_usage_stats`, `curate_skills`, `consolidate_skills`, `get_curator_report`, `search_skills`, `_validate_skill`, `_rollback_skill`
  - **설계의도**: 단일 책임 원칙 — skill_evolver는 자동 스킬 생성만, curator_ext는 스킬 생명주기 관리
  - **순환import 방지**: 공유 상수(SKILLS_ROOT, TEMPLATES)는 curator_ext에 중복 정의
- `sia_engine.py`: SelfImprovingAgent (v9.2) — 피드백 기록(평점/맥락) → 추세 분석 → LLM 개선 제안. SQLite 기반.
- `curator.py`: Skill Curator + 코드베이스 편집자 (🆕 2026-06-08 확장) — 스킬 JSON 관리(30d/90d 미사용 ARCHIVE) + `scan_large_files()`(300줄+ 파일 등급 분류) + `scan_bak_files()`(.bak 임시 파일 탐지) + `full_audit()`(통합 리포트). 텔레그램 🧹 큐레이터 버튼으로 실행.

**진단/검증 (1개 신규 — 2026-06-09)**
- `harness_verifier.py`: 🆕 **Hermes 구조 건강 자동 진단** — 텔레그램 `/verify_harness` 명령어의 백엔드. AI 의견이 아닌 실제 수치로 6개 항목을 측정해 100점 만점으로 점수화.
  - **항목 1 — 파일 비대화**: 모든 .py 파일 줄 수 측정 → 300+(🟢)/600+(🟡)/900+(🔴) 등급 분류
  - **항목 2 — 모듈 의존성(SRP)**: 핵심 파일의 `from modules.` import 수 → 10개 초과 시 SRP 위반 경고
  - **항목 3 — 문서 동기화**: `02_스크립트 정보.md` 언급 파일명 vs 실제 파일 비교 → 👻 표류 문서 / 📄 미문서화 신규 파일 자동 감지 (**문서 표류 방지의 핵심**)
  - **항목 4 — 서비스 상태**: `launchctl list`로 com.hermes.bot / ai.hermes2.bot / ai.hermes.gateway 실행 확인
  - **항목 5 — 메모리 파일**: L1/L2/L3 JSON 파일 존재 + 파싱 가능 여부 + 크기
  - **항목 6 — 임시 파일**: .bak / _old / _backup 패턴 파일(폴더 제외) 탐지
  - **점수 등급**: S(90+) / A(75+) / B(60+) / C(45+) / D(미만)
  - **첫 진단 결과 (2026-06-09)**: 60/100 B등급 — 🔴harness_agent.py(1243줄)/skill_evolver.py(918줄) 분할 필요, 👻문서 표류 5개(_deriver.py 등), 서비스 3개 정상, 메모리 정상
  - **MD 자동 저장**: 실행 시마다 `00_Meta/verify_harness_report.md` 자동 갱신. 현재 상태(전체 결과) + 이력 테이블(최근 20회 보존). 점수 추이로 개선 효과 객관적 측정 가능.
  - **연결**: `handlers/_ui.py` `cmd_verify_harness()` → `/verify_harness` 텔레그램 명령어 → `hermes_local.py` CommandHandler 등록 → 결과 `00_Meta/verify_harness_report.md` 자동 저장

**루프 안전/구조 (3개 신규 — 2026-06-09)**

> 💡 **빠른 진단이 필요하면** 이 파일 하단 `## 루프 안전 시스템 빠른 진단 가이드` 섹션으로 바로 이동

- `mayor_agent.py`: 🆕 **Mayor 에이전트 (Stage 5 루프 감독관)**
  - **역할**: 에이전틱 루프가 무한 반복 / 토큰 낭비 / 정체되지 않도록 생애주기 전체 감독
  - **핵심 API**: `register(name, max_iters=3, token_budget=12000)` → loop_id 발급 / `tick(loop_id, tokens)` → 예산 초과 시 False 반환(루프 중단 신호) / `finish(loop_id)` / `dashboard()`
  - **설정 상수** (`mayor_agent.py` 상단):
    - `DEFAULT_MAX_ITERS = 3` — 루프 기본 최대 반복 횟수
    - `DEFAULT_TOKEN_BUDGET = 12000` — 루프당 기본 토큰 예산 (**현재 NVIDIA 무료라 금전 의미 없음. 유료 전환 시 이 상수만 낮추면 됨**)
    - `STALE_TIMEOUT = 300` — 5분 이상 tick 없으면 `stale` 경고
  - **텔레그램 사용법**: `/orchestrate mayor` 또는 `/orchestrate loops` → 전체 루프 상태 대시보드
  - **대시보드 아이콘**: `🔄` 실행중 / `✅` 정상완료 / `⛔` 예산초과 강제종료 / `⚠️` 정체
  - **연결**: `harness_agent.py` 에이전틱 루프 시작 시 자동 register → 매 턴 tick → 루프 종료 시 finish. `handlers/_orchestrator.py`의 `/orchestrate mayor` 서브커맨드로 대시보드 출력
  - **장애 징후 & 대응**:
    - 답변 끝에 `⚠️ [Mayor 루프 감독] 에이전틱 루프 조기 종료` 반복 → 작업을 더 작게 나눠 재시도
    - 루프가 응답 없이 멈춘 것 같음 → `/orchestrate mayor` 확인 → `stale` 상태면 재시도
    - 봇 재시작 후 대시보드 비어있음 → 정상 (인메모리, 재시작 시 초기화)

- `tool_result.py`: 🆕 **ToolResult 구조화 + Prompt Injection 방어**
  - **역할 1 (보안)**: 웹 검색/읽기 결과에 `[TOOL_OUTPUT_START]...[TOOL_OUTPUT_END]` 샌드박스 태그를 씌워 LLM이 외부 데이터를 지시문으로 오인하지 않도록 차단
  - **역할 2 (품질)**: 도구 결과에 `next_actions` (권장 다음 행동) 포함 → LLM의 다음 단계 결정 품질 향상
  - **적용 도구**: `[READ:]` / `[WEB_READ:]` / `[SEARCH:]` — 외부 데이터 유입 3종
  - **미적용 도구**: `[RUN_CMD:]` / `[WRITE:]` / `[MOVE:]` — 내부 작업, 외부 데이터 주입 위험 없음
  - **핵심 클래스**: `ToolResult(tool_type, content, ok, artifacts, recovery_hint, next_actions)` → `to_prompt()` / `ToolResult.error(tool_type, msg)` 단축 생성자
  - **연결**: `harness_agent.py` 에이전틱 루프 내 도구 실행 결과에 자동 적용 (`from modules.tool_result import ToolResult as _TR`)
  - **장애 징후 & 대응**:
    - LLM이 웹 검색 결과를 보고 이상한 지시를 따름 → `[TOOL_OUTPUT_START` 태그 정상 생성 여부 로그 확인
    - `ImportError: modules.tool_result` → `Scripts/modules/tool_result.py` 파일 존재 확인

- `harness_agent.py` — Circuit Breaker 섹션 (`_CIRCUIT_BREAKER` dict, 인메모리): 🆕 **엔진별 장애 자동 격리**
  - **역할**: LLM 엔진이 연속 실패 시 해당 엔진을 일정 시간 차단 → 죽은 엔진 반복 호출로 인한 수십 초 지연 방지
  - **설정 상수** (`harness_agent.py`의 `_CIRCUIT_BREAKER` 섹션):
    - `_CB_THRESHOLD = 3` — 몇 회 연속 실패 시 차단?
    - `_CB_TIMEOUT = 60.0` — 차단 후 몇 초간 유지?
  - **동작**: 실패 → `_cb_record_fail(key)` → 3회 누적 → 60초 차단 / 성공 → `_cb_record_success(key)` → 즉시 카운터 초기화
  - **연결**: `_call_primary_with_fallback()` 안에서 엔진 호출 전 `_cb_is_open()` 체크. 폴백 체인도 동일하게 CB 확인 후 차단된 엔진은 건너뜀
  - **장애 징후 & 대응**:
    - 텔레그램 알림 `⚠️ [자동 폴백 발동]` 수신 → 특정 엔진 장애. 60초 후 자동 복구됨
    - 로그: `⛔ [CircuitBreaker] GPT OSS 120B — 3회 연속 실패 → 60초 차단`
    - 즉시 복구 필요 시: `/restart_bot` → 인메모리 `_CIRCUIT_BREAKER` dict 초기화

**로드밸런싱/크론 (4개)**
- `load_balancer.py`: ModelLoadBalancer v1.0 (v9.2) — 성능 가중치 기반 모델 선택, 성공/실패/지연시간 기록, 주기적 가중치 재조정. SQLite 기반.
- `cron_manager.py`: CronManager
- `natural_language_cron.py`: 자연어 크론 스케줄러
- `dream_scheduler.py`: Self-Healing Loop v1.0

**검색/논문 (5개)**
- `hyde_search.py`: HyDE 검색 (가상 문서 기반 검색)
- `news_engine.py`: 뉴스 엔진
- `paper_ingest.py`: 논문 인제스트
- `paper_bundle_manager.py`: 논문 번들 관리
- `ontology_graph.py`: 온톨로지 그래프

**기타 (12개)**
- `cascade_engine.py`: 계단식 엔진 (ontology + graphify 연동 가능)
- `cli_anything.py`: CLI anything 래퍼
- `config_loader.py`: 설정 로더
- `gdrive_failover.py`: Google Drive 장애 조치
- `git_manager.py`: Git 저장소 관리
- `meta_updater.py`: 메타 파일 자동 업데이트 (현재 비활성화)
- `policy_engine.py`: 정책 엔진
- `pre_question.py`: 사전 질문 처리
- `profile_manager.py`: 프로필 관리
- `resolver.py`: 리졸버
- `tag_linker.py`: 태그 링커
- `vault_deduplicator.py`: Vault 중복 제거
- `web_reader.py`: 웹 리더
- `kanban_manager.py`: 칸반 관리자

**서브패키지 (2개)**

- `dialectic_layer/` (6개 파일): `__init__.py`, `_base.py`, `_fts.py`, `_handoff.py`, `_dreamer.py`, `_deriver.py`, `_dialectic.py` — 변증법 계층 엔진
- `graph_memory/` (3개 파일): `graph_engine.py`, `memory_consolidator.py`, `__init__.py` — 그래프 메모리 엔진

---

### `/Users/bluesea/Applications/Mjobsidian/wiki/00_Meta/` (메타 정보 및 장부 폴더)

- `00_Meta_지도.md`: 00_Meta 폴더 내 혼재된 40여 개의 파일들을 범주화한 지도 문서
- `스크립트 정보.md` (현재 문서): 시스템 전체 작동 원리 및 가이드
- `시스템 인벤토리.md`: 설치된 런타임, 포트 및 환경 구성표
- `시스템 상태.md`: 엔진 변경 이력과 현재 에이전트 작업 상황
- `주요 시스템 가이드 및 FAQ.md`: 시스템 문제 해결 및 잦은 질문/이력 보관소
- `USER.md`: 박사님(사용자)의 선호 AI 스택(llama.cpp 등)과 페르소나/프롬프트 설정
- `hot.md`: 에이전트와 박사님이 공유하는 프로젝트 핫토픽 및 투두 리스트
- `HERMES3_MASTER_DEVELOPMENT_GUIDE.md`: 마스터 개발 가이드 (v9.2)

---

### `/Users/bluesea/Applications/Mjauto/Scripts/tests/` (테스트 스위트 — 11개 테스트 파일 + 1개 __init__)

| 파일 | 설명 | 테스트 수 |
|:---|---|:---|
| `__init__.py` | 패키지 초기화 | — |
| `conftest.py` | 공통 픽스처 | — |
| `test_imports.py` | 임포트 검증 | — |
| `test_bio_memory.py` | 바이오 메모리 엔진 테스트 | — |
| `test_hybrid_router.py` | 하이브리드 라우터 테스트 | — |
| `test_kanban_manager.py` | 칸반 관리자 테스트 | — |
| `test_skill_evolver.py` | 스킬 에볼버 테스트 | — |
| `test_core_reducer.py` | Core Reducer 테스트 | — |
| `test_sia_engine.py` | SIA 엔진 테스트 (v9.2) | 12개 |
| `test_load_balancer.py` | 로드밸런서 테스트 (v9.2) | 10개 |
| `test_monitoring_engine.py` | 모니터링 엔진 테스트 (v9.2) | 12개 |

> 11개 테스트 파일 (v9.3.2). test_layer_harness.py 추가 (19개 레이어 격리 테스트, 1.31초 실행).

---

## meta_updater.py 정의 및 위치

- **위치**: `/Users/bluesea/Applications/Mjauto/Scripts/modules/meta_updater.py`
- **목적**: 메타 폴더 내 MD 파일들의 사용 정의와 업데이트 가이드를 자동으로 생성·갱신하는 스크립트.
- **상태**: 비활성화 — 현재 `__main__` 블록에서 실행 차단 (print만 하고 종료). 나중에 적응 후 하단 주석 해제로 활성화 가능.
- **주요 기능**
  - `generate_usage_guide()` → `meta 폴더 파일 저장 방법.md` 생성
- **사용법** (활성화 시)
  ```bash
  python3 /Users/bluesea/Applications/Mjauto/Scripts/modules/meta_updater.py
  ```

---

## graphify 연동 (v9.2.5)

- **패키지**: `graphifyy 0.8.28` (pip 설치됨)
- **연동 핸들러**: `handlers/_vault.py` — `cmd_graph()` 함수
- **명령어**: `/vault graph` — vault 스캔 → extract → build → god_nodes → to_html → to_obsidian
- **출력물**: `graphify-out/` 디렉토리 (vault 루트 → 이관 완료)
- **핵심 기능**: 고립 노드(isolated nodes) + 허브 문서(hub documents) 탐지 리포트 반환

---

## 최근 변경

### 2026-06-28 — MJcoin signal_tracker_coin 고도화 + screener signal_tracker star/circle_type + chart.py 분석카드

**프로젝트 루트**: `/Users/bluesea/Applications/Mjstock/Coin`, `/Users/bluesea/Applications/Mjstock/screener`

**변경 모듈**

| 파일 | 변경 내용 |
|---|---|
| `Coin/signal_tracker_coin.py` | score_delta(직전 스캔 대비 점수 변화) / ATR14 기반 suggested_position_krw / star_type / circle_type 필드 추가 — 총 25컬럼 |
| `Coin/run_scan_coin.py` | `_quant_snapshot`에 atr14 반환. 스캔/개별조회 끝에 `record_scan()` / `record_single()` 자동 호출 |
| `Coin/batch_fill_returns_coin.py` | `_collect_csv_files()` 신설(results/ 재귀 탐색). `fill_returns()` 자동 호출 |
| `screener/signal_tracker.py` | star_type / circle_type 필드 추가. `record_scan()`에서 시그널 종류 자동 추출 저장 |
| `screener/chart.py` | `_inject_analysis_card()` 신규. `save_chart()` / `save_farming_chart()` / `save_chowuryang_chart()` 에 카드 주입 호출 추가 |

**신규 함수**

| 함수 | 위치 | 설명 |
|---|---|---|
| `_inject_analysis_card(html, data)` | `screener/chart.py` | 차트 HTML 하단에 분석 카드(현재가·RSI14·RVOL·EMA20·MACD Hist·통과 검색기·진입 기준가) 삽입 |
| `_collect_csv_files()` | `Coin/batch_fill_returns_coin.py` | results/ 루트 + 서브폴더 재귀 검색으로 CSV 목록 반환 |

**에러 진단 포인트**:
- `suggested_position_krw` = 0 → `run_scan_coin.py` `_quant_snapshot` atr14 키 누락 확인
- 분석 카드 미표시 → `chart.py` save 함수 끝 `_inject_analysis_card()` 호출 확인
- `star_type` 빈 칸 → `signal_tracker.py` `record_scan()` BUY_SIGNALS 리스트 확인

---

### 2026-06-30 (3차) — BUG-006 ok/pass 필터 + BUG-007 정규화 제거 + 퀀트 저장 확인

**프로젝트 루트**: `/Users/bluesea/Applications/Mjstock`

**수정 파일**

| 파일 | 변경 내용 |
|---|---|
| `handlers/_stock_mjstock.py` | **BUG-006**: `_handle_mjstock_chart()` `bool_items` 필터에 `and k not in ("ok", "pass")` 추가. 제외 기준 4가지 확정: `exp_` prefix / `entry_` prefix / `ok` / `pass`. |
| `screener/ticker_analyze.py` | **BUG-007**: 정규화 로직(최고점=100 스케일링) 제거 → 절대 점수 정렬. `bool_vals` 계산에 4가지 제외 필터 적용 (scan_single.py와 통일). |
| `screener/scan_single.py` | **BUG-007**: `bool_cols` 계산에 4가지 제외 필터 적용 (ticker_analyze.py와 통일). 목록/상세 점수 계산 기준 일치. |

**퀀트 저장 확인 (2026-06-30)**:
- 전체 20개 검색기 results/ 정상 저장 확인. chowuryang 82종목 17컬럼, chowuryang_kr 13종목, samdoli_kr 10종목.

**에러 진단 포인트**:
- `✅ ok` / `❌ pass` 조건표 노출 → `_handle_mjstock_chart()` `k not in ("ok", "pass")` 필터 확인 (06번 BUG-006)
- 목록 100점 / 상세 67점 불일치 → `ticker_analyze.py` 정규화 제거 여부 + bool 필터 4가지 제외 기준 통일 여부 (06번 BUG-007)

---

### 2026-06-30 (2차) — run_fetch_fundamentals.sh 신규 + scan_single ticker 연동 + 삼돌이 표시명 + entry_ 필터

**프로젝트 루트**: `/Users/bluesea/Applications/Mjstock`

**신규 파일**

| 파일 | 역할 | 위치 |
|---|---|---|
| `run_fetch_fundamentals.sh` | 재무 캐시 자동 갱신 wrapper — nasdaq1000 → kospi200 → kosdaq150 순차 실행. `com.mjstock.fetch_fundamentals.plist`에서 호출. | `Mjstock/` 루트 |

**수정 파일**

| 파일 | 변경 내용 |
|---|---|
| `screener/scan_single.py` | chowuryang/chowuryang_kr 특수 케이스: `check_fn(df, ticker=ticker)` 전달 (재무 캐시 연동). samdoli/samdoli_kr 특수 케이스: `df_5min`, `df_30min` 전달. 기존 else 분기의 ticker="" 전달 버그 수정. |
| `handlers/_stock_mjstock.py` | `_MJSTOCK_SCREENER_NAMES`에 `samdoli`/`samdoli_kr` 추가 ("🔺 삼돌이(US)"/"🔺 삼돌이(KR)"). `_handle_mjstock_chart()` bool_items 필터에 `not k.startswith("entry_")` 추가. |
| `auto_scan_morning.py` | 에러 로깅 개선: `err_msg` 400자 제한 + RC 명시 출력. |
| `~/Library/LaunchAgents/com.mjstock.fetch_fundamentals.plist` | 실행 명령 변경: `fetch_fundamentals.py --universe nasdaq1000` → `run_fetch_fundamentals.sh` (KR 포함). |

**에러 진단 포인트**:
- `/mjstock TICKER` 초우량주 proxy 평가 → `scan_single.py` chowuryang 특수 케이스 분기 존재 여부 확인
- 삼돌이 raw key 표시 → `_MJSTOCK_SCREENER_NAMES` samdoli 항목 확인
- entry_45day 조건표 노출 → `_handle_mjstock_chart()` `entry_` prefix 필터 확인
- KR 재무 토요일 미갱신 → `com.mjstock.fetch_fundamentals.plist` 실행 명령 확인

---

### 2026-06-30 — MJcoin + MJstock 통합 개선 (UI·텔레그램·버그 수정·재무 필터)

**프로젝트 루트**: `/Users/bluesea/Applications/Mjstock`

**신규 모듈**

| 파일 | 역할 | 위치 |
|---|---|---|
| `fetch_fundamentals.py` | yfinance 기반 재무 지표 캐시 — PER/PBR/ROE/부채비율 주간 자동 수집. **🔄 2026-06-30 KR 유니버스 추가**: `kospi200`/`kosdaq150` 지원, `KR_SUFFIX` 상수, `get_tickers()` → `(cache_key, yf_ticker)` 튜플 반환. launchd: `com.mjstock.fetch_fundamentals` → `run_fetch_fundamentals.sh` wrapper (매주 토요일 06:00, US+KR 자동 갱신). | `screener/` |

**수정 파일**

| 파일 | 변경 내용 |
|---|---|
| `Coin/app_coin.py` | 차트 높이 720→1200, scrolling=True. 검색기 결과 UI: 신호 있는 A급 우선 표시, expander 클릭 시 스캐너 탭에서 바로 차트 표시 |
| `app.py` | 스캔 중단 버튼 추가(사이드바). 차트 높이 700→1200, scrolling=True. **🔄 2026-06-30 차트탭 콤보박스 타임스탬프 표시**: `우량주농사` → `우량주농사 20260630_1920` 형식. CSV glob → 파일명 파싱 → 날짜시간 접미. |
| `Coin/run_scan_coin.py` | 텔레그램 메시지 형식: "신호(N개), 점수(N개)" 형식으로 변경. 검색기별 "결과보기" 버튼 추가. 스캔 결과 캐시 저장 |
| `Coin/batch_fill_returns_coin.py` | **버그 수정**: `iloc[-1]`(항상 최신 가격) → target_dt 기준 일봉 매칭으로 수정. days_ago 동적 계산 추가 |
| `screener/screen_chowuryang.py` | `fetch_fundamentals.py` 재무 캐시 연동 코드 추가 |
| `screener/screen_chowuryang_kr.py` | **🔄 2026-06-30 재무 캐시 연동 추가**: `_FUND_CACHE = json.load(open("data/fundamentals_cache.json"))` 파일 상단 로드. `check_chowuryang_kr()` 시그니처에 `ticker=""` 추가. D/E/F: 캐시 있으면 실제 재무(revenue_growth/op_margin/net_margin), 없으면 proxy fallback. |
| `screener/run_scan.py` | **🔄 2026-06-30 라인 ~931 ticker 전달 추가**: `check_fn(df_daily, market_cap=market_cap, ticker=code)`. KR 초우량주 재무캐시 연동용. |
| `handlers/_stock_coin.py` | 코인 검색기 결과 버튼 콜백 핸들러 추가. **🔄 2026-06-30 대규모 갱신** — 3단계 네비게이션, 결과 포맷 변경, coin_all 뒤로가기 (상단 항목 참조) |

**핵심 버그 수정 — batch_fill_returns_coin.py**
- **증상**: 수익률 자동 채움 시 3d/7d 모두 현재 가격으로 기록됨
- **원인**: `df_ohlcv.iloc[-1]` = DataFrame 마지막 행(=최신 봉). target_dt 경과 여부 무관하게 현재 가격 반환
- **수정**: `df_ohlcv[df_ohlcv['date'] >= target_dt].iloc[0]['close']` — 목표 날짜 이후 첫 봉 종가 사용
- **주의**: launchd `com.mjstock.fill_returns_coin` 등록 완료 (매일 10:00). 3d/7d 경과 전에는 빈 값이 정상

**에러 진단 포인트**:
- 수익률 0 또는 현재가 그대로 → `batch_fill_returns_coin.py` target_dt 매칭 로직 확인
- 재무 필터 미작동 (초우량주 100개↑) → `fundamentals_cache.json` 존재 여부 확인 후 `fetch_fundamentals.py --universe russell1000` 실행
- 텔레그램 "결과보기" 버튼 무응답 → `_stock_coin.py` CallbackQueryHandler 등록 확인
- KR 재무 미적용 → `fundamentals_cache.json`에 KR 6자리 코드 key 존재 여부 확인 (`json.load()` 후 keys 조회)

---

### 2026-06-28 — MJcoin signal_tracker_coin 신설 + batch_fill_returns 서브폴더 버그 수정

**프로젝트 루트**: `/Users/bluesea/Applications/Mjstock/Coin`

**신규 모듈**

| 파일 | 역할 |
|---|---|
| `Coin/signal_tracker_coin.py` | 코인 시그널 트래커 — 스캔 결과 CSV 누적 저장·수익률 자동 채움·요약 조회 |

**주요 함수**

| 함수 | 설명 |
|---|---|
| `record_scan(results, screener)` | 전체 스캔 결과 `tracker/signal_log.csv`에 기록 |
| `record_single(symbol, result)` | 개별 `/coin SYMBOL` 조회 결과 기록 |
| `fill_returns()` | 진행 중(active) 행의 ret_1d/ret_3d/ret_7d 자동 채움 + verdict 확정 |
| `get_summary()` | 검색기별 성공률·평균 수익률 요약 반환 |

**변경 파일**

| 파일 | 변경 내용 |
|---|---|
| `Coin/run_scan_coin.py` | `run_scan()` / `scan_single()` 끝에 `record_scan()` / `record_single()` 자동 호출 추가 |
| `Coin/batch_fill_returns_coin.py` | `_collect_csv_files()` 신설 — 루트+서브폴더(samdoli/uryangju 등) 재귀 탐색. 버그 수정(루트만 탐색하던 문제). `fill_returns()` 함께 호출. |
| `docs/MJcoin_사용설명서.html` | 섹션 14: 흐름 순서도 + 주식vs코인 교차비교표(11항목) 추가 |
| `docs/MJstock_사용설명서.html` | 섹션 18: 흐름 순서도 + 주식vs코인 교차비교표(11항목) 추가 |

**에러 진단 포인트**:
- `signal_log.csv` 미생성 → `Coin/tracker/` 디렉토리 존재 여부 확인
- 서브폴더 CSV 누락 → `_collect_csv_files()` 재귀 탐색 동작 여부 확인

---

### 2026-06-28 — hermes_stock_bot AI 채팅 + signal_tracker 대폭 개선 + quant CSV 저장

**프로젝트 루트**: `/Users/bluesea/Applications/Mjauto/Scripts/`, `/Users/bluesea/Applications/Mjstock/screener/`

**hermes_stock_bot.py 신규 기능**

| 항목 | 설명 |
|---|---|
| `_call_gpt_oss()` | openai/gpt-oss-120b 호출. `max_tokens=2048` 필수(reasoning model). `content=None` 시 `reasoning_content` fallback. |
| `_history` | 채팅방별 최대 20메시지 히스토리 (dict[chat_id, list]). |
| `handle_text()` | MessageHandler — 일반 텍스트 수신 → AI 응답 파이프라인. |
| `/help` 명령어 | 주식 관련 HELP_TEXT 출력. |
| `CallbackQueryHandler` 추가 | `callback_mjstock`, `callback_mjstock_results` inline 버튼 작동. |

**signal_tracker.py 신규/변경**

| 항목 | 설명 |
|---|---|
| `BUY_SIGNALS` 상수 | 8종: BUY_URYANGJU/BUY_SELYEOK/NONGSSA/RAPID_ACCUM/GONGJOONG/NAKPOK/REVERSAL_EARLY/MACD_BB_BUY |
| `SELL_SIGNALS` 상수 | 2종: SHORT_PEAK/MACD_BB_SELL — circle_date 자동 기록 트리거 |
| `star_signal_date` 수정 | BUY_SIGNALS 8종 해당 시만 세팅. SCAN_PASS는 ★ 세팅 안 함. |
| `circle_date` 자동 기록 | SELL_SIGNALS 발생 시 active 행의 circle_date/circle_price 업데이트 (최초 1회, circle_date="" 행만) |
| `exited` 탐지 개선 | exit_price = price_map 대신 마지막 기록된 close_price. circle_date 없이 exited → verdict="⚠️신호없음" |
| `return_30d` 필드 추가 | SIGNAL_LOG_FIELDS 스키마 누락 수정 |
| `update_returns()` 기준 변경 | r10 → r30 (30일 수익률) 기준으로 verdict 확정 |

**scan_all_ticker.py / run_scan.py / _stock.py 변경**

| 파일 | 변경 내용 |
|---|---|
| `scan_all_ticker.py` | output dict에 close_price/atr14/passed_screeners 추가. record_scan() 호출로 signal_log.csv 저장. JSON 파싱 전 `{` 시작 줄만 추출. |
| `run_scan.py` | save_scan_result() 이후 record_scan() 추가 호출. BUY_ 계열 + 4종 시그널 자동 추출. |
| `handlers/_stock.py` | 860/930줄: `json_line = next(...)` 패턴으로 비-JSON stdout 줄 필터링. |

**에러 진단 포인트**:
- AI 응답 없음 → `_call_gpt_oss` max_tokens 확인(최소 2048), reasoning_content fallback 로직 확인
- signal_log.csv 미생성 → `tracker/` 디렉토리 존재 여부 확인
- JSON 파싱 오류 → stdout에 [Tracker] 로그 섞임 → `{` 시작 줄 필터링 적용 확인

---

### 2026-06-24 — MJstock HTML 결과 리포터 + NASDAQ 1000개 확장 + app.py Tab 2 개편

**프로젝트 루트**: `/Users/bluesea/Applications/Mjstock/`

**신규 파일**
- `generate_results_html.py` (NEW): 스캔 결과 자기완결형 HTML 생성기. CDN Plotly.js 2.27.0 사용으로 파일 크기 ~1.4MB. 시그널 종목(`has_buy_signal=True`) 상단 정렬 → 점수 종목 하단. 종목 행 클릭 시 `toggleChart()` 함수로 Plotly 차트 인라인 펼침. 내부 헬퍼: `_extract_plotly_calls()`, `_signal_badge()`, `_indicator_chips()`, `_row_html()`. 출력 경로: `results/_html/results_{screener_key}_{date}.html`.
- `mjstock_callback_bot.py` (NEW, inactive): 독립 콜백 봇 데몬. 409 Conflict(동일 토큰 이중 폴링) 문제로 미사용. Hermes 봇에 콜백 핸들러 추가하는 방식으로 해결.

**수정 파일**
- `auto_scan_morning.py`: `run_screener()` 반환값 구조화(`{ok, count_score, count_signal, date_str}`). 스캔 성공 시 `generate_results_html()` 자동 호출. `send_results()` 신규(검색기별 시그널/점수 카운트 텔레그램 메시지). `_send_telegram_with_buttons()` 신규(인라인 키보드 버튼 포함). `SCREENER_NAMES_LOCAL` 딕셔너리 추가. 기존 차트 파일 첨부 방식 완전 제거.
- `screener/data_loader.py`: `_update_nasdaq()` — Wikipedia NASDAQ-100 → NASDAQ Screener API(`https://api.nasdaq.com/api/screener/stocks?...`) 교체. 시총 기준 상위 1000개 → `nasdaq1000.csv`. `_parse_cap()` 신규(T/B/M suffix 없는 순수 달러 숫자 파싱). **버그 수정**: `c.lower()` → `str(c).lower()` — DataFrame 컬럼명이 integer일 때 AttributeError 방지.
- `app.py`: Tab 2 스캔 탭 — selectbox → checkbox 그리드(2열). [▶️ 스캔 시작] 버튼 우상단 배치. `st.empty()` 플레이스홀더로 스캔 진행 상황 표시. 선택된 검색기만 순차 실행.
- `docs/MJstock_사용설명서.html`: 새 텔레그램 메시지 포맷 안내, 시그널 vs 점수 구분 표, [결과 보기] 버튼 동작 설명 추가.

**Hermes 봇 연동 (Mjauto/Scripts/)**
- `handlers/_callbacks.py`: `mjstock_results__` prefix 콜백 라우팅 추가 (line 64-73).
- `handlers/_stock.py`: `callback_mjstock_results()` 신규 — `mjstock_results__{screener_key}__{date_str}` 파싱, HTML 없으면 실시간 생성, `send_document`로 전송.

**에러 진단 포인트**:
- NASDAQ 1000개 미갱신 → `str(c).lower()` 패치 적용 여부 확인. API 응답에 `marketCap` 컬럼이 정수형으로 올 수 있음.
- HTML 버튼 무반응 → Hermes `_callbacks.py`의 `mjstock_results__` prefix 라우팅 등록 여부 확인.
- 두 봇 동시 실행 → 409 Conflict → `mjstock_callback_bot.py` unload 상태 유지 필수.

---

### 2026-06-30 (2차) — date_str 형식 수정 + 텔레그램 발송 방식 전환

**수정 파일**:
- `auto_scan_morning.py`: **date_str 형식 변경** `%Y%m%d` → `%Y%m%d_%H%M%S` (타임스탬프 포함 — CSV glob 매칭 정확도 향상). **텔레그램 발송 형식 변경**: 기존 벌크 텍스트 → `신호(N개), 점수(N개)` 요약 + `mjstock_scan_list:{key}:{ts}` InlineKeyboard 콜백 버튼. **에러 로깅 개선**: `result.stderr[:300]` → `(result.stderr or result.stdout or "(출력 없음)")[:400]` + RC 표시(이중 실행 충돌 시 stderr 비어도 진단 가능).
- `auto_scan_intraday.py`: `auto_scan_morning.py`와 동일한 date_str 형식, 발송 방식, 에러 로깅 개선 동일 적용.
- `app.py`: **`send_scan_result_telegram()` 시그니처 변경**: `(name, cnt)` → `(name, cnt, screener_key=None, df_r=None, date_str=None)`. 발송 내용 plain text → `신호(N개), 점수(N개)` + InlineKeyboard 버튼. **스캔 루프 블로킹 제거**: `while proc.poll() is None: time.sleep(1)` → `session_state` 폴링 + `st.rerun()` 비동기 방식.

**에러 진단 포인트 (추가)**:
- 스캔 완료 후 텔레그램 버튼 없음 → `app.py` `send_scan_result_telegram()` 호출부에 `screener_key/df_r/date_str` 인수 전달 여부 확인
- CSV glob 매칭 실패 → date_str 형식이 `%Y%m%d_%H%M%S` 인지 확인 (구형 `%Y%m%d`와 혼재 시 glob 미매칭)
- RC!=0 + stderr 비어있음 → 이중 실행 충돌 시그니처. `pgrep -f auto_scan` 으로 프로세스 중복 확인

---

### 2026-06-21 — nightly changelog 자동화 (auto_dream_trigger)

- **`auto_dream_trigger.py`** — `write_changelog()` 함수 신규 추가
  - Dreaming 성공 직후 자동 실행 (기존 코드 무변경, 2줄 추가만)
  - 수집 소스 3개: `auto_dream.log`(Dreaming 완료 항목) + `05_시스템 상태.md`(✅ 완료 줄) + `01_hot.md`(오늘 날짜 줄)
  - 출력: `wiki/00_Meta/CHANGELOG.md` — `## YYYY-MM-DD` 섹션으로 자동 추가
  - 안전장치: atomic write(tmp→rename), 오늘 날짜 중복 시 스킵, 변경사항 없으면 파일 미생성, 실패해도 Dreaming 결과에 영향 없음
  - **배경**: Loop Library "nightly changelog loop" 패턴 적용 — `01_hot.md` 수동 관리 보완

### 2026-06-21 — Karpathy LLM-Wiki 패턴 적용 (wiki_manager + _meta)

- **`modules/wiki_manager.py`** — 메서드 2개 신규
  - `write_wiki(rel_path, content, overwrite=False)`: 대화 분석 결과를 wiki 마크다운 페이지로 저장. 경로 탈출(`../`) 방어, atomic write(tmp→rename), 타임스탬프 헤더 자동 삽입, `index.md` 자동 갱신(없으면 생성). overwrite=False 시 기존 파일 덮어쓰기 차단.
  - `lint_wiki(stale_days=30)`: wiki 건강 점검 4종 탐지 — ①고아 페이지(어디서도 `링크` 미참조), ②오래된 페이지(N일 이상 미수정), ③깨진 내부 링크(`대상` 파일 없음), ④빈 페이지(본문 50자 미만). 텔레그램 요약 + 항목별 리스트 반환.
- **`handlers/_meta.py`** — 핸들러 2개 신규 (`/claude_brief` 기존 유지)
  - `/save_wiki [경로] [--overwrite]`: `context.user_data["last_save_content"]` 에 저장된 내용을 wiki 페이지로 기록. LLM `[SAVE]...[/SAVE]` 태그 처리와 연동. 경로 미지정 시 `분석/YYYYMMDD_HHMMSS.md` 자동 생성.
  - `/wiki_lint [일수]`: lint_wiki 결과를 텔레그램으로 전송. 기본 30일, 인자로 기준 변경 가능(`/wiki_lint 60`).
- **`hermes_local.py` 등록 필요** (수동 2줄):
  ```python
  from handlers._meta import cmd_claude_brief, cmd_save_wiki, cmd_wiki_lint
  app.add_handler(CommandHandler("save_wiki", cmd_save_wiki))
  app.add_handler(CommandHandler("wiki_lint", cmd_wiki_lint))
  ```
- **배경**: Karpathy llm-wiki gist 패턴 분석 결과, 하네스 wiki 시스템이 이미 70% 구현 완료 상태였음. 미구현 갭 2개(쿼리결과 wiki 누적, wiki lint)만 추가 적용.

### 2026-06-11 — 논문 5편 기반 Hermes 강화 (v9.3.2)

- **`modules/agentic_loop.py`**: `_verify_gate()` + `_FAILURE_SIGNALS` 추가 (Goal-Autopilot)
  - RUN_CMD/CREATE 실행 후 결과 검증. 실패 감지 시 VerificationGate 경고 → 거짓 완료 보고 구조적 차단
- **`modules/memory_refinement.py`**: Sycophancy 필터 + HORMA 계층 검색 추가
  - `should_store()`: 아첨 패턴(80자 미만 동의 응답) 감지 → 저장 차단
  - `hybrid_recall()`: context_tags 클러스터 기반 계층 검색 → 전체 L2 브루트포스 대비 효율화
- **`modules/skill_auditor.py` 신규**: Runtime Skill Auditor
  - ~/.hermes/skills/ 전체 스캔, 위험 패턴 10종 탐지, 3단계 위험도 분류
  - 감사 로그: `~/.hermes/runtime/skill_audit.log`
- **`tests/test_layer_harness.py` 신규**: Layer-Isolated Evaluation Harness
  - 19개 결정론적 테스트, LLM 없이 1.31초 실행, 4개 레이어 독립 검증

### 2026-06-11 — 봇 이중 기동 근본 해결 + /claude_brief 버그 수정

- **`hermes_local.py`**: `_ensure_single_instance()` 신규 함수 추가
  - `fcntl.flock` → PID 파일(`~/.hermes/hermes_local.pid`) 기반 자동 교체 방식으로 전환
  - 구 프로세스 SIGTERM → 5초 대기 → SIGKILL 순서로 강제 교체. 항상 단일 인스턴스 보장
  - `import signal, time` 추가
- **`handlers/_meta.py`**: `from handlers._base import safe_reply, safe_edit` import 추가
  - bulk 교체 시 누락됐던 import로 인해 `/claude_brief` 명령어 NameError 발생하던 것 수정

### 2026-06-11 — 하네스 업그레이드 로드맵 P1+P2 완료 (v9.2.6 → v9.3.1)

#### P1 완료 항목
- **`auto_dream_trigger.py`** (Scripts 루트): L2 미처리 ≥20개 시 L3 증류 자동 실행. DeepSeek API 직접 호출 (봇 스택 의존 없음). `com.hermes.autodream.plist` launchd 매주 일요일 03:30. 🆕 **2026-06-21**: `write_changelog()` 추가 — Dreaming 성공 후 `auto_dream.log` + `05_시스템 상태.md` + `01_hot.md` 3개 소스에서 오늘 변경사항 수집 → `wiki/00_Meta/CHANGELOG.md` 자동 기록 (atomic write, 중복 방지, 실패 시 메인 프로세스 무영향).
- **`handlers/_base.py` — `safe_reply()`/`safe_edit()` 헬퍼 추가**: Markdown `parse_mode` 실패 시 plain text 자동 폴백. 전체 핸들러 **255곳** 적용 (결함 #4 완전 해소).
- **`ingest_text_utils.py` 신규**: `ingest_engine.py` 642줄에서 순수함수 분리 → 459줄로 축소
- **`indexer_text_utils.py` 신규**: `knowledge_indexer.py` 631줄에서 순수함수 분리 → 537줄로 축소

#### P2 완료 항목
- **`memory_refinement.py` — `hybrid_recall()` 3소스 확장**: L2 에피소딕 + knowledge_indexer FTS5 + `semantic_index.db` FTS5 (00_Meta 82개 문서) 통합 검색
- **GitHub Remote 연결**: `harness_scripts` + `harness_wiki` SSH 원격 연결 완료
- **`com.hermes.gitbackup.plist` 신규**: 매주 일요일 04:00 Scripts+Wiki 자동 push + 텔레그램 알림

#### 주식 관련 (별도 세션)
- **`stock_fetcher.py`**: `yf.download()` 전환 + 일별 JSON 캐시 (`market_context_cache.json`) — `/scan` rate limit 장애 해소
- **`_stock.py`**: V_FINAL v1.4 ADX 점수 + 4티어 스캔 신규 추가

### 2026-06-02 — graphify 연동 + 스크립트 정보.md 전수 갱신 (v6.8→v9.2.5)
- **신규 핸들러 4개 등록**: `_orchestrator.py`, `_kanban.py`, `_grill.py`, `_ui.py` — handlers 실제 11개 모듈 반영
- **modules 전수 갱신**: 53개 모듈 + graph_memory/ 서브패키지 3파일 — v6.8 대비 누락된 30+개 모듈 전부 등록
- **루트 스크립트 전수 갱신**: 20개 .py + 5개 .sh + 데이터 파일 — hybrid_router, index_db군, agent_harness 등 추가
- **tests 전수 갱신**: 9개 테스트 파일 (v9.2 SIA/Monitoring/LoadBalancer 테스트 포함)
- **디렉터리 구조**: inbox/outbox/_archive/_backup/backup_plists/working 서브디렉터리 등록
- **graphify 연동 섹션 신설**: `/vault graph` 명령어 + graphifyy 0.8.28 명세

### 2026-06-02 — Hermes 2 WebUI 대화 맥락 단절 및 폴더 격리 확보
- **SWA 캐시 무효화 버그 수정**: `start_llama.sh`를 `llama-server -np 1`, `--cache-reuse 256`으로 수정하여 KV 캐시 무효화 방지
- **조기 Truncation 문제 수정**: `.hermes2/config.yaml`의 컨텍스트 엔진을 `truncation`으로, `threshold`를 0.85로 상향하여 대화 단절 및 메모리 누수 방지
- **경로 오염 방지 정책 확립**: Hermes 1(`~/.hermes`)과 Hermes 2(`.hermes2`)의 혼용을 원천 차단하고 `llama-context-fix` 스킬 기록

### 2026-06-01 — v9.2 SIA 피드백 학습 + 모니터링 엔진 + 멀티 모델 로드 밸런싱
- **신규 모듈 3개**: `modules/sia_engine.py`, `modules/monitoring_engine.py`, `modules/load_balancer.py`
- **통합**: `modules/core_reducer.py` — SIA/Monitoring 피드백 라우팅. `hybrid_router.py` — 로드밸런서 연동
- **테스트**: `tests/test_sia_engine.py` (12개), `tests/test_monitoring_engine.py` (12개), `tests/test_load_balancer.py` (10개). **전체 150/150 전면 통과**

### 2026-06-01 — PermissionBridge 2-tier + Ingest Inbox Deferral 구현
- `modules/permission_bridge.py`: 2-tier 도구 인가 게이트웨이
- `harness_agent.py`: RUN_CMD/SAVE/파일작업 승인 경유
- `ingest_engine.py`: 저신뢰(Unsorted)/파싱 실패 → Inbox deferral

### 2026-05-31 — /restart_bot 버그 수정 + /run 제거
- `handlers/__init__.py`: `cmd_restart_bot` import 추가
- `handlers/_system.py`: `cmd_run_cmd` + 화이트리스트 제거
- `hermes_local.py`: `/run` CommandHandler 등록 제거

### 2026-06-03 — Ingest TagLinker vault_path 오류 수정 + HERMES3_ENCYCLOPEDIA.md write_file 사고
- `modules/ingest_engine.py` line 20: `TagLinker(vault_path=...)` → `TagLinker()` 수정 (tag_linker.py는 db_path만 받으므로)
- **HERMES3_ENCYCLOPEDIA.md** (1108줄)가 실수로 write_file로 덮어써져 77줄로 축소됨 — 복원 필요
- **교훈**: 기존 파일 수정은 write_file 금지, patch만 사용 — CRITICAL 안전 규칙

### 2026-06-03 — PKM_2 Knowledge Mesh v1.0 구현 완료 (4개 모듈 + /research 통합)
- `modules/knowledge_mesh_orchestrator.py` (359줄) — JSON 레시피 기반 DAG 실행. arXiv/Semantic Scholar 검색, 로컬 의미 검색, 타임라인 병합, 교차 참조, LLM 요약. LRU 캐시 128개.
- `modules/timeline_builder.py` (77줄) — 통합 타임라인 정렬 + arXiv ID/DOI 중복 병합. 다중 날짜 형식 지원.
- `modules/cross_reference_analyzer.py` (131줄) — TF-IDF 코사인 유사도 + 시간 감쇠. alignment_type(predict_and_realize / retrospective_match). 임계값 0.7.
- `modules/auto_topic_manager.py` (178줄) — TF-IDF 기반 주제 분류. 5개 기본 템플릿(AI/ML, 법학/AI 규제, 연구 방법론, 자동화/시스템, 기타/미분류). ~/.hermes/runtime/pkm_topics.json 저장.
- `handlers/_research.py` (796줄) — `/research` 명령어로 Knowledge Mesh 통합. 서브명령어: stats, topics, classify, classifyall, recluster, local, tl(timeline), xref(crossref), 전체 파이프라인. 기존 논문 핸들러(/paper)와 병합 상태.
- **의존성**: `knowledge_indexer.py` (HybridKnowledgeIndexer) 활용 — TF-IDF, FTS5, 벡터 검색 재사용. 외부 pip 패키지 불필요.
- **테스트**: 기능 통합 테스트 완료.
- **2026-06-03 — constitution.local.md §X 지시 과잉 행위 금지 규칙 추가**
- §X 지시 과잉 행위 금지: 허가 없는 파일 생성 금지, 선제적 판단 금지, 직전 패턴 무비판적 재사용 금지, 사전 확인(search_files/read_file) 의무, 목표와 수단 구분 의무, 위반 시 복구 절차 명시
- constitution.local.md 버전 1.4 → 1.5

### 2026-06-04 — 에이전트 세션 초기화 프로토콜 신설 (Initialization Protocol)
- **문제**: WebUI 재시작 등 세션 리셋 시 헌법 규칙(타임스탬프, 태그)을 망각한 채 파일을 생성하는 문제 발생.
- **해결**: `USER.md` 시스템 프롬프트(페르소나) 단에 에이전트 첫 턴 필수 동작으로 `constitution.local.md`와 `01_hot.md` 스캔을 강제화.

### 2026-06-04 — 세션 워크스페이스 망각 장애 해결 및 다중 경로 바인딩 규칙 추가
- **문제**: 세션 컴팩션 시 `Mjobsidian` 외부의 프로젝트 폴더(`MarineOS-XR Project`)를 스캔하지 않고 파일이 지워졌다고 단정하는 현상 발생.
- **해결**: `constitution.local.md`에 §X.4 다중 작업 경로 바인딩 규칙 추가, 물리적 실재 확인 의무화(§X.2). `01_hot.md`에 외부 경로 기록 명시.
- **보고서**: `06_에이전트_오류_및_재발방지_보고서.md` 신규 발행.
- **관련**: 7종 메타 파일(00_Meta_지도.md, 01_hot.md, 02_스크립트 정보.md, 03_시스템 인벤토리.md, 04_주요 시스템 가이드.md, 05_시스템 상태.md, 06_에이전트_오류_및_재발방지_보고서.md) 전면 업데이트

### 2026-06-05 — Llama-Server 멀티 슬롯 컨텍스트 불일치 및 망각 장애 해결
- **문제**: 대화가 길어질 때 로컬 Gemma 4 모델이 자신이 생성한 프로그램이나 대화 내용을 망각하는 문제 발생.
- **해결**: `com.bluesea.llama_server2.plist`의 병렬 슬롯 옵션을 `-np 2`에서 `-np 1`로 수정하여 단일 슬롯에 65,536 컨텍스트를 온전히 할당. `config.yaml`의 `context_length: 65536`과 일치시켜 서버단 강제 Truncation 유실을 방지하고 SWA 캐시 보존율을 최적화.

*최종 업데이트: 2026-07-02 14:51*

---

### 2026-06-05 — v9.3 Memory Engine 개선: 하이브리드 트리거 + 용량 기반 증류 + 안전장치
- **bio_memory_engine.py**: L2→L3 이관 조건에 용량(>1MB) 하이브리드 트리거 신설. 기존 개수 기반(>200개)과 OR 조건으로 병행 작동. `L2_MAX_BYTES = 1MB` 상수 도입.
- **dreamer_layer.py**: `offline_consolidation()`에 용량 기반 증류 로직 추가. 500자 이상 & 중요도≥4.0 혹은 800자 초과 원문을 L3 패턴으로 강제 이관. 에빙하우스 망각 공식과 용량 기반 압축 전이 병행.
- **dreaming_v2.py**: `deep_distill()` L3 직접 누적 — `semantic_memory.json.patterns` 배열 정형화 저장. Early Exit Guard Bypass: `raw_events` 비어도 L2 > 1MB면 다이어트 강제 구동. Atomic Write: `os.replace()` 원자적 파일 교체로 Race Condition 방지.
- **실측 현황 반영**: L2 episodic_memory.json: 83 entries, 2.4MB raw (1.7MB payload), 모든 항목 >500자. L3 semantic_memory.json: patterns=0, procedural=1 (570B) — L3 공백 상태 확인 완료.

---

## 2026-06-03 신규 추가 스크립트

|| 스크립트 | 용도 |
||----------|------|
|| `wiki_auto_stamper.py` | wiki 이하 .md 파일 타임스탬프 자동 갱신 + YAML frontmatter 태그 8개 자동 조율 및 본문 링크 텍스트화 자동 정돈. **형식 규칙 적용 전담** (내용 처리 아님). 2026-06-23부터 fswatch-indexer.sh와 연동되어 상시 자동 실행 — digest 외 Hermes·Claude Code 생성 파일에도 적용됨. |
|| `digest` (ingest_engine.py 기반) | **내용 처리 전담**. Clippings/Inbox 원본 소스(기사·PDF·웹클리핑) → wiki 페이지 요약·분류·생성. wiki_auto_stamper와 역할 분리: digest=요리(재료→음식), stamper=식기정리(그릇 규칙 적용). digest 후 stamper가 자동 후처리. |

*최종 업데이트: 2026-07-02 14:51*

---

## 📋 AI Agent Memory 개념 분석 — 문제 인식 저장 (2026-06-05)

**참조**: 01_hot.md 동일 섹션 상세. 본 문서는 참고 위치 기록용.

블로그 "AI Agent Memory" 4계층(컨텍스트 윈도우/단기/장기/외부지식) + 4대 연산(Write/Read/Update/Forget) vs 현재 Bio-Memory Engine 시스템 대비 분석 완료.

**핵심 갭 (향후 로드맵 확정 시 반영):**
- Forget 정책 부재 — 메모리 블로트 리스크
- Update 자동 충돌 감지 부재
- Writer 저장 전 self-question 부재
- semantic_index.db ↔ memory 검색 미연동

## 🛠️ 2026-06-06 스크립트 업데이트 정보
- **harness_agent.py**: 
  - NVIDIA API 타겟 모델명을 `meta/llama3-70b-instruct`로 교체.
  - `handle_message` 내부에 `progress_msg.edit_text`를 오버라이드(Monkey Patch)하여 백그라운드 비동기 애니메이션(🤔 생각 중...) 태스크 추가.
- **web_agent_module.py**:
  - `search_web` 함수 내에 "날씨" 키워드 감지 시, `urllib`와 `BeautifulSoup`을 이용해 네이버 날씨를 스크래핑하는 인터셉터 루틴 삽입 (기존 DDGS 한계 극복).

## 🛠️ 2026-06-06 신규 스크립트: switch_model.sh (모델 스위치 도구)
- **파일 경로**: `/Users/bluesea/Applications/venu/scripts/switch_model.sh`
- **심볼릭 링크**: `/usr/local/bin/switch-model` (어디서든 실행 가능)
- **용도**: Hermes Agent의 메인 모델을 GPT-OSS-120B(NVIDIA)와 DeepSeek Chat 사이에서 전환
- **사용법**:
  ```bash
  switch-model got       # GPT-OSS-120B (NVIDIA)로 전환
  switch-model deepseek  # DeepSeek Chat으로 전환
  ```
- **적용 대상**: 두 Hermes Home 모두 전환
  - `~/.hermes/config.yaml` (Gateway + WebUI)
  - `~/Applications/venu/.hermes2/config.yaml` (Telegram Bot)
- **특징**:
  - 전환 시 이전 설정을 삭제하지 않고 `# [SWITCHED to ...]` 주석 처리하여 보존
  - 언제든지 반대 명령어로 원복 가능
  - 전환 후 `hermes gateway restart` 필요
   359|- **배경**: DeepSeek API 대신 NVIDIA의 무료 GPT-OSS-120B를 메인 모델로 사용. Qwen2.5-14B-Instruct(로컬)는 별도 API이므로 영향 없음.

## 🛠️ 2026-06-07 harness_agent.py — NVIDIA NIM 70B → GPT OSS 120B 교체
- **파일**: `/Users/bluesea/Applications/Mjauto/Scripts/harness_agent.py`
- **변경 이유**: NVIDIA NIM `meta/llama3-70b-instruct` 서비스 종료 (404 오류)
- **수정 내용**:
  - `MODE_NVIDIA = "NVIDIA 70B"` → `"GPT OSS 120B"`
  - `MODE_LABELS[MODE_NVIDIA]` → `"🟣 GPT OSS 120B (NVIDIA)"`
  - `_call_nvidia()`: `model="meta/llama3-70b-instruct"` → `model="openai/gpt-oss-120b"`
  - `_ENGINE_MAP`, `_FALLBACK_ORDER` 라벨 일괄 업데이트
  - `get_current_mode()`: 이전 `"NVIDIA 70B"` 값 자동 마이그레이션 로직 추가
- **API 키**: `CAPT_NVIDIA_API_KEY` 그대로 사용 (동일 NVIDIA 엔드포인트)
- **`~/.hermes/llm_mode.txt`**: `"NVIDIA 70B"` → `"GPT OSS 120B"` 업데이트

## 🛠️ 2026-06-07 ~/.hermes/config.yaml — 시스템 프롬프트 최적화 (로컬 LLM 응답 속도)
- **배경**: Qwen2.5-14B 로컬 첫 응답 ~120초 → prefill 10,919 토큰 처리 시간
- **측정된 원인**: Skills index 3,348 tokens + browser toolset ~1,000 tokens = 4,348 tokens 불필요
- **변경 내용**:
  - `agent.disabled_toolsets: [browser, vision]` — browser 13개 + vision 1개 도구 비활성화
  - `skills.disabled: [115개 전체]` — Skills Hub 인덱스 시스템 프롬프트 완전 제거 (0 tokens)
  - `.skills_prompt_snapshot.json` 캐시 삭제, Gateway 재시작
- **효과**: 예상 첫 응답 120초 → ~77초 (나머지는 hermes-cli 48개 tool schema로 하드웨어 한계)
- **주의**: 박사님은 Hermes Hub 스킬 미사용(직접 제작 운영)이므로 기능 손실 없음

## 🛠️ 2026-06-07 패키지 패치 (스크립트 아님 — 설치 패키지 직접 수정)

> ⚠️ 이번 세션에서 별도 스크립트를 생성하지 않고, 설치된 venv 패키지 파일을 직접 수정함.

- **`gateway/platforms/api_server.py`** (`venu/venv/.../`):
  - `@custom:<slug>:model` WebUI 접두사를 실제 모델명으로 스트립하는 로직 추가
  - `runtime_kwargs.pop("context_length", None)` — AIAgent에 전달 전 custom key 제거
- **`gateway/run.py`** (`venu/venv/.../`):
  - `_resolve_custom_provider_by_model()`: custom_provider config에서 `context_length` 읽어 반환
- **`hermes-webui/api/config.py`**:
  - `_whitelist_keywords`에 `"qwen"` 추가 → Qwen 모델 드롭다운 표시
- **`~/.hermes/config.yaml`**:
  - Qwen2.5-14B custom_provider에 `discover_models: false` + `context_length: 65536` 추가

⚠️ **주의**: `pip upgrade`로 venv 패키지 업데이트 시 위 세 파일의 수정 내용이 덮어써짐. 업그레이드 전 반드시 패치 내용 재확인 또는 백업 필요.

### 🛠️ 최근 장애 복구 요약
- **장애 현상**: 서브 봇 날씨 API 키 누락, WebUI 단기 기억 오염
- **근본 원인**: 환경 변수 누락, `harness_memory.json` 공유
- **조치**: `.env`에 `ANTIGRAVITY_NVIDIA_API_KEY` 추가, `harness_memory.json` 삭제, 게이트웨이 재시작
- **결과**: Hermes WebUI 정상 작동, 텔레그램 봇 응답 일치

---

## 루프 안전 시스템 빠른 진단 가이드

> 2026-06-09 추가. 아래 시스템들은 모두 `harness_agent.py` 에이전틱 루프에 통합되어 있음.
> 2026-06-09 2차 갱신 — context_assembler.py / agentic_loop.py 분리 이후 모듈 구조 반영.

### 전체 흐름 구조도 (2026-06-09 3차 분리 반영)

```
텔레그램 메시지
      │
      ▼
harness_agent.py handle_message()   ← 얇은 글루 (319줄)
      │
      ├─ [modules/command_router.py] route_command() → True/False
      │       ├─ /confirm, /cancel  → file_ops_agent 실행
      │       ├─ 🔄 모드전환        → InlineKeyboard 표시
      │       ├─ @검색/@상태/@정리/@ingest → 각 전용 모듈 호출
      │       └─ Logic Engine       → 파일 키워드 없으면 우선 처리
      │         (True이면 return, False이면 LLM으로)
      │
      └─ [modules/response_handler.py] handle_llm_response()
              │
              ├─ 진행 애니메이션 task (asyncio)
              │
              ├─ [modules/context_assembler.py] assemble_context()
              │       ├─ wiki ctx 5분 캐시 주입
              │       ├─ style_profile (handlers/_base._get_style_profile)
              │       ├─ history 최근 15턴 (history_manager.get_history_for_llm)
              │       ├─ L2/L3 메모리 hybrid_recall (memory_refinement.py)
              │       ├─ Resolver 작업 유형 컨텍스트 (hermes_context_builder.py)
              │       ├─ 시스템 팩트 주입 (claude_briefing.md → "내 시스템" 등 키워드 감지)
              │       └─ 날씨 스크래핑 주입 (BS4 네이버 날씨 → "날씨" 키워드 감지)
              │
              ├─ LLM 호출 → get_llm_response()
              │       └─ ① [Circuit Breaker] 엔진 차단 확인 → 차단이면 즉시 폴백
              │
              ├─ [modules/agentic_loop.py] run_agentic_loop()
              │       │
              │       ├─ ② [Mayor] mayor.register() → loop_id  (루프 시작 1회)
              │       ├─ 도구 태그 파싱 & 실행
              │       │       ├─ LIST/READ/WEB_READ/SEARCH/RUN_CMD
              │       │       ├─ CREATE/DELETE/MOVE/COPY/RENAME (→ file_ops_agent.py)
              │       │       └─ ③ [ToolResult] 샌드박스 태그 래핑 (tool_result.py)
              │       ├─ ④ mayor.tick(loop_id, tokens) → False면 강제 종료
              │       └─ 도구 없이 종료 시:
              │               ├─ ⑤ [루프 자기검증] CoVe._filesystem_grounding() 자동 실행
              │               └─ mayor.finish(loop_id)
              │
              └─ [SAVE] 태그 처리 → permission_bridge → file_m.write_file()

별도 동작:
history_manager.get_history_for_llm()
      └─ ⑥ [Context Compaction] 30턴 초과 시 자동 압축
```

### 증상별 진단 순서

| 증상 | 먼저 확인 | 원인 | 조치 |
|---|---|---|---|
| 응답 엔진이 갑자기 바뀜 | 텔레그램 `⚠️ [자동 폴백]` 알림 | Circuit Breaker 발동 | 60초 대기 자동 복구. 즉시 필요 시 `/restart_bot` |
| 루프가 중간에 멈춤 | `/orchestrate mayor` 대시보드 | Mayor 예산/반복 초과 | 작업을 더 작게 나눠 재시도 |
| 답변에 ⚠️ 파일 경고 블록 | 경고된 파일명 실제 존재 확인 | LLM 환각 (파일명 날조) | 경고 내용대로 수동 확인 후 사용 |
| 오래된 대화 맥락이 사라짐 | `harness_memory.json` 첫 항목 확인 | 30턴 초과 → 자동 압축 | 정상. 최근 10턴은 그대로 보존됨 |
| LLM이 검색 결과를 지시로 따름 | 로그에 `[TOOL_OUTPUT_START` 존재 여부 | ToolResult 래핑 실패 | `modules/tool_result.py` 파일 존재 확인 |
| 컨텍스트(날씨/메모리/wiki) 누락 | `context_assembler.py` 각 try/except | assemble_context 내부 오류 | 각 블록 개별 확인 (날씨=BS4, 메모리=memory_refinement, wiki=wiki_manager) |
| 도구 태그 실행 안 됨 | `agentic_loop.py` import 오류 | mayor_agent/tool_result 미존재 | `Scripts/modules/` 내 해당 파일 존재 확인 |

### 상수 변경이 필요할 때

| 변경 목적 | 파일 | 상수명 | 기본값 |
|---|---|---|---|
| 루프 반복 한도 늘리기 | `modules/mayor_agent.py` | `DEFAULT_MAX_ITERS` | `3` |
| 토큰 예산 변경 (유료 전환 시) | `modules/mayor_agent.py` | `DEFAULT_TOKEN_BUDGET` | `12000` |
| 정체 감지 시간 변경 | `modules/mayor_agent.py` | `STALE_TIMEOUT` | `300` (5분) |
| CB 민감도 높이기 | `harness_agent.py` | `_CB_THRESHOLD` | `3` |
| CB 차단 시간 변경 | `harness_agent.py` | `_CB_TIMEOUT` | `60.0` |
| 대화 압축 기준 변경 | `modules/history_manager.py` | `COMPACT_THRESHOLD` | `30` |
| 압축 후 보존 턴 수 | `modules/history_manager.py` | `KEEP_RECENT` | `10` |

### 이 시스템들 없었을 때 vs 있을 때

| 상황 | 이전 | 현재 |
|---|---|---|
| NVIDIA API 일시 장애 | 타임아웃 60s × 3회 = 3분 후 에러 | CB 발동 → 즉시 폴백, 60초 후 자동 복구 |
| 웹 검색 결과에 악성 지시 | LLM이 지시로 오인 가능 | ToolResult 샌드박스로 데이터 영역 격리 |
| 100턴 이상 대화 | 전체 히스토리 → 토큰 낭비 + 느림 | 30턴 초과분 자동 압축 |
| LLM이 없는 파일 언급 | 환각 그대로 출력 | 루프 종료 시 grep 검증 → 경고 삽입 |
| 루프 예산 관리 | range(3)만 있고 토큰 감시 없음 | Mayor가 반복수 + 토큰 실시간 감시 |

---

## 2026-06-12 모듈 업데이트 (논문 기반 개선)

### `modules/bio_memory_engine.py` 변경

| 변경 항목 | 설명 |
|---|---|
| `ImportanceScorer.score()` | assistant role 점수 상한: `min(score, L1_TO_L2_THRESHOLD - 0.1)` — Model Collapse 방어 |
| `_strip_inline_embeddings()` | 신규 메서드: 기존 L2 인라인 임베딩 → turbovec 이관. 기동 시 1회 실행 |
| `_promote_to_l2()` | source 필드 추가, embedding 인라인 저장 제거, eviction `if`→`while` |
| `recall()` | ep["embedding"] 재저장 라인 비활성화 |

**에러 진단 포인트**: `[Bio-Memory] 인라인 임베딩 N개 제거 완료` 로그 기동 시 1회 출력되면 정상 마이그레이션 완료.

### `modules/memory_refinement.py` 신규 함수 (4개)

| 함수 | 역할 |
|---|---|
| `novelty_score(text, role)` | SAGE 신선도: 기존 L2 키워드 비교 → 0.0~1.0 점수 |
| `is_novel_enough(text, role, force_if_different_outcome)` | 저장 게이트 결정 (bool, score, reason) |
| `diversity_check()` | bigram 다양성 + human/llm 비율 모니터링 |
| `get_diversity_report()` | 텔레그램 포맷 다양성 보고서 |

**상수**: `NOVELTY_SIMILARITY_THRESHOLD=0.85`, `DIVERSITY_HUMAN_MIN_RATIO=0.30`

### `modules/context_assembler.py` 변경

| 변경 항목 | 설명 |
|---|---|
| `_sanitize_wiki_content(content)` | 신규 함수: ClawTrojan 패턴 라인 제거 + 경고 로깅 |
| `_INJECT_SUSPICIOUS_PATTERNS` | 7개 regex 패턴: [SYSTEM], [INST], 역할 재정의 등 |

**에러 진단 포인트**: `[CtxAssembler] ClawTrojan 패턴 제거: ...` 로그 출현 시 wiki 파일 오염 의심.

### `modules/skill_auditor.py` 신규 클래스

| 항목 | 설명 |
|---|---|
| `SkillLifecycle` 클래스 | lifecycle DB 관리: `record_call()`, `get_stale_skills()`, `get_lifecycle_report()` |
| `get_skill_lifecycle()` | 싱글톤 접근자 |
| DB 경로 | `~/.hermes/runtime/skill_lifecycle.json` |

**에러 진단 포인트**: DB 파일 권한 오류 시 `SkillLifecycle._save()` 예외 — `~/.hermes/runtime/` 디렉토리 존재 확인.

### `modules/agentic_loop.py` 변경

| 항목 | 설명 |
|---|---|
| `_trace(turn, tool, path, result, repaired)` | TraceGraph 궤적 기록 함수 |
| 로그 경로 | `~/.hermes/runtime/trace_log.jsonl` (롤링 200엔트리) |
| 기록 시점 | LIST, READ, RUN_CMD, CREATE 태그 실행 후 자동 기록 |

**에러 진단 포인트**: trace_log.jsonl가 커지면 자동 롤링됨 (200엔트리 초과분 자동 삭제).

---

## 2026-06-12 협업 규칙 기반 추가 (v9.4.1)

### `modules/history_manager.py`
- `HANDOFF_WARN_RATIO = 0.75` 상수 추가
- `get_context_pressure()` 메서드 — 포화도 반환, 75% warn / 100% critical

### `modules/skill_auditor.py`
- `SkillLifecycle.record_feedback(skill_name, success)` — 소급 성공률 보정
- **에러 진단**: `[SkillLifecycle] feedback: skill_name → success=False` 로그 = 정정 감지 정상 동작

### `handlers/_base.py`
- `_CORRECTION_SIGNALS` 리스트 (13개 정정 표현)
- `_detect_correction(text)` — 정정 패턴 bool 반환
- `emit_skill_feedback(skill_name, success)` — lifecycle 피드백 전달

### `modules/context_assembler.py`
- `_input_clarity_score(text)` — 모호성 점수 0.0~1.0
- Input Clarity Gate: 점수 0.6+ 시 system 메시지 자동 주입
- **에러 진단**: `[CtxAssembler] clarity gate: score=...` 로그 = 모호 입력 감지됨

---

## 2026-06-18 MJstock 신규 함수 추가

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### `screener/run_scan.py`

| 항목 | 설명 |
|---|---|
| `EXP_USE_FRESH_TREND` | 토글 (bool) — 200일선 기울기 필터 활성화 여부 (GROK V2.3) |
| `EXP_USE_IS_LEADER` | 토글 (bool) — RSI>55 + 장기상승 강세 근사 필터 |
| `EXP_USE_IS_TIGHT` | 토글 (bool) — ATR 이중 압축 필터 (GROK V3.0) |
| `compute_exp_filters(df_daily)` | US/KR 스캔 결과에 공통 적용되는 실험 필터 계산 함수. `exp_fresh_trend`, `exp_is_leader`, `exp_is_tight` 컬럼 반환 |

**에러 진단 포인트**: `df_daily`에 일봉 데이터가 없는 종목 통과 시 KeyError — `atr14` / `close` 컬럼 존재 여부 사전 확인 필요.

### `screener/signal_tracker.py`

| 항목 | 설명 |
|---|---|
| `ACCOUNT_EQUITY` | 계좌 총자산 기준값 (기본 100,000) |
| `RISK_PCT` | 1회 거래 리스크 비율 (기본 0.01 = 1%) |
| `_calc_position_size(price, atr14)` | ATR 기반 권장 매수 수량 계산 (GROK V3.0 Adaptive 공식). `suggested_shares` 반환 |
| `record_scan()` 확장 파라미터 | `atr_map`, `exp_map` 추가 — `atr14`, `suggested_shares`, `exp_*` 3종 컬럼 기록 |
| `BUY_SIGNALS` 상수 (2026-06-28) | BUY_URYANGJU / BUY_SELYEOK / NONGSSA / RAPID_ACCUM / GONGJOONG / NAKPOK / REVERSAL_EARLY / MACD_BB_BUY — ★ 세팅 기준 8종 |
| `SELL_SIGNALS` 상수 (2026-06-28) | SHORT_PEAK / MACD_BB_SELL — circle_date 자동 기록 트리거 2종 |
| `star_signal_date` 로직 수정 (2026-06-28) | BUY_SIGNALS 8종 해당 시만 세팅. SCAN_PASS는 ★ 아님. |
| `circle_date` 자동 기록 (2026-06-28) | SELL_SIGNALS 발생 시 active 행(circle_date="")의 `circle_date`, `circle_price` 자동 업데이트. 최초 1회만. |
| `exited` 탐지 개선 (2026-06-28) | `exit_price` = price_map 대신 마지막 기록된 `close_price` 사용. circle_date 없이 exited → `verdict="⚠️신호없음"` |
| `return_30d` 필드 추가 (2026-06-28) | `SIGNAL_LOG_FIELDS` 스키마에 `return_30d` 추가 (기존 누락) |
| `update_returns()` 기준 변경 (2026-06-28) | verdict 기준: r10 → r30 (30일 수익률 기준 ✅적중/❌불발 확정) |

**에러 진단 포인트**:
- `atr14=0` 입력 시 ZeroDivisionError 가능 — `_calc_position_size()` 내 0 가드 확인
- `star_signal_date` 안 세팅됨 → 시그널명이 `BUY_SIGNALS` 8종 중 하나인지 확인
- `circle_date` 안 채워짐 → SELL_SIGNALS 발생 여부, 이미 circle_date가 채워진 행인지 확인
- `return_30d` KeyError → `SIGNAL_LOG_FIELDS`에 해당 컬럼 있는지 확인
- JSON 파싱 오류(`json.loads` 실패) → stdout에 [Tracker] 같은 비-JSON 줄 섞임 → `{`로 시작하는 줄만 추출 패턴 적용

---

## 2026-06-18 MJstock data_loader.py 신규 함수 추가

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### `screener/data_loader.py`

| 항목 | 설명 |
|---|---|
| `_UNIVERSE_CSV` | dict — 유니버스 키 → CSV 파일명 매핑 (`sp500`→`sp500.csv` 등 6개) |
| `get_universe_csv_mtime(universe)` | CSV 파일 마지막 수정 시각 반환 (`2026.06.18 08:00PM` 형식). 파일 없으면 None |
| `update_universe_csv(universe)` | 유니버스별 최신 데이터 갱신. `(bool, str)` 반환 |
| `_update_sp500()` | Wikipedia S&P500 크롤링 → `data/sp500.csv` 저장 |
| `_update_nasdaq()` | Wikipedia Nasdaq-100 크롤링 → `data/nasdaq1000.csv` 저장 |
| `_update_dow30()` | Wikipedia DJIA 크롤링 → `data/dow30.csv` 저장 |
| `_update_russell_from_downloads()` | `~/Downloads`에서 russell*.csv/xlsx 탐색 → `data/russell1000.csv` 복사 |
| `_update_kr(universe)` | KRX OpenAPI POST → 시총 정렬 후 `data/kospi_full.csv` 또는 `data/kosdaq_full.csv` 저장 |

**변경**: `get_sp500_tickers()` — 실행마다 위키 크롤링 → `data/sp500.csv` 우선 읽기로 변경 (스캔 속도 개선)

**에러 진단 포인트**:
- `_update_kr()`: KRX `OutBlock_1` 빈 배열 → 장 마감 직후 당일 날짜 조회 실패. 재시도 또는 전일 날짜 파라미터 수정
- `_update_russell_from_downloads()`: 티커 컬럼 자동 감지 실패 시 첫 번째 컬럼 사용 — 파일 구조 확인 필요

---

## 2026-06-18 MJstock 매수신호 AND 조건 + 십자선 추가

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### `screener/run_scan.py`

| 항목 | 설명 |
|---|---|
| `compute_buy_signal_snapshot(df_daily)` | 최근 5거래일 이내 farming 매수신호(BUY_URYANGJU→BUY_SELYEOK→NONGSSA 우선순위) 발생 여부 체크. `has_buy_signal`(bool), `buy_signal_type`(str), `buy_signal_days_ago`(int) 딕셔너리 반환. 오류 시 has_buy_signal=False로 안전 처리 |

**에러 진단 포인트**: `has_buy_signal` 항상 False면 `compute_farming_signals()` 내부 컬럼 계산 실패 가능성 — try/except로 보호되므로 스캔은 중단 안 됨. 재스캔 후 CSV 컬럼 존재 확인.

### `screener/chart.py`

| 항목 | 설명 |
|---|---|
| 십자선(Crosshair) | 4개 차트 함수(SEPA/초우량주/Farming/5분봉) `update_layout()`에 `hovermode="x unified"` + `showspikes=True` 추가. 마우스 이동 시 날짜·가격 동시 표시. 다크 테마: `#555555`, 밝은 테마: `#888888` |

---

## 2026-06-18 MJstock 퀀트 스냅샷 함수 추가

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### `screener/run_scan.py`

| 항목 | 설명 |
|---|---|
| `compute_quant_snapshot(df_daily)` | 스캔 통과 종목에 대해 17개 퀀트 컬럼 계산. 진입가·거래량·ATR·RSI·MACD·BB·EMA이격도·52주고가·거래량비율·미래수익률 자리 포함. 계산 실패 시 None 반환(스캔 중단 없음) |

**에러 진단 포인트**: `add_bbands_width()` 호출 시 데이터 30봉 미만이면 NaN — 정상 동작(짧은 상장 종목)

---

## 2026-06-18 MJstock batch_fill_returns.py 신규

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### `screener/batch_fill_returns.py`

| 항목 | 설명 |
|---|---|
| `run(dry=False)` | 전체 스캔 CSV 순회 → ret_5d/10d/20d 자동 채움. `--dry` 플래그로 미리보기 가능 |
| `fill_one_file(csv_path, client, is_kr, dry)` | 단일 CSV 처리. entry_price 기준 수익률 계산 후 저장 |
| `health_check()` | 검색식별 적재 상태 dict 반환. app.py 퀀트 로그 탭에서 호출 |
| `fetch_current_price(client, ticker, is_kr)` | KIS API로 최근 종가 1봉 조회 |

**실행**: `python3 screener/batch_fill_returns.py`  
**cron**: 매주 월~금 17:30 자동 실행 등록됨  
**로그**: `results/fill_returns.log`

---

## 2026-06-18 MJstock 신규 모듈 2종 — mobile_dashboard.py / health_check.py

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### `mobile_dashboard.py` (신규)

| 항목 | 설명 |
|---|---|
| 목적 | 모바일 웹 대시보드 — 같은 WiFi 폰 브라우저에서 스캔 결과 확인 |
| 기반 | Python 내장 `http.server` (추가 패키지 불필요) |
| 포트 | 8765 |
| 주요 기능 | 검색식별 카드 + 상위 10종목 + 점수바 + 5분 자동 새로고침, 다크 테마 모바일 최적화 CSS |
| 접속 | `http://[Mac IP]:8765` |

**실행**: `python3 mobile_dashboard.py`  
**에러 진단**: 접속 불가 시 Mac과 폰이 같은 WiFi인지, 포트 8765 방화벽 차단 여부 확인

### `health_check.py` (신규)

| 항목 | 설명 |
|---|---|
| 목적 | 일일 스캔 시스템 헬스 체크 → 텔레그램 리포트 발송 |
| 체크 항목 | 오늘 스캔 실행 여부(검색식별) / 스캔 로그 에러 / 수익률 채움 실행 여부 / 퀀트 데이터 누적 현황 |
| 발송 | 텔레그램 일일 헬스 리포트 |
| 자동 실행 | 평일 18:00 crontab 등록 완료 |

**실행**: `python3 health_check.py`  
**cron**: `0 18 * * 1-5 python3 /Users/bluesea/Applications/Mjstock/health_check.py`  
**에러 진단**: 미발송 시 `TELEGRAM_TOKEN`/`CHAT_ID` 환경변수 확인

### `app.py` 설정 탭 추가 (tabs[5])

| 항목 | 설명 |
|---|---|
| 위치 | `app.py` — `tabs[5]` "설정" 탭 신규 |
| 텔레그램 설정 | 봇 토큰 입력/저장, 채팅 ID 입력/저장 |
| 자동 조회 | "내 채팅 ID 자동 조회" 버튼 (getUpdates API 호출) |
| 테스트 | "테스트 메시지 발송" 버튼 |
| 연동 상태 | 텔레그램 봇 / 채팅 ID / KIS API 3개 metric 요약 표시 |
| 설계 의도 | 가족 배포용 — USB로 받은 가족이 자기 텔레그램 계정으로 알림 받도록 |

**설정 저장 경로**: `~/.mjstock/telegram_config.json`

---

## 2026-06-18 MJstock + Hermes 연동 — /mjstock 온디맨드 단일 종목 분석

### `Mjstock/screener/scan_single.py` (신규)

| 항목 | 설명 |
|---|---|
| **역할** | 단일 티커 × 검색식 → 점수 계산 + 차트 HTML 즉시 생성 |
| **입력** | `--ticker NVDA --screener selyeok` |
| **출력** | JSON stdout: `ok`, `score`, `pass_count`, `total_count`, `chart_path`, 조건별 bool |
| **차트 생성** | `save_farming_chart()` 호출 → `charts/SCREENER/TICKER_farming_YYYYMMDD_HHMMSS.html` |
| **핵심** | `run_scan.py`의 `US_SCREENERS`/`KR_SCREENERS` 레지스트리 재활용 — 검색식 추가 시 자동 반영 |
| **에러 진단** | `KIS_APP_KEY`/`KIS_APP_SECRET` 없으면 빈 DataFrame → 검색식 오류 / 검색식 키 오타 시 "알 수 없는 검색식" 반환 |

### `Mjauto/Scripts/handlers/_stock.py` 추가 함수

| 함수 | 설명 |
|---|---|
| `cmd_mjstock` | `/mjstock TICKER` → 8개(US) + 6개(KR) 검색식 인라인 버튼 팝업 |
| `callback_mjstock` | 버튼 탭 → `scan_single.py` subprocess → 결과 파싱 → 점수+조건표+차트URL 메시지 |

**흐름**: `/mjstock RDW` → 버튼 선택 → 30초 → 점수 + `http://[IP]:8765/chart/selyeok/RDW` → URL 탭 → 차트 즉시 열림  
**특징**: 07:30 전체 스캔 불필요 — 아무 시간에나 단일 종목 온디맨드 차트 생성 가능

---

## 2026-06-23 MJstock 스캔 시스템 개편 v2.0 — 신규 파일 2종 + send_scan_result 수정

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### `auto_scan_morning.py` (신규 — 구 auto_scan_nasdaq500.py 대체)

| 항목 | 설명 |
|---|---|
| **역할** | 아침 일봉 스캔 전용. US 5종 + KR 5종 (uryangju/judoju/danta/selyeok/chowuryang) |
| **실행 시간** | cron: `0 7 * * 1-5` (KST 07:00) |
| **출력** | 텔레그램 요약 메시지 + 종목별 HTML 차트 파일 첨부 (sendDocument) |
| **로그** | `logs/scan_morning.log` |
| **에러 진단** | `logs/scan_morning.log` 확인 / `.venv/bin/python` venv 경로 문제 시 crontab 경로 재확인 |

### `auto_scan_intraday.py` (신규)

| 항목 | 설명 |
|---|---|
| **역할** | 장중 스캔 전용. pochak(세력포착)/shooting(매수후바로슈팅)/chuddoli(단타추돌이) |
| **실행** | `python auto_scan_intraday.py --market us` 또는 `--market kr` |
| **실행 시간** | US: cron `30 22 * * 1-5` (KST 22:30) / KR: `10 9 * * 1-5` (KST 09:10) |
| **로그** | `logs/scan_intraday_us.log` / `logs/scan_intraday_kr.log` |
| **에러 진단** | 5분봉/30분봉 데이터 없으면 조건 판정 실패 — 장 시작 후 최소 30분 후 실행 필요 |

### `screener/send_scan_result.py` (수정)

| 변경 항목 | 내용 |
|---|---|
| `get_chart_paths()` | 신규 함수. farming/intraday 차트 HTML 경로 탐색 |
| `send_document()` | 신규 함수. `sendDocument` Telegram API로 HTML 파일 직접 첨부 |
| `run()` | 로컬 IP 링크 제거. 차트 경로 → `send_document()` 호출로 대체 |
| **에러 진단** | `sendDocument` 응답 200이지만 파일 미수신 시 → `caption` 인코딩 확인 / 파일 경로 `chart_path` 존재 여부 확인 |

---

## 2026-06-23 Hermes 메모리 개선 3종 + MJstock 티커 분析기

**프로젝트**: `Mjauto/Scripts/modules` + `Mjstock/screener`

### `modules/memory_schema.py` (신규)

| 항목 | 설명 |
|---|---|
| **역할** | Hermes 메모리 데이터 구조 정의. TAOUP Rule of Representation 적용 |
| **EpisodicEntry** | L2 항목 dataclass. `is_worth_storing` / `is_expired` / `retention_score` 속성 내장 |
| **SemanticMemory** | L3 저장소. 기존 `patterns[]` 배열 → `{category: []}` dict 구조. `recall(query, categories=[])` |
| **classify_category()** | 키워드 테이블(`CATEGORY_KEYWORDS`)로 자동 분류. maritime_law / stock / system / personal / research / general |
| **의존성** | `context_assembler_v2.py`가 임포트. `memory_consolidator_v2.py`는 독립(미사용) |
| **에러 진단** | `from modules.memory_schema import ...` ImportError → 설치 경로 `modules/` 확인 |

### `modules/memory_consolidator_v2.py` (신규)

| 항목 | 설명 |
|---|---|
| **역할** | L2→L3 증류 파이프라인. Dreaming 폐기 후 고아된 경로 복구. 독립 실행형(memory_schema 미사용) |
| **경로** | L2: `~/.hermes/memory/episodic_memory.json` / L3: `~/.hermes/memory/semantic_memory.json` |
| **실행** | `python -m modules.memory_consolidator_v2 [--dry-run] [--health]` |
| **cron** | `0 4 * * *` 매일 새벽 4시 자동 실행 → `logs/consolidation.log` |
| **7단계** | 로드 → 나이 필터 → 보유율 필터 → 승격 후보 → L3 저장 → L2 다이어트 → 상태 저장 |
| **에러 진단** | `logs/consolidation.log` / L2 없으면 `~/.hermes/memory/` 경로 확인 / STATE_PATH: `hermes/memory_engine/consolidator_state.json` |

### `modules/context_assembler_v2.py` (신규, 미전환)

| 항목 | 설명 |
|---|---|
| **역할** | assemble_context() 개선판. 동일 인터페이스. 7블록 → 키워드 감지 시 3~7블록 |
| **TRIGGERS** | weather/system/stock/wiki 키워드 테이블 → 선택적 블록 추가 |
| **history** | 기존 15턴 → 10턴으로 축소 |
| **측정** | `logger.info(f"[context_assembler_v2] {block_count}블록 | {elapsed*1000:.0f}ms")` |
| **전환 방법** | harness_agent.py 1줄: `from modules.context_assembler_v2 import assemble_context` (현재 미전환) |
| **의존성** | `memory_schema.py` 필요 (`from modules.memory_schema import SemanticMemory, classify_category`) |
| **에러 진단** | memory_schema 미설치 시 ImportError / L3 기본경로 주의 (`modules/semantic_memory.json` ← 잘못됨, 호출 시 l3_path 명시 필요) |

### `screener/ticker_analyze.py` (신규)

| 항목 | 설명 |
|---|---|
| **역할** | `/mjstock {티커}` 전체 검색기 동시 분析. 현재 scan_single.py와 공존 |
| **호출** | `cmd_mjstock_analyze(update, context)` — 텔레그램 CommandHandler 등록 필요 (현재 미등록) |
| **검색기** | US 9종 + KR 9종 (nongsa_danta 포함). SCREENER_META dict에 진입/손절/보유 메타 내장 |
| **데이터** | KIS API 1회 다운로드 → 전체 검색기 재사용 (Pike Rule 1) |
| **출력** | 순위표(점수 바) + 1등 검색기 시그널 상세 + 2등 근접 시 경고 |
| **장중** | `include_intraday=False` (기본) = 일봉만. `/mjstock NVDA 5min` = 5분봉 포함 |
| **등록 방법** | `_stock.py`에서 `CommandHandler("mjstock", cmd_mjstock_analyze)` 교체 (현재 미등록) |
| **에러 진단** | `KIS_APP_KEY` 없으면 테스트 모드(더미 데이터) 실행 / `check_fn` 없음 → `US_SCREENERS` 키 확인 |

---

## 2026-06-23 MJstock 퀀트 DB 확장 + 자동 스캔 개편 + 차트 외부 접속

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### `screener/run_scan.py` — 신규/변경 함수

| 함수/항목 | 설명 |
|---|---|
| `compute_quant_snapshot()` 확장 | `ret_30d`, `ret_60d`, `ret_90d` 컬럼 추가. 기존 `ret_5d/10d/20d`에 연속 추가 |
| `compute_intraday_signal_snapshot(df_5min, is_kr=False)` | **신규 함수** — 5분봉 데이터로 7개 장중 시그널 컬럼 계산 |
| `--scan-type` CLI 인자 | `morning` / `intraday` 선택 (default: morning). argparse 등록 |
| `run_one(scan_type="morning")` | `scan_type` 파라미터 추가. matched 딕셔너리에 `scan_type` 자동 기록 |

**`compute_intraday_signal_snapshot()` 상세 — 7컬럼**

| 컬럼 | US 파라미터 | KR 파라미터 | 설명 |
|---|---|---|---|
| `intra_vwma78_above` | n=78 | n=66 | 종가 > VWMA78(KR:66) |
| `intra_vwma390_above` | n=390 | n=330 | 종가 > VWMA390(KR:330) |
| `intra_pink_above` | — | — | 핑크선 위 여부 |
| `intra_fifty_bok` | $5M | 5B KRW | 50억봉 출현 여부 |
| `intra_fifty_bok_bars` | — | — | 50억봉 출현 횟수 |
| `intra_star_count` | BB(33, 3.5σ) | BB(33, 3.5σ) | 상단 돌파 횟수 (★ 시그널) |
| `intra_obv_bull` | — | — | OBV 강세 여부 |

**US/KR 자동 구분**: `n_yellow = 66 if is_kr else 78`, `n_sky = 330 if is_kr else 390`

**에러 진단**:
- 컬럼 누락 → `is_kr` 파라미터 전달 여부 확인
- `df_5min` 없거나 비어있을 때 → 빈 dict 반환(정상), 호출부 None 처리 확인
- BB 3.5σ 통과 없음 → 장 시작 직후 데이터 부족 (30분 이상 봉 쌓인 후 실행 권장)

### `screener/batch_fill_returns.py` — 수익률 기간 확장

| 변경 항목 | 내용 |
|---|---|
| 수익률 기간 | `[5, 10, 20]` → `[5, 10, 20, 30, 60, 90]` — 3개월 수익률까지 확장 |
| API 최적화 | 종목별 현재가 1회만 조회 (`cur_price = None` 초기화 → 첫 days 루프에서 채움) |

**에러 진단**: `cur_price = None` 패턴에서 첫 호출 실패 시 이후 days도 전부 건너뜀 — API 응답 확인

### `auto_scan_morning.py` — 2026-06-23 개편

| 변경 항목 | 내용 |
|---|---|
| KR 스캔 | 코드 작성 완료이나 전체 주석 처리. `kr_results = {}` placeholder 유지 |
| subprocess 인자 | `--scan-type morning` 추가 |
| 활성 스캔 | 미국 morning 스캔만 실행 |

### `auto_scan_intraday.py` — 2026-06-23 개편

| 변경 항목 | 내용 |
|---|---|
| KR 스캔 | `if args.market == "kr": return 0` 즉시 종료 |
| subprocess 인자 | `--scan-type intraday` 추가 |
| universe | `"nasdaq100"` 하드코딩 |
| 활성 스캔 | 미국 intraday 스캔만 실행 |

### `handlers/_stock.py` — 차트 외부 접속 수정

| 함수 | 변경 내용 |
|---|---|
| `_handle_mjstock_chart()` | 로컬 IP URL 링크 방식 → `bot.send_document()` HTML 파일 첨부 방식으로 전환 |

**에러 진단**: `bot.send_document()` 실패 시 HTML 파일 경로 존재 여부 + 파일 크기(50MB 제한) 확인

---

### `screener/screen_samdoli_kr.py` + `screen_samdoli_us.py` — 2026-06-23 신규

**삼돌이 검색기**: 바닥 반등 후보 선별 + 5분봉 장중 진입 신호

| 항목 | KR | US |
|---|---|---|
| 시총 기준 | 200억원↑ | $1B↑ |
| 5일 거래대금 | 30억↑ | $50M↑ |
| 거래량 변화 | +80%↑ | +150%↑ |
| OBV Signal | 26봉 (OBV < Signal, 반전 직전) | 동일 |
| 추가 필터 | 없음 | SMA200 추가 |
| 5분봉 진입 | BB(78,2.2) + ENV(78,1.5%) 상단 돌파 | 동일 |
| 스캔 시간 | 07:00 (일봉) / 09:10 (장중) | 07:00 (일봉) / 22:30 (장중) |

**삼돌이 3타입**: DD돌파(반등선, ~90% 승률) / C2돌파(VWMA50) / EMA45돌파

**에러 진단**:
- US 버전 함수명 주의: `prepare_daily_us()`, `prepare_5min_us()`, `prepare_30min_us()` — `_us` 접미사
- `samdoli_type` 컬럼: `DD돌파`, `C2돌파`, `EMA45돌파` / US는 `(BounceBreak)` 등 영문 병기

---

### `screener/screen_nongsa_danta_kr.py` + `screen_nongsa_danta_us.py` — 2026-06-23 신규

**농사단타 검색기**: 삼돌이 후보 대상 장중 3박자 단타 진입

| 항목 | KR | US |
|---|---|---|
| 전제 조건 | 삼돌이KR 일봉 통과 필수 | 삼돌이US 일봉 통과 필수 |
| 50억봉 기준 | 5분봉 거래대금 50억원↑ | 5분봉 거래대금 $5M↑ |
| 3번째 매수신호 | BB(78,2.2) 상단 돌파 3회 | 동일 |
| OBV 조건 | OBV > Signal(26) — 순매수 전환 완료 | 동일 |
| 30분봉 사용 | 사용 (needs_30min: True) | **미사용** (needs_30min: False) |
| Phase 상태 | 7단계 한국어 표시 | 혼용 (영문 일부 병기) |

**Phase 순서**: 사전후보 → 50억봉대기 → 매수신호대기(N/3) → [진입금지 체크] → **진입가능✅**

**에러 진단**:
- US 버전 `prepare_30min` 함수 없음 — `needs_30min: False`로 등록 필수 (run_scan.py 레지스트리)
- `진입가능✅` phase인데 매수 안 되는 경우 → 반등선/분홍선 조건 재확인

---

## 2026-06-23 MJstock 장중 스캔 버그 수정 + 퀀트 CSV 60컬럼 고도화

### `screener/run_scan.py` — 핵심 안전장치 추가

**빈 DataFrame 이중 가드 (US/KR 공통)**
```
fetch → 빈 가드 → prep_daily(try/except) → 빈 가드 → check_fn(try/except)
```
- US: `samdoli`/`nongsa_danta` check_fn 분기 추가
- KR: `samdoli_kr`/`nongsa_danta_kr` 분기 추가 + prep_daily_fn 호출 누락 수정
- US check_fn 블록 전체 try/except 래핑 (KR은 기존부터 있었음)

**`compute_quant_snapshot()` — 50컬럼 → 60컬럼 (9개 신규)**

| 신규 컬럼 | 설명 | 진단 활용 |
|---|---|---|
| `ema45_dist_pct` | EMA45 이격도 | 삼돌이 핵심선 밀착 여부 |
| `adx14` | ADX(14) 추세 강도 | >25 = 추세장 확인 |
| `mfi14` | Money Flow Index(14) | 80↑ 과매수 / 20↓ 과매도 |
| `gap_pct` | 갭 등락율 (전일종가→당일시가) | 갭업 크기 정량화 |
| `obv_slope_5d` | OBV 5일 기울기 | 양수 = 순매수 방향 |
| `lower_wick_pct` | 아래꼬리 비율 | 지지선 테스트 강도 |
| `upper_wick_pct` | 위꼬리 비율 | 매물 압력 강도 |
| `stoch_rsi_k` | Stochastic RSI K선 | 20↓ 과매도 진입 타점 |
| `stoch_rsi_d` | Stochastic RSI D선 | K 교차 확인 |
| `dd_dist_pct` | DD 반등선 이격도 | samdoli 검색기 전용 — DD에 얼마나 붙어있나 |

**에러 진단**:
- `stoch_rsi_k/d` 계산 실패 → RSI_14 계산 가능 여부 확인 (데이터 17봉 미만이면 None)
- `dd_dist_pct` NaN → samdoli 외 검색기는 DD 컬럼 없음 (정상)
- `obv_slope_5d` NaN → OBV 5봉 미만 데이터 (정상)

### 2026-07-02 — quant.db 중앙 DB + 시장강도 필터 + ticker NaN 버그 수정

**프로젝트 루트**: `/Users/bluesea/Applications/Mjstock`

**신규 파일**

| 파일 | 역할 | 위치 |
|---|---|---|
| `quant_db.py` | SQLite 기반 중앙 퀀트 데이터베이스. `quant.db` 관리. `scan_results`(62컬럼) + `market_context`(SPY/KOSPI 일별 시장 상태) 테이블 2개. UNIQUE 제약 `(scan_datetime, screener, ticker)` 중복 방지. | `Mjstock/` 루트 |
| `migrate_to_db.py` | 기존 CSV 505개 → quant.db 일괄 마이그레이션 (4,146행). SPY/KOSPI 시장강도 날짜 기준 소급 적용. 1회성 실행 완료. | `Mjstock/` 루트 |

**QuantDB 클래스 주요 메서드**

| 메서드 | 설명 |
|---|---|
| `insert_scan_rows(rows)` | 스캔 결과 행 삽입 (UNIQUE 제약 위반 시 무시) |
| `update_returns(screener, ticker, scan_date, ret_map)` | 특정 행의 수익률 컬럼 업데이트 |
| `upsert_market_context(date, spy_data, kospi_data)` | 날짜별 시장 상태 upsert |
| `query(sql, params)` | 원시 SQL 조회 |
| `stats()` | 총 행 수 + 검색기별 집계 반환 |

**수정 파일**

| 파일 | 변경 내용 |
|---|---|
| `screener/run_scan.py` | **BUG-008/BUG-009 수정**: samdoli `check_fn` 호출에 `ticker=ticker` 파라미터 추가 (ticker NaN 버그 제거). nongsa_danta 동일 수정. **시장강도 필터 추가**: `_fetch_market_strength()` (SPY) + `_fetch_kospi_strength()` (^KS11) 함수 신규. US/KR 스캔 CSV에 각각 5개 시장강도 컬럼 자동 추가. `MARKET_FILTER_BEAR_STRICT` 토글 추가. **quant.db 연동**: 스캔 저장 직후 `QuantDB().insert_scan_rows()` 자동 호출. |
| `screener/batch_fill_returns.py` | 수익률 채움 완료 시 `QuantDB().update_returns()` 동시 호출 추가 → CSV + DB 수익률 동기화. |

**에러 진단 포인트**:
- samdoli CSV ticker 컬럼 NaN → `run_scan.py`의 samdoli `check_fn` 호출에 `ticker=ticker` 전달 여부 확인
- quant.db 미업데이트 → `QuantDB().stats()` 조회로 행 수 확인
- 시장강도 컬럼 누락 → `_fetch_market_strength()` yfinance SPY 조회 오류 (네트워크/API 제한 의심)
- DB 확인 명령: `python3 -c "import sys; sys.path.insert(0,'/Users/bluesea/Applications/Mjstock'); from quant_db import QuantDB; print(QuantDB().stats())"`

---

### `auto_scan_intraday.py` — 중복 실행 방지

fcntl 파일 락 추가: `auto_scan_intraday_us.lock` / `auto_scan_intraday_kr.lock`
- 동일 market 스캔이 이미 실행 중이면 즉시 종료
- 텔레그램 중복 메시지(4개) 문제 재발 방지

### `screen_samdoli_us.py` / `screen_nongsa_danta_us.py` / `screen_nongsa_danta_kr.py`

`prepare_5min()` 내 pandas index 충돌 수정:
```python
# 수정 전 (오류)
df["date"] = df.index.normalize()  # index.name="date"와 충돌

# 수정 후
df.index.name = None  # 먼저 클리어
df["date"] = df.index.normalize()
```
