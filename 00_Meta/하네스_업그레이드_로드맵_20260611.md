# 🚀 하네스 업그레이드 로드맵 (2026-06-11 진단)

> 작성: Claude Code (Fable 5) — 메타 7종 + verify_harness_report 전수 분석 기반
> 목적: 현재 갭 진단 → 우선순위별 업그레이드 계획. `/improve` 철학 적용 (고성능 모델이 감사·계획, 실행은 저비용 모델 가능하도록 자급자족형 계획 작성)

---

## 1. 시스템 현재 좌표

- **구조 점수**: 95/100 (S등급, 2026-06-09) — 구조는 건강
- **5-Layer OS**: L1~L5 완성 (Hooks/Skills/Git/Agents/MCP)
- **Self-Harness 3단계**: WeaknessMiner → ProposalValidator 완성
- **결론**: "골격은 완성, 이제 순환계(데이터 흐름)와 면역계(장기 버그)가 과제"

## 2. 진단된 갭 (영향도 ÷ 노력 = 레버리지 순)

### 🔴 P1 — 즉시 (이번 주)

#### ① L3 시맨틱 메모리 증류 정지 상태
- **증거**: L2 episodic 2,438KB vs L3 semantic 4KB — 600:1 비율
- **의미**: 경험은 쌓이는데 장기 지식·패턴으로 승화되지 않음. "시스템 성장 원칙"의 L3 성장 경로가 사실상 막혀 있음
- **진단 확정 (2026-06-11)**: `consolidator_state.json` = `{"last_processed_index": 29}` ← L2는 83항목. **54항목(65%)이 미증류 상태로 정체**. L3 내용물: patterns 7개, procedural 1개뿐
- **원인**: Dreaming 버튼 수동 의존 — 마지막 증류 이후 L2만 계속 누적
- **실행안**:
  1. 단기: 텔레그램 Dreaming 버튼 1회 수동 실행 (54항목 증류)
  2. 자동화: L2 미처리 항목 ≥ 20개 시 자동 Dreaming 트리거 — **Lock Stack 수정 없이** 외부 트리거 스크립트(launchd 주간) 신규 작성
- **검증**: last_processed_index가 L2 항목 수와 동기화, L3 patterns > 15개

#### ② 문서 표류 (System Drift — 결함 #6 재발)
- **증거**: `claude_briefing.md`가 "Forget 정책 부재/충돌 감지 부재"를 미해결 갭으로 표시 ← 실제로는 2026-06-09 Phase 2(`memory_refinement.py`)로 해결 완료
- **실행안**: `/claude_brief` 생성 로직이 01_hot.md 완료 섹션을 반영하는지 점검. 수동 보정 1회 실시 (✅ 2026-06-11 보정 완료)
- **재발 방지**: `/claude_brief` 실행 시 "미해결 갭" 섹션을 01_hot의 ✅ 완료 항목과 교차 대조하는 단계 추가

#### ③ 장기 미해결 버그 2종 종결
- **결함 #1 Zombie Poller**: PTB run_polling 블로킹 — launchd ThrottleInterval로 완화됐지만 근본 해결 미정. → 발생 빈도 로그 확인 후 종결 또는 수정 결정
- **결함 #4 Telegram Markdown**: 사용자 콘텐츠를 Markdown 파싱해 크래시 위험
  - **진단 확정 (2026-06-11)**: handlers 14개 파일에서 escape 없는 `parse_mode` **137곳** — 사용자/LLM 출력에 `_`, `*`, `[` 포함 시 BadRequest 크래시 가능
  - **실행안**: 공통 `safe_reply()` 헬퍼를 `_base.py`에 추가 → 전송 실패 시 parse_mode=None 재시도 (137곳 개별 수정 대신 1곳 폴백). 점진적으로 고빈도 핸들러부터 적용
- **검증**: `_`, `*` 포함 메시지 전송 테스트 통과

### 🟡 P2 — 단기 (이번 달)

#### ④ semantic_index.db ↔ 메모리 검색 연동
- **현황**: RAG 벡터 DB(semantic_index.db)와 L2/L3 메모리 검색(hybrid_recall)이 별도 운영
- **실행안**: `memory_refinement.py`의 hybrid_recall에 semantic_index FTS 결과 병합 (3소스: L2 + FTS5 + 벡터)
- **주의**: memory_refinement.py는 Lock Stack 아님 — 수정 가능

#### ⑤ 대형 모듈 2종 분할
- `ingest_engine.py` 642줄 → ingest_core + pdf_pipeline + inbox_deferral
- `knowledge_indexer.py` 631줄 → indexer_core + fts_layer + semantic_layer
- harness_agent.py 3차 분할(887→319줄)과 동일 패턴 적용
- **검증**: `/verify_harness` 경고 2건 소멸, 기존 명령어(`/ingest`, `/search`) 회귀 테스트

#### ⑥ GitHub Private Remote 연결 (백업)
- Scripts/.git(201파일)과 wiki git(306문서)이 로컬 전용 — 디스크 장애 시 전부 소실
- **실행안**: GitHub private repo 2개 생성 → remote add → 주간 자동 push (launchd)
- **차단 요소**: GitHub 계정/URL 필요 — **MJ님 제공 필요**

### 🟢 P3 — 중기 (분기)

#### ⑦ 외부 데이터 멀티소스 폴백
- 오늘(2026-06-11) yfinance rate limit 장애 사례 — 단일 데이터 소스 의존 취약
- **실행안**: `stock_fetcher.py`에 폴백 체인 추상화 (yf.download → Stooq(pandas-datareader, 무키) → Alpha Vantage 무료키). LLM 폴백 체인(DeepSeek→NVIDIA→로컬)과 동일 철학
- 일별 캐시는 2026-06-11 구현 완료 ✅

#### ⑧ /harness_report 월간 대시보드
- verify_harness 이력(20회) + WeaknessMiner DB + ProposalValidator DB 통합 월간 추이 리포트
- 텔레그램 `/harness_report` 명령어 + 매월 1일 자동 발송

#### ⑨ 검증 스킬 자연어 트리거 등록
- harness_verifier, curator, system_monitor 모듈이 스킬 미등록 (01_hot 차기 과제)
- "구조 진단해줘" → verify_harness / "코드 정리해줘" → curator 트리거를 `hermes_harness_skill_모음.md` + CLAUDE.md에 등록

## 3. 나아갈 방향 (전략)

1. **축적에서 증류로**: 데이터 수집 체계(L1/L2, wiki, weakness DB)는 완성. 다음 단계는 축적분을 지식으로 바꾸는 자동 증류 루프(Dreaming 자동화 = ①, 월간 리포트 = ⑧)
2. **수동 트리거의 자동화**: Dreaming 버튼, /claude_brief, /verify_harness 모두 수동 — 조건 기반 자동 트리거(크기/주기)로 전환하면 사람 개입 없이 성장
3. **단일 의존 제거**: LLM은 3중 폴백 완성, 데이터 소스(⑦)와 백업(⑥)도 동일 수준으로
4. **`/improve` 워크플로우 정착**: 큰 변경은 Fable/Opus가 감사·계획(plans/) → Sonnet/Haiku가 실행 → 검증. 이 문서가 그 첫 산출물

## 4. 실행 현황

| 항목 | 상태 | 날짜 |
|---|---|---|
| ② 문서 표류 보정 (briefing 갭 섹션) | ✅ 완료 | 2026-06-11 |
| ⑨ /improve 스킬 설치 (`~/.hermes/skills/improve/`) | ✅ 완료 | 2026-06-11 |
| ⑦ 주식 일별 캐시 + yf.download 전환 | ✅ 완료 | 2026-06-11 |
| ① L3 증류: `auto_dream_trigger.py` 신규 + launchd 주간(일 03:30) + 1회 실행 (Journal 5건/memory.md 2건/hot.md 정리) | ✅ 완료 | 2026-06-11 |
| ③ safe_reply/safe_edit Markdown 폴백 — 핸들러 전체 16개 **255곳** 적용 완료 | ✅ 완료 | 2026-06-11 |
| ⑤ 모듈 분할: ingest_engine 642→459줄 (`ingest_text_utils.py`) / knowledge_indexer 631→537줄 (`indexer_text_utils.py`) — 순수함수 추출+별칭 바인딩, 호출자 무변경 | ✅ 완료 | 2026-06-11 |
| ④ semantic_index ↔ memory 연동 — `hybrid_recall()` 3소스 통합 (L2+FTS5+semantic_index.db) | ✅ 완료 | 2026-06-11 |
| ⑥ GitHub Remote | ⬜ 차단 (URL 필요) | |

### ①에서 발견된 추가 사실 (2026-06-11)
- `MemoryEngine.dream()`의 L3 커밋은 망각곡선 미달 후보만 전이하는 구조 — 이번 실행에선 전이 대상 0건이라 L3 patterns는 7개 유지. L3 patterns를 직접 늘리려면 `dreaming_v2.py` 경로(텔레그램 Dreaming 버튼) 확인 필요 — 차기 과제
- 첫 실행 시 hybrid_router가 봇 외부에서 DummyRouter로 폴백하는 구조 확인 → auto_dream_trigger는 DeepSeek API 직접 호출로 우회 구현

---
*다음 갱신: 항목 완료 시마다. 관련: [[01_hot]], [[verify_harness_report]], [[자동화_시스템_사용법]]*
