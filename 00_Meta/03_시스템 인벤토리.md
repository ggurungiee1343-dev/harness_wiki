---
tags: []
---
# 시스템 인벤토리 (Software & Environment)

이 문서는 MJ님의 Mac Studio에 설치된 모든 주요 프로그램, AI 에이전트 환경, 개발 런타임 정보를 기록합니다.

---

## 🏛️ 주요 애플리케이션 (Core Apps)

### 1. Obsidian
-   **앱 경로**: `/Applications/Obsidian.app`
-   **보관소(Vault)**: `/Users/bluesea/Applications/Mjobsidian`

### 2. Telegram
-   **앱 경로**: `/Applications/Telegram.app`

### 3. llama.cpp (llama-server)
-   **실행 경로**: `/opt/homebrew/bin/llama-server`
-   **API 경로**: `http://127.0.0.1:8080/v1` (기본 로컬 백엔드 - 상시 기동)
-   **현재 로드 모델**: `Qwen2.5-14B-Instruct-A3B-UD-Q4_K_M.gguf` (경로: `/Users/bluesea/Applications/Mjauto/unsloth/Qwen2.5-14B-Instruct-A3B-Q4_K_M.gguf/`)
-   **실행 옵션**: `-c 32768 -ngl 99 -ctk q8_0 -ctv q8_0 --port 8080`
-   **config.yaml override**: context_length 65536 (Hermes Agent 최소 요구사항 대응)
-   **이전 모델**: ~~Qwen2.5-14B-Instruct (gemma-4-26b-a4b-it, localhost:1234)~~ — 2026-06-07 Qwen2.5-14B-Instruct-A3B로 교체

### 4. ~~LM Studio~~ (삭제됨)
- LM Studio 앱이 제거되었습니다. 로컬 추론은 `llama.cpp (llama-server)`로 대체됨.

---

## 🤖 AI 에이전트 및 자동화 (Agents)

### 5. 하네스 에이전트 (Harness Agent v3.7 - 통합)
-   **소스 경로**: `/Users/bluesea/Applications/Mjauto/Scripts/harness_agent.py`
-   **메인 엔진**: **DeepSeek API (deepseek-chat)**
-   **특징**: 기존 하네스, 맥봇, 안티그래비티 봇의 기능을 하나로 통합. NPU 세만틱 검색, 실시간 웹 탐색, 자가 치유 시스템 및 에빙하우스 망각 곡선 기반 3계층 인지 메모리 시스템(Bio-Memory Engine) 탑재.
-   **토큰**: `8165645310:***` (하네스 전용 토큰 사용)

> [!NOTE]
> **레거시 상태**: 기존 `harness_v2.py`, `mac_bot_v2.py`, `antigravity_bot_v2.py`는 코드 보존을 위해 중지 상태이며 모든 기능은 하네스 에이전트로 통합되었습니다. **`mac_bot_v2.py`**, **`start_bots.sh`**, **`hermes_handlers_backup_20260526.py`** (최종 백업본)는 `Scripts/_archive/`로 이관 완료 (2026-05-26).

### 5. Bio-Memory Engine (L1/L2/L3 메모리 시스템)
|- **관리 파일**:
|  - `modules/bio_memory_engine.py` — L1/L2 관리, Hybrid Trigger (개수 or 1MB), `_get_l2_bytes()` 실시간 KB/MB 보고
|  - `modules/dreamer_layer.py` — L2→L3 증류, `offline_consolidation_forced()` 중요도 하위 50% 강제 전이
|  - `modules/dreaming_v2.py` — L3 패턴 통합, Early Exit 우회, atomic write(`_commit_to_l3_semantic`)

### 6. Hermes Agent (Kanban & Multi-Agent)
-   **실행 파일**: `/Users/bluesea/.local/bin/hermes`
-   **버전**: `v0.14.0 (2026.5.16)`
-   **기능**: SQLite 기반 Kanban 시스템, 다중 프로필(Multi-Profile) 작업 할당 및 Swarm 에이전트(분석/검증/종합) 병렬 처리 지원.
-   **가이드**: `/Users/bluesea/.gemini/antigravity/scratch/hermes_kanban_guide.md`

### 7. Hermes Telegram Bot (Harness v9.2.8 — 태그 자동화 기능 확장)
> **[2026-06-07 모드 업데이트]**: `harness_agent.py` NVIDIA 모드 = GPT OSS 120B (openai/gpt-oss-120b). 이전 NVIDIA NIM 70B(meta/llama3-70b-instruct)는 서비스 종료됨.  
> **현재 모드 3종**: Qwen2.5 14B (로컬) / DeepSeek (API) / GPT OSS 120B (NVIDIA)
- **진입점**: `/Users/bluesea/Applications/Mjauto/Scripts/hermes_local.py`
- **런타임**: Python 3.14.4 (Homebrew Framework)
- **Launchd 레이블**: `com.hermes.bot`
- **Plist**: `/Users/bluesea/Library/LaunchAgents/com.hermes.bot.plist`
|- **상태**: 항시 구동 (KeepAlive), foreground 직접 실행
|- **실행 명령어**: `/usr/local/bin/python3 -u /Users/bluesea/Applications/Mjauto/Scripts/hermes_local.py` (PYTHONUNBUFFERED+ThrottleInterval=5)
|- **로그**: `~/Applications/Mjauto/Scripts/hermes_launchd.log`
- **에러 로그**: `~/Applications/Mjauto/Scripts/hermes_launchd.error.log`
- **핸들러 모듈**:
 - `handlers/` (17개 서브모듈 패키지 — v9.2.6 + 2026-06-09 분할)
   - `_base.py`: 공유 인스턴스 (router, cove_engine, action_layer) + 공통 텍스트 핸들러 (🔄 425줄 — handle_button_callback 분리됨)
   - `_callbacks.py`: 🆕 InlineKeyboard 버튼 콜백 라우터 (2026-06-09 신규) — mem_purge/kanban/perm/verify_harness 처리
   - `_memory.py`: cmd_ask/cmd_cove/cmd_dreaming/cmd_goal/cmd_clip/memory
   - `_system.py`: cmd_status/cmd_audit/cmd_secreview/cmd_delegate/cmd_exec/cmd_model/cmd_caveman (🔄 521줄)
   - `_system_ops.py`: 🆕 cmd_handoff/cmd_search/cmd_restart_bot (2026-06-09 신규, 191줄)
   - `_file.py`: cmd_recent/cmd_ingest/cmd_web
   - `_kanban.py`: cmd_kanban
   - `_paper.py`: 🆕 cmd_paper (humanize/draft/review/list/bundle) (2026-06-09 신규, 582줄)
   - `_research.py`: cmd_research (Knowledge Mesh 파이프라인) (🔄 254줄)
   - `_ui.py`: cmd_start/cmd_help/cmd_verify_harness (InlineKeyboard 버튼 포함)
   - `_vault.py`: cmd_vault (Vault 정합성 진단/중복 스캔 + **Graphify 지식 그래프 분석 — `/vault graph`**) — graphifyy(v0.8.28) 패키지 연동
   - `_grill.py`: cmd_grill (Vault 문서 기반 LLM Q&A)
   - `_orchestrator.py`: cmd_orchestrate / cmd_agent (Multi-Agent 오케스트레이션)
   - `_approval.py`: cmd_tag / 승인 콜백
   - `_meta.py`: cmd_claude_brief (6대 메타 문서 브리핑 생성)
 - 순환참조 방지: `_base.py`에서 다른 모듈 함수 호출 시 **함수 내부 lazy import** 사용. `_callbacks.py`는 자체 logger 정의.
 - `hermes_file_ops.py` (20,734 bytes) — 파일 조작 핸들러: /create, /read, /move, /copy, /delete, /rename, /list, /confirm, /cancel
 - `web_agent_module.py` — 웹 검색/읽기/학술검색 (**백엔드 교체: DDGS 기본, SearXNG 자체호스팅 전환 가능; Firecrawl 우선 + r.jina.ai fallback**)
 - `hermes_harness.py` — ACID 하네스 제어 (/harness, /hdod, /hstatus, /hrollback)
 - `audit_engine.py` (신규) — 포트 스캔/SSH 키/인프라 감사 로깅
- **기능**: 텔레그램 슬래시 명령어 (Ask/CoVe/Web/Search/SearchPaper/Read/Ingest/Exec/Clip/Recent/Status + **Paper Humanize/Draft/Review + Vault Check + Grill** + 파일 조작: Create/Move/Copy/Delete/Rename/List + Confirm/Cancel 확인 시스템), Life-Harness ❸ Action Realization Layer 내장
- **ALLOWED_BASES** (3개 + venu/): `/Applications/`, `/hermes/`, `/.hermes/`, `~/Applications/venu/` (Desktop/Downloads/시스템 폴더 보호)
- **안전 장치**: `/create`, `/delete`, `/rename`, `/move`, `/copy` → 실행 전 `/confirm` 승인 필요, `/cancel` 취소 가능, `/delete`는 휴지통(`~/.Trash/`)으로 이동 (완전 삭제 아님)
- **2026-05-26 업데이트**: `/create` 명령어가 Vault 내 `.md` 파일 생성 시 `FileManager.write_file()` 경로로 라우팅 → 타임스탬프 푸터(`*최종 업데이트*` + `*작성자: 헤르메스봇*`) 자동 추가
- **2026-05-26 업데이트**: `file_manager.py` 타임스탬프 정규식 5포맷 대응 (볼드체, 날짜만, 괄호 노트, 초단위 포맷 보존)
- **2026-05-26 업데이트**: 텔레그램 하단 **🧠 메모리** 버튼 UI를 복잡한 프로세스 목록에서 16GB 파일 캐시를 즉시 확보하는 원클릭(`sudo purge`) 버튼으로 개편 및 무암호 권한 연동.
- **2026-05-26 업데이트**: 파일매니저 및 설정 파일(`config.py`)에 KST(Asia/Seoul) 타임존 강제 동기화 패치 적용. 에이전트 간 시간 불일치 버그 해결.
- **2026-05-26 업데이트**: 텔레그램 `/goal` 장기 목표 설정 명령어 탑재. ~~(야간 `dream_scheduler.py` 내 목표/헌법 자율 감사 기능 연동 — 2026-05-27 폐기)~~ (LLM 400 포맷 버그 및 Git 동기화 경로 수정 완료)
- **2026-05-26 업데이트**: `hermes_local.py`의 `secure_path` 함수 내 절대경로 이중 부착 버그 완벽 패치 (이제 `/list /Users/...` 등 정상 동작).
||- **2026-05-26 업데이트**: **[Phase 3] 4개 연구 SKILL.md 생성 (literature-review, citation-formatter, abstract-writer, argument-mapper) + /paper humanize 법률/학술 문체 변환 명령어 + ✍️ 논문 버튼**. `/chart`는 MJ님 요청으로 삭제됨. hot.md/스크립트 정보.md 업데이트 완료. (`v5.2→v5.5`)
||- **2026-05-28 업데이트**: **#10 /paper draft (논문 초안 생성) 구현 완료**: 주제 입력 → 개요 생성(Outline) → 본문 초안(Draft) → 문체 변환(Humanize) 3단계 LLM 파이프라인. 텔레그램 4096자 초과 시 분할 전송. 도움말/메타 문서 전면 업데이트. (`v5.8→v5.9`)\n||- **2026-05-28 업데이트**: **#12 constitution.local 포인터 패턴 완료**: 중복 내용을 constitution.md/나의 비서 가이드.md 참조 포인터로 대체 (98→50줄). *(이후 v1.2에서 인라인 전환으로 대체됨)* **#13 validate-skills git hook 완료**: `~/.hermes/scripts/validate_skill_md.py` + `~/.git/hooks/pre-commit` 설치. ~~**#14 dream_scheduler ExecPlan 완료** — **[2026-05-27 폐기됨]**~~
||- **2026-05-26 업데이트**: **#19 constitution.local v1.2 인라인 전환 완료**: 기존 #12 포인터 패턴(참조)을 모든 내용 인라인 대체 방식으로 전환 (98→204줄). constitution.md §4/§5/§7도 통합 병합. Gemma4가 단일 파일만 읽어도 모든 규칙 파악 가능.
||- **2026-05-26 업데이트**: `harness_agent.py` 시스템 뇌관 프롬프트에 `[RUN_CMD: 쉘명령어]` 에이전틱 액션 권한 부여. 사용자가 터미널 진단을 자연어로 요구할 때, 수동적으로 텍스트 답변만 하는 것이 아니라 실제 터미널을 열고 직접 확인 후 답변하도록 자율성 대폭 격상.
- **2026-05-26 업데이트**: `harness_agent.py` `import executor` 누락 패치 — 633번째 줄 `executor.execute_bash_command()` 호출 시 NameError 발생, `import executor` 추가로 해결. (`v5.8→v5.9`)
|- **2026-05-26 업데이트**: **llama-server launchd plist 바이너리 경로 brew symlink로 변경**: `brew upgrade` 시 Cellar 버전 경로가 깨지는 문제 근본 해결. (`/opt/homebrew/Cellar/llama.cpp/8960/bin/llama-server` → `/opt/homebrew/bin/llama-server`)
|- **2026-05-27 업데이트**: **Python 3.10 호환성 수정 + 테스트 인프라 구축**: `dialectic_layer/_dreamer.py` + `hermes_context_builder.py` — `from __future__ import annotations` 추가로 `str|None` 문법 Python 3.9.6 호환. **pytest 8.4.2** 설치. **tests/** (`~/Applications/Mjauto/Scripts/tests/`) — 6개 테스트 파일: test_imports.py(32개 modules/ + 9개 handlers/ import 검증), test_bio_memory.py, test_skill_evolver.py, test_kanban_manager.py, test_hybrid_router.py. **87 passed, 0 skipped, 0 failed**. 기존 handler skip 조건(Python 3.10 버전 체크) 제거. 관련 5개 메타 문서 일괄 업데이트.
|| - **🚀 2026-05-27 (12:50) 업데이트**: **/ingest v2.1 — 루트 파일 LLM 분류 이동**: `_process_root_files()` 전면 재작성. 루트 방치 파일 26개를 LLM(DeepSeek) 분류 후 4개 폴더(10_AI_Automation/20_Research/30_Journal/Unsorted)로 이동 + frontmatter(category 태그) 업데이트. `_build_tag_prompt` 제거(dead code). `cmd_ingest()`에 `_call_llm` 연결 완료. 루트 clean. | ingest_engine.py, handlers/_file.py | 완료 |
|| - **🚀 2026-05-27 (12:56) 업데이트**: **/ingest v2.2 — 모드 중립 분류 라우팅**: `cmd_ingest()`가 DeepSeek 고정 대신 사용자 현재 모드(Gemma4/DeepSeek/NVIDIA)를 따르는 `_ingest_llm_wrapper` 사용. `ingest_engine.py` 중복 `_build_classify_prompt` 제거. | handlers/_base.py, handlers/_file.py, modules/ingest_engine.py | 완료 |
||- **2026-05-28 업데이트**: **Dreaming/Ingest/Recent/DummyRouter 버그픽스 완료**: `_memory.py` Dreaming 3종 (MemoryEngine import 누락, DummyRouter 우회, effective_message), `_file.py` Ingest 2종 + Recent 1종, `hermes_local.py` DummyRouter 전 서브모듈 패치. `/dreaming`(Telegram 명령어), `/ingest`, `/recent` 복구 — 단, 야간 스케줄러(dream_scheduler.py)는 2026-05-27 폐기됨.
|- **2026-05-28 업데이트**: **handlers/ 9개 서브모듈 (+_vault, _grill)**: `_vault.py`(Vault 정합성 진단/중복 스캔 `/vault check`, `/vault duplicates`) + `_grill.py`(Vault 문서 기반 LLM Q&A `/grill`) 신규. 하단 키보드 `🔍 보관함 진단` 버튼 추가. `handlers/_base.py` 버튼 매핑 업데이트. `vault_scanner.py` 타임스탬프 동적화. (`v6.5→v6.6`)
|| ~~**🚀 2026-06-03 업데이트**: **[A] 경고 정리**~~: ~~`hermes_local.py` warnings.filterwarnings 추가 (DeprecationWarning, UserWarning 무시).~~ **[C] 주간 펄스 리포트**: ~~`weekly_pulse.py` 생성 + `com.bluesea.weekly_pulse.plist` launchd 등록 (매주 일요일 23:00).~~ **[2026-05-27 weekly_pulse 폐기됨]**. **[B] style_profile 일반 명령어 주입**: `handlers/_base.py` `_get_style_profile()` 캐시 추가 + `_call_llm()`에 style_profile 텍스트 시스템 프롬프트 주입 + `harness_agent.py handle_message()`에도 동일 주입. (`v6.6→v6.7`)
||- **2026-05-27 업데이트**: **봇 Polling 안정화 (Zombie Poller 방지)**: `hermes_local.py` — `app.run_polling()`(loop.run_forever) → 직접 `updater.running` 모니터링. asyncio 예외 핸들러 등록 + 로깅 레벨 격상. `_auto_heal_loop` bare except 제거. `com.hermes.bot.plist` — PYTHONUNBUFFERED=1 + `-u` + ThrottleInterval=5. 이제 polling 중단 시 `sys.exit(0)` → launchd KeepAlive 재시작. (`v6.8→v6.9`)
- **🚀 2026-06-01 업데이트**: **v9.2 — SIA 피드백 학습 + 모니터링 엔진 + 멀티 모델 로드 밸런싱**: (1) `modules/sia_engine.py` — SelfImprovingAgent: 피드백 기록(평점/맥락) → 추세 분석 → LLM 개선 제안 (DeepSeek). 격리 SQLite DB. (2) `modules/monitoring_engine.py` — MonitoringEngine: 액션별 메트릭(지연시간/성공률) 기록, 에러율 추적, 3축 임계 경보. 격리 SQLite DB. (3) `modules/load_balancer.py` — ModelLoadBalancer: 성능 가중치 기반 모델 선택, 성공/실패/지연시간 기록, 주기적 가중치 재조정. 격리 SQLite DB. (4) `core_reducer.py` 통합 — SIA/Monitoring 피드백 라우팅 메서드 추가. (5) `hybrid_router.py` 통합 — 로드밸런서 연동 (`select_best_model()` + `record_model_performance()`). (6) **전체 pytest 150/150 ✅ 전면 통과** (기존 116 + 신규 34). (`v8.1→v9.2`)
- **2026-06-01 22:30 업데이트**: **PermissionBridge 2-tier 도구 권한 게이트웨이 + Ingest 저신뢰 Inbox Deferral + ENCYCLOPEDIA.md 2항목 + GUIDE.md 9.2**: (1) `modules/permission_bridge.py` 신규 — 2-tier tool authorization: 내부 도구 자동 승인, 외부 영향 도구(RUN_CMD/SAVE/DELETE/MOVE/COPY/RENAME) Telegram 인라인 승인 필요. (2) `harness_agent.py` + `handlers/_base.py` — RUN_CMD/SAVE/파일작업 PermissionBridge 인가 경유. (3) `ingest_engine.py` — 저신뢰 분류/파싱 실패 시 `Inbox/*_pending.md` deferral → `_process_inbox()` 재분류. (4) `GUIDE.md` changelog + 9.2 해야할 일 섹션 신설. (5) `ENCYCLOPEDIA.md` PermissionBridge + Save-vs-Organize 2항목 추가. (6) meta 7종 전면 업데이트. (`v9.2`)
|- **🚀 2026-06-03 업데이트**: **PKM_2 Knowledge Mesh v1.0 — 4개 모듈 + /research 연동**:
| (1) `modules/knowledge_mesh_orchestrator.py` (359줄) — JSON 레시피 기반 DAG 실행, arXiv/Semantic Scholar 검색, 로컬 의미 검색, 타임라인 병합, 교차 참조, LLM 요약. LRU 캐시 128개.
| (2) `modules/timeline_builder.py` (77줄) — 통합 타임라인 정렬 + arXiv ID/DOI 중복 병합.
| (3) `modules/cross_reference_analyzer.py` (131줄) — TF-IDF 코사인 유사도 + 시간 감쇠. alignment_type(predict_and_realize/retrospective_match).
| (4) `modules/auto_topic_manager.py` (178줄) — TF-IDF 기반 주제 분류. 5개 기본 템플릿(AI/ML, 법학/AI 규제, 연구 방법론, 자동화/시스템, 기타/미분류). ~/.hermes/runtime/pkm_topics.json 저장.
| (5) `handlers/_research.py` 확장 (796줄) — `/research` 명령어 신규: stats, topics, classify, classifyall, recluster, local(local_search), tl(timeline), xref(cross_reference), 전체 파이프라인. 기존 논문 핸들러(/paper)와 병합.
| 의존성: `knowledge_indexer.py` (HybridKnowledgeIndexer) 재사용. 외부 pip 패키지 불필요.
- **2026-06-03 업데이트**: `wiki_auto_stamper.py`를 확장하여 실시간 파일 저장 감지 시 YAML frontmatter의 태그를 본문 해시태그와 병합하여 최대 8개로 자동 유지하고, 본문의 대괄호 `링크`를 대괄호 없는 단순 텍스트로 풀어주는 자동 정돈 기능 추가.
- **2026-06-03 업데이트**: 하네스 컨트롤(`harness`, `hdod`, `hstatus`, `hrollback`)의 작동 원리, 이유, 사용법, 효용성을 완벽 분석한 `하네스_컨트롤_가이드.md` 문서 추가 및 깨진 링크(`지식 베이스 사용 가이드.md`) 정리.

    > [!WARNING]
> **409 Conflict 재발 방지**: 이 서비스는 plist에서 **직접 foreground**로 실행해야 합니다. `start_bots.sh`(`Scripts/_archive/`로 이관 완료) 같은 shell wrapper 경유로 백그라운드(&) 실행 시 KeepAlive가 무한 재실행되어 2개 인스턴스가 동시 폴링 → 409 Conflict 발생. 3차례 반복 후 2026-05-24에 근본 해결 완료. **추가**: v6.9의 `updater.running` 모니터링도 외부 getUpdates 호출 시 polling이 중단되는 것을 launchd 재시작으로 해결함. (완전 방지는 아니지만 재시작 보장)

### 8. Hermes2 Telegram Bot (클라우드 전용 — DeepSeek + NVIDIA)
- **위치**: `/Users/bluesea/Applications/venu/`
- **진입점**: `/Users/bluesea/Applications/venu/hermes2_bot.py`
- **런타임**: Python 3.11 (venu/venv)
- **Launchd 레이블**: `ai.hermes2.bot`
- **Plist**: `/Users/bluesea/Applications/venu/ai.hermes2.bot.plist`
- **상태**: 항시 구동 (KeepAlive: CrashedOnly), foreground 직접 실행
- **HERMES_HOME**: `/Users/bluesea/Applications/venu/.hermes2/`
- **CLI 래퍼**: `/Users/bluesea/Applications/venu/hermes2.sh`
- **LLM 라우팅**: DeepSeek(api.deepseek.com, 기본) ↔ GPT OSS 120B(NVIDIA, 폴백) — 클라우드 전용, 게이트웨이 미사용  
  - [2026-06-07] NVIDIA NIM 70B(meta/llama3-70b-instruct) → GPT OSS 120B(openai/gpt-oss-120b) 교체 (404 오류로 서비스 종료 확인)
- **보안**: `secure_path()` + `ALLOWED_BASE_PATHS` — 4개 경로 (Applications/, hermes/, .hermes/, venu/)
- **모드 시스템**:
모드 | 설명 |
:--- | :--- |
**deepseek** | 기본 모드 — DeepSeek V4 Flash (직접 API) |

### 9. Hermes Gateway config.yaml 최적화 (2026-06-07)
- **파일**: `~/.hermes/config.yaml`
- **비활성화된 toolsets**: `browser`, `vision` — 로컬 LLM 시스템 프롬프트 절감 (~1,000 tokens)
- **비활성화된 skills**: 115개 전체 (Hermes Hub 스킬) — 시스템 프롬프트 3,348 tokens 절감
  - 박사님 시스템은 Hermes Hub 스킬 미사용 (직접 제작 `handlers/`, `modules/` 운영)
  - Skills 파일 자체는 삭제되지 않음, prompt 인덱스에서만 제외
- **효과**: Qwen2.5-14B 첫 응답 ~120초 → ~77초 (나머지는 hermes-cli tool schema 한계)

### 9. Hermes WebUI
-   **실행 경로**: `/Users/bluesea/Applications/hermes-webui/server.py`
-   **Launchd 레이블**: `com.bluesea.hermes-webui`
-   **포트**: **8787** (WebUI) → **8642** (Gateway) → **8080** (llama-server) 체인
-   **상태**: 항시 구동 (KeepAlive), 웹 브라우저 기반 AI 채팅 인터페이스
-   **WorkDir**: `/Users/bluesea/Applications/hermes-webui/`
-   **지원 모델 (2026-06-07 기준)**:
    - DeepSeek Chat (API) ✅
    - GPT OSS 120B (NVIDIA API) ✅
    - Minimax M2.7 (NVIDIA API) ✅
    - Qwen2.5-14B (로컬 llama-server:8080) ✅ (첫 응답 ~2분, KV 캐시 후 빠름)
-   **모델 드롭다운 필터**: `hermes-webui/api/config.py`의 `_whitelist_keywords` 리스트에 있는 모델만 표시됨
-   **모델 목록 캐시**: `~/.hermes/webui/models_cache.json` — config 변경 후 안 바뀌면 이 파일 삭제
-   **컨텍스트 최적화 (2026-06-02 및 2026-06-05 적용)**:
    - 대화 맥락 단절 방지를 위해 `.hermes2/config.yaml`에 `context.engine: truncation`, `threshold: 0.85` 설정.
    - SWA 캐시 무효화 및 슬롯 분할로 인한 컨텍스트 유실(32k 제한) 방지를 위해 `com.bluesea.llama_server2.plist`와 `start_llama.sh`에 `llama-server -np 1` 및 `--cache-reuse 256`을 적용하여 단일 슬롯에 65,536 컨텍스트를 온전히 할당 완료.
-   **초기화 프로토콜 (2026-06-04 적용)**: `USER.md`를 통해 새 세션 시작 시 `constitution.local.md` 및 `01_hot.md`를 먼저 스캔하도록 강제화.

---

## 🔐 환경 변수 중앙 관리 (.env)
-   **파일 위치**: `/Users/bluesea/Applications/Mjauto/.env`
-   **관리 항목**: 모든 Bot Token, API Key, 시스템 절대 경로, NVIDIA API 키 등.
-   **보안 방침**: API 키와 토큰 정보는 반드시 `.env` 파일에서만 중앙 관리하며 소스 코드에 직접 기입하지 않습니다.

---

## 🛠️ 개발 및 시스템 환경 (Runtime)
-   **OS**: macOS Sequoia (Mac Studio **M4**, 메모리 **36GB**)
-   **Python**: `3.14.4` (Global) — PyMuPDF (fitz) v1.27.2.3 (PDF 텍스트 추출용), **mcp==1.27.2** (2026-06-10 설치, MCP 서버용), **yfinance** (주식 데이터), **httpx-sse, sse-starlette, pydantic-settings** (mcp 의존)
-   **Node.js**: v20+ (일부 AI 서비스용)
-   **FFmpeg**: `8.1.1` (`/opt/homebrew/bin/ffmpeg`) — 🆕 2026-06-08 8.1→8.1.1 보안 업그레이드. Hermes1/2 핵심 파이프라인에서 미사용. Hermes2 creative 스킬(pixel-art, manim-video, p5js)에서만 로컬 파일 변환용으로 사용. 절대경로 고정 완료.
-   **Allowed User ID**: `5365732604` (MJ 전용 ID)

---
*최종 업데이트: 2026-06-28 04:39*
*이전 업데이트: 2026-06-07 21:15 — NIM→GPT OSS 120B 교체, Skills/Toolset 비활성화, M4 스펙 수정*
*작성자: Hermes*

---

## 📋 V9.0 추가 모듈 (설계 제안)
*V9.0 프로그램 추가 2.md 기준 — 코드 미구현, 설계 검토 단계*

### NVIDIA SkillSpector (v9.4.2)
- **역할**: 스킬 사용 패턴 분석 및 프로파일링 도구
- **상태**: 📝 설계 제안, 미구현

### Anthropic CyberAuditor (v9.5)
- **역할**: 코드 보안 감사 및 취약점 분석 도구
- **상태**: 📝 설계 제안, 미구현

### OpenSRE SRE Investigator (v9.5.1)
- **역할**: 장애 조사 및 원인 분석 자동화 도구
- **상태**: 📝 설계 제안, 미구현

**참조**: `wiki/99_Archive/V9.0 프로그램 추가 2.md` (2026-06-01 00_Meta→99_Archive 이관)

---

## 🔄 모델 전환 도구 (2026-06-06 추가)

- **스크립트**: `~/Applications/venu/scripts/switch_model.sh`
- **심볼릭 링크**: `/usr/local/bin/switch-model` (어디서든 실행 가능)
- **용도**: Hermes Agent(Gateway+WebUI+Telegram Bot)의 메인 모델 전환
- **명령어**:
  - `switch-model got` — GPT-OSS-120B (NVIDIA, 무료)로 전환
  - `switch-model deepseek` — DeepSeek Chat으로 전환
- **원칙**: 전환 시 이전 설정을 `# [SWITCHED to ...]` 주석처리로 보존, 파일 삭제 없음
- **Qwen2.5-14B-Instruct**: 별도 API(localhost:8080)이므로 전환 영향 없음
- **적용 대상**: 두 Hermes Home 모두 전환
  - `~/.hermes/config.yaml` (Gateway + WebUI)
  - `~/Applications/venu/.hermes2/config.yaml` (Telegram Bot)
- **주의**: 전환 후 `hermes gateway restart` 필요

---

## 📊 MJstock 데이터 파일 (2026-06-28 추가)

| 파일/디렉토리 | 경로 | 설명 | 생성일 |
|---|---|---|---|
| `tracker/` | `/Users/bluesea/Applications/Mjstock/tracker/` | 시그널 추적 데이터 디렉토리 | 2026-06-28 신규 생성 |
| `tracker/signal_log.csv` | `/Users/bluesea/Applications/Mjstock/tracker/signal_log.csv` | 개별/정기 스캔 결과 quant CSV. `signal_tracker.record_scan()` 호출로 자동 기록. 스키마: `SIGNAL_LOG_FIELDS` (star_signal_date, circle_date, circle_price, exit_price, return_30d 포함) | 2026-06-28 신규 생성 |

**관련 모듈**:
- `screener/signal_tracker.py` — record_scan(), update_returns(), BUY_SIGNALS/SELL_SIGNALS 상수
- `screener/scan_all_ticker.py` — 개별 종목 검색 결과 → record_scan() 호출
- `screener/run_scan.py` — 정기 스캔 결과 → record_scan() 호출

---

## 📋 AI Agent Memory 개념 분석 — 문제 인식 저장 (2026-06-05)

**참조**: 01_hot.md 동일 섹션 상세.

블로그 "AI Agent Memory" vs 현재 Bio-Memory Engine 대비 분석 완료. 핵심 갭 항목은 01_hot.md 참조. 본 문서는 참고 위치 기록용.