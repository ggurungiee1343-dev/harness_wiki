---
tags: []
---
# 시스템 상태 및 작업 이력 (Shared Registry)

이 파일은 Antigravity와 Harness가 시스템에 가한 모든 주요 변경 사항을 기록하는 **공통 장부**입니다. 업무 시작 전 반드시 이 파일을 확인하여 최신 상태를 파악해야 합니다.

## 🕒 최근 작업 이력

## 2026-06-24 논문 기반 검증층 4종 개선 (Governance Decay / Memory Contagion / Skill Coverage / Process Verification)

**프로젝트**: `/Users/bluesea/Applications/Mjauto/Scripts`

| 파일 | 변경 내용 |
|---|---|
| `modules/history_manager.py` | `compact_and_save()` + `get_history_for_llm()` 양쪽에 GOVERNANCE_ANCHOR 메시지 삽입. 내용: Grounding 원칙 / Lock Stack 4개 / 경로 제한 / 지식 보존 / 증거 기반 보고. 압축 후에도 핵심 거버넌스 규칙이 LLM 컨텍스트 맨 앞에 항상 살아있도록 보장. |
| `modules/bio_memory_engine.py` | `_promote_to_l2()` episode에 `confidence`, `observation_count`, `provenance` 필드 추가. `save_procedural_memory()` L3 절차 기억에 `confidence`, `success_count` 필드 추가. confidence < 0.6이면 불확실 메모리로 처리 예정. |
| `modules/weakness_miner.py` | `record_skill_invocation(skill_name)` 신규 메서드 — 스킬 호출 기록. `get_skill_coverage()` 신규 메서드 — ~/.hermes/skills + ~/.claude/skills 전체 스캔 후 호출률 반환. `skill_invocations` 테이블 weakness.db에 신규 생성. |
| `verification_engine.py` | `verify_file_changed()`, `verify_db_row_exists()`, `snapshot_file_hash()` 3개 메서드 추가. Process Verification (결과가 아닌 과정 검증) 지원. |
| `handlers/_system.py` | `/status` 명령어에 스킬 커버리지 섹션 추가 — 아이콘 + 미호출 스킬 목록 표시. |

**근거 논문**:
- Governance Decay (arXiv 2606.22528) — 압축 후 안전 제약 위반율 0%→59%
- Memory Contagion (arXiv 2606.23195) — 편향된 평가가 메모리를 통해 누적 전파
- Skill Coverage (arXiv 2606.20659) — 스킬 행동 제약의 39%만 실제 검증됨
- GroundEval (arXiv 2606.22737) — LLM 판사가 결과만 보고 과정 무시

**연결 구조**:
- `history_manager.py` → GOVERNANCE_ANCHOR → LLM 컨텍스트 최상단 (압축 전/후 모두)
- `bio_memory_engine.py` → L2/L3 메모리 confidence 필드 → 향후 불확실 메모리 격리 로직
- `weakness_miner.py` → `skill_invocations` 테이블 → `get_skill_coverage()` → `handlers/_system.py` `/status`
- `verification_engine.py` → Process Verification 3종 → harness 검증 루프 연동 예정

**에러 진단 포인트**:
- GOVERNANCE_ANCHOR 미적용 의심 → history_manager.get_history_for_llm() 반환값 첫 메시지 확인
- 스킬 커버리지 0% 표시 → weakness.db의 skill_invocations 테이블 존재 여부 확인
- confidence 필드 누락 → bio_memory_engine L2 promote 시 episode dict 구조 확인

---

## 2026-06-24 MJstock results HTML 차트 iframe 방식 전환 (버그 수정)

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

| 파일 | 변경 내용 |
|---|---|
| `generate_results_html.py` | 차트 렌더링 방식 전면 교체. **기존**: Plotly.newPlot 데이터 추출+재조립+JS 렌더링 (모바일 onclick 실패). **신규**: `_load_chart_iframe()` — 원본 chart HTML에서 4.8MB Plotly 번들을 CDN 태그로 교체(75KB) → base64 data URI → `<iframe data-src="...">` lazy load. `toggleChart()`가 `iframe.src = iframe.dataset.src`만 설정하는 방식으로 단순화. `_decode_typed_arrays()`, `_extract_plotly_calls()` 함수는 유지(미래 활용 가능). 파일 크기: 6MB → 1.8MB. |
| `docs/MJstock_사용설명서.html` | "4. 차트 보는 법" 섹션 업데이트. 텔레그램 결과 HTML에서 차트 열기 방법(iframe 방식) 명시. 재발방지 warn 박스 추가: 4.8MB+ JS 인라인 금지, Plotly 재조립 방식 금지, `_load_chart_iframe()` 패턴이 정답임을 기록. |

**근본 원인** (다음 세션 참고):
- 4.8MB Plotly.js를 `<head>` 인라인 번들 → 모바일 브라우저 JS 파싱 중 onclick 바인딩 자체 실패 (클릭해도 무반응)
- Plotly 데이터 재조립 과정에서 bdata TypedArray, margin null, autosize, divId vs DOM element 등 복합 실패 요인

**연결 구조**:
- `generate_results_html.py` → `_load_chart_iframe()` → `charts/{screener_key}/{ticker}_farming_{date}*.html` (원본 차트) → CDN 교체 → base64 → iframe
- `results/_html/results_{screener_key}_{date}.html` (출력, 1.8MB) → 텔레그램 전송

**에러 진단 포인트**:
- 차트 미표시(클릭 무반응) → `_PLOTLY_BUNDLE_RE` 패턴이 원본 차트 번들을 정확히 매칭하는지 확인 (`plotly.js v3.6.0`, `window.Plotly = Plotly;` 패턴)
- iframe 내 차트 빈 화면 → 원본 chart HTML 파일이 정상인지 직접 브라우저에서 열어 확인
- 파일 크기 폭증 → 원본 chart HTML에 번들 제거 실패 (regex miss) → 로그에서 `CDN 교체 후` 크기 확인

---

## 2026-06-24 MJstock 차트 전체화면 모달 전환 + position_monitor_guide 이동 + 사용설명서 업데이트

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

| 파일 | 변경 내용 |
|---|---|
| `generate_results_html.py` | 차트 표시 방식 전면 변경: 종목 행 아래 인라인 펼침 → 클릭 시 전체화면 오버레이(position:fixed, z-index:9998). 상단 `#modal-toolbar`(종목명 + ✕ 닫기 버튼) 고정. JS: `toggleChart(idx)` → `closeChartModal()` 추가, ESC키 닫기 지원. **x축 range 누락 버그 수정**: 기존 y축만 autorange 처리 → x축도 `layout["xaxis*"].pop("range")` + `autorange=True` 추가(전체 히스토리 정상 표시). 차트 height: `900px` → `calc(100vh - 56px)` 반응형. Plotly config: `{"responsive":true,"scrollZoom":true}` 추가. `layout["height"]`·`layout["width"]` 제거(모달 크기 자동). |
| `sellstock/position_monitor_guide.html` (이동) | 이전 위치: `docs/position_monitor_guide.html`. /mjbuy, /mjsell, /mjpositions 텔레그램 명령어 상세 문서 추가. 전체 sellstock 스크립트 설명 + 퀀트 매도 시그널 논리 추가(v2.0). |
| `docs/MJstock_사용설명서.html` | 차트 사용법 설명 수정: "종목 탭 → 인라인 표시" → "종목 탭 → 전체화면 오버레이, ESC/✕ 닫기" 반영. |

**연결 구조**:
- `generate_results_html.py` → `results/_html/results_{screener_key}_{date}.html` (차트 모달 방식)
- `sellstock/position_monitor_guide.html` ← 기존 `docs/` 위치에서 이동 (sellstock 스크립트 문서화)

**에러 진단 포인트**:
- 차트 x축 전체 히스토리 미표시 → `generate_results_html.py` 내 `xaxis*` range 제거 코드 확인 (`layout["xaxis*"].pop("range")` 누락 여부)
- 모달 ESC 닫기 미동작 → JS `document.addEventListener("keydown")` 이벤트 핸들러 확인
- `position_monitor_guide.html` 링크 깨짐 → 경로가 `docs/` → `sellstock/`으로 변경됐음에 주의

---

## 2026-06-24 MJstock HTML 결과 리포터 + 텔레그램 콜백 버튼 + NASDAQ 유니버스 확장 + app.py Tab 2 개편

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

| 파일 | 변경 내용 |
|---|---|
| `generate_results_html.py` (NEW) | 스캔 결과 자기완결형 HTML 생성기 (CDN Plotly.js 2.27.0, ~1.4MB). 시그널 종목 상단 → 점수 종목 하단 배열. 종목 행 클릭 → Plotly 차트 인라인 토글(toggleChart). `_extract_plotly_calls()`: 기존 chart html에서 data/layout/config 추출. Output: `results/_html/results_{screener_key}_{date}.html` |
| `auto_scan_morning.py` | `run_screener()` 반환값 `{ok, count_score, count_signal, date_str}` 구조로 변경. 스캔 성공 시 `generate_results_html()` 자동 호출. `send_results()` 신규: 검색기별 `시그널(N개), 점수(N개)` 포맷 텔레그램 메시지. `_send_telegram_with_buttons()` 신규: 인라인 키보드 버튼 포함 메시지. 결과 없는 검색기 버튼 미첨부. `SCREENER_NAMES_LOCAL` 딕셔너리 추가. 기존 차트 파일 첨부 방식 완전 제거. |
| `handlers/_callbacks.py` | `mjstock_results__` prefix 콜백 라우팅 추가 (line 64-73) |
| `handlers/_stock.py` | `callback_mjstock_results()` 신규: callback_data 파싱(`mjstock_results__{screener_key}__{date_str}`), HTML 파일 없으면 실시간 생성 후 `send_document`로 전송 |
| `mjstock_callback_bot.py` (NEW, inactive) | 독립 콜백 봇 데몬. 409 충돌로 미사용 — Hermes 봇이 콜백 처리하므로 비활성 상태 |
| `~/Library/LaunchAgents/com.mjstock.callbackbot.plist` (NEW, unloaded) | mjstock_callback_bot.py용 launchd plist. unload 상태 유지 |
| `screener/data_loader.py` | `_update_nasdaq()`: Wikipedia NASDAQ-100 → NASDAQ Screener API 교체. `https://api.nasdaq.com/api/screener/stocks?tableonly=true&limit=5000&exchange=NASDAQ&download=true`. 시총 기준 상위 1000개 저장 → `nasdaq1000.csv`. `_parse_cap()` 신규: 달러 숫자 문자열 파싱(T/B/M suffix 없음). 버그 수정: `c.lower()` → `str(c).lower()` (integer column name 오류 방지) |
| `app.py` | Tab 2 스캔 탭 개편: selectbox → checkbox 그리드(2열). [▶️ 스캔 시작] 버튼 우상단. `st.empty()` 플레이스홀더로 스캔 중 진행 상황 표시. 선택된 검색기만 순차 실행. |
| `docs/MJstock_사용설명서.html` | 새 텔레그램 메시지 포맷 안내 섹션 추가. 시그널 vs 점수 구분 표 추가. [결과 보기] 버튼 동작 설명 추가. |

**연결 구조**:
- `auto_scan_morning.py` → `generate_results_html()` → `results/_html/results_{screener_key}_{date}.html`
- `auto_scan_morning.py` → `_send_telegram_with_buttons()` → 텔레그램 인라인 버튼(callback_data: `mjstock_results__{key}__{date}`)
- 텔레그램 버튼 탭 → Hermes `handlers/_callbacks.py` → `handlers/_stock.callback_mjstock_results()` → HTML `send_document`
- `screener/data_loader.py` → NASDAQ Screener API → `data/nasdaq1000.csv`

**에러 진단 포인트**:
- HTML 파일 미생성 → `generate_results_html.py` import 오류 또는 `results/_html/` 디렉터리 없음 확인
- 텔레그램 버튼 무반응 → Hermes `_callbacks.py` `mjstock_results__` prefix 라우팅 등록 여부 확인
- NASDAQ 1000개 미갱신 → `data_loader._update_nasdaq()` 내 API 응답 확인, `marketCap` 컬럼 정수형 여부 확인(`str()` 변환 적용됐는지)
- `callback_mjstock_results` HTML 없음 에러 → HTML 실시간 생성 경로(`generate_results_html()`) 호출 로그 확인

---

## 2026-06-23 핸들러 전체 먹통 버그 3종 수정 — executor SyntaxError / restart 레이스컨디션 / vault 노드 초과

**프로젝트**: `/Users/bluesea/Applications/Mjauto/Scripts`

| 파일 | 변경 내용 |
|---|---|
| `handlers/_base.py` | `from executor import execute_bash_command` 제거. PyPI `executor` 패키지가 Python 3.14에서 `async` 예약어 SyntaxError → 패키지 전체 import 실패. `asyncio.create_subprocess_shell` 기반 `execute_bash_command()` 20줄 직접 구현으로 교체. 16개 핸들러 import 성공 검증. |
| `handlers/_system_ops.py` | `/restart_bot` 레이스 컨디션 수정. 기존 NEW 먼저 기동 → 2초 대기 → OLD SIGTERM 순서에서, OLD 먼저 SIGTERM → `shell=True, sleep 1 && python3 hermes_local.py`로 1초 후 NEW 기동 순서로 변경. 두 인스턴스 동시 폴링으로 인한 409 Conflict + 양쪽 동시 사망 패턴 해소. |
| `handlers/_vault.py` | `/vault graph` 노드 수 초과 에러 수정. Vault 5298 노드 > graphify 기본 제한 5000. `GRAPHIFY_VIZ_NODE_LIMIT=10000` 환경변수 자동 설정 추가. `to_html()` 실패 시 HTML skip + 텍스트 리포트만 반환 fallback 추가. |
| `venu/.hermes2/.env` | `TELEGRAM_BOT_TOKEN` 추가. plist bootout/재등록 시 토큰 누락 → 빈 토큰으로 API 호출 → DNS 에러 즉시 crash 패턴 재발방지. |

**핵심 원인 — executor 패키지 단일 장애점**:
- `handlers/_base.py` 최상단 `from executor import execute_bash_command` 한 줄
- → `handlers/__init__.py`가 `_base`를 import
- → 16개 핸들러 전부 `__init__` 통해 로드 → 전부 import 실패
- → `/help`를 포함한 모든 명령어 실행 시 아무 응답 없음

**연결 구조**:
- `handlers/_base.py` → `handlers/__init__.py` → 16개 핸들러 전체
- `handlers/_system_ops.py` → `subprocess.Popen` → `shell=True, sleep 1 && hermes_local.py`
- `handlers/_vault.py` → `GRAPHIFY_VIZ_NODE_LIMIT` 환경변수 → graphify HTML 생성

**에러 진단 포인트**:
- 모든 핸들러 무응답 → `python3 -c "import handlers"` 실행 → SyntaxError / ImportError 확인
- `/restart_bot` 후 봇 미복구 → 두 인스턴스 동시 생존 여부 `ps aux | grep hermes_local` 확인
- `/vault graph` 에러 → `GRAPHIFY_VIZ_NODE_LIMIT` 환경변수 설정 여부 확인

---

## 2026-06-23 MJstock 장중 스캔 크래시 수정 + 퀀트 CSV 60컬럼 고도화

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

| 파일 | 변경 내용 |
|---|---|
| `screener/run_scan.py` | US/KR 스캔 루프에 빈 DataFrame 이중 가드 추가 (`prep_daily_fn` 전후). US 스캔 `check_fn` 블록 전체 `try/except`로 래핑. `samdoli`/`nongsa_danta`(US) 및 `samdoli_kr`/`nongsa_danta_kr`(KR) 분기 추가. KR 스캔에 `prep_daily_fn` 호출 로직 추가(기존 누락). numpy 임포트 추가. `compute_quant_snapshot()` 9개 신규 컬럼 추가 (50→60컬럼). |
| `screener/screen_samdoli_us.py` | `prepare_5min()` — `df.index.name = None` 추가. index명과 column명 'date' 중복 → groupby 오류 수정. |
| `screener/screen_nongsa_danta_us.py` | 동일 date 충돌 수정. |
| `screener/screen_nongsa_danta_kr.py` | 동일 date 충돌 수정. |

**신규 퀀트 컬럼 (매매 타점 정밀도 향상)**:

| 컬럼 | 설명 |
|---|---|
| `ema45_dist_pct` | 삼돌이 핵심선 EMA45 이격도 — 진입 타이밍 판단 |
| `adx14` | ADX(14) 추세 강도 — 25 이상 = 추세장 |
| `mfi14` | Money Flow Index(14) — 80↑ 과매수, 20↓ 과매도 |
| `gap_pct` | 갭 등락율 (전일종가→당일시가) |
| `obv_slope_5d` | OBV 5일 기울기 — 양수=순매수 방향 |
| `lower_wick_pct` | 아래꼬리 비율 (캔들 total range 대비) |
| `upper_wick_pct` | 위꼬리 비율 |
| `stoch_rsi_k` | Stochastic RSI K선 — 20 이하 = 과매도 |
| `stoch_rsi_d` | Stochastic RSI D선 |
| `dd_dist_pct` | 삼돌이 반등선(DD) 이격도 — samdoli 검색기만 채워짐 |

**버그 원인**:
- `IndexError: single positional indexer is out-of-bounds` → top-n 500 유니버스에서 KIS API가 일부 종목에 빈 DataFrame 반환 → `iloc[-1]`, `df["close"]` 실패
- `KeyError: 'close'` → 빈 DataFrame에 `prep_daily_fn` 적용 → 충돌
- `'date' is both an index level and a column label` → 5분봉 prepare 함수에서 index 이름 정리 없이 동명 column 생성

**에러 진단 포인트**:
- 장중 스캔 top-n 500 실패 → `result.stderr.splitlines()[-5:]` 확인 → IndexError/KeyError이면 이 패치 확인
- `dd_dist_pct` NaN → samdoli 외 검색기는 DD 컬럼 없음 → 정상

---

## 2026-06-23 봇 무응답 버그 3종 수정 — _base.py / _system_ops.py / check_bot_alive.sh / botwatch

**프로젝트**: `/Users/bluesea/Applications/Mjauto/Scripts`

| 파일 | 변경 내용 |
|---|---|
| `handlers/_base.py` | `get_saturation()` → `get_context_pressure()` 오타 수정. 오타로 인해 모든 메시지가 AttributeError → 무응답으로 죽는 패턴 해소. |
| `handlers/_system_ops.py` | `/restart_bot` 재시작 방식 변경: launchctl unload/load 제거 → `subprocess.Popen(start_new_session=True)`로 새 프로세스 먼저 기동 후 기존 프로세스 SIGTERM. |
| `Scripts/check_bot_alive.sh` | 프로세스 완전 소멸 케이스(PID=0) 감지 로직 추가. launchctl 실패 시 nohup fallback. 복구 성공/실패 텔레그램 알림 추가. |
| `~/Library/LaunchAgents/com.hermes.botwatch.plist` | launchd bootstrap 재등록: `launchctl bootstrap gui/$(id -u)` 실행. botwatch 자동 복구 기능 정상화. |

**연결 구조**:
- `handlers/_base.py` → `history_mgr.get_context_pressure()` (HistoryManager)
- `handlers/_system_ops.py` → `subprocess.Popen` → 새 `hermes_local.py` 프로세스
- `com.hermes.botwatch` → `check_bot_alive.sh` (5분마다) → 봇 프로세스 감지·복구

**에러 진단 포인트**:
- 봇 무응답 + LazyLoad 이후 로그 없음 → `add_to_history()` AttributeError 의심 → `python3 -c "from handlers._base import add_to_history; print('OK')"` 테스트
- `/restart_bot` 후 봇 미복구 → launchctl 방식 여부 확인. Popen 방식인지 점검
- botwatch 미실행 → `launchctl list com.hermes.botwatch` 확인 후 bootstrap 재등록

---

## 2026-06-23 bio_memory_engine.py SRP 분할 + Dreaming 결함 #5 수정 + PID 충돌 자동복구 + 메모리 포화도 자동 압축 + harness_verifier 경로 버그 수정

**프로젝트**: `/Users/bluesea/Applications/Mjauto/Scripts`

| 파일 | 변경 내용 |
|---|---|
| `modules/bio_memory_engine.py` | SRP 분할: 955줄 → 627줄. `TurboVecLight`, `VectorIndexManager`, `get_vector_backend`, `set_vector_backend`, `init_vector_index`, `_auto_bind_vim` → `vector_engine.py`로 이동. `ImportanceScorer`, `ForgettingCurve` 중복 정의 제거 (deriver_layer.py에서 import). 상단에 `from modules.vector_engine import ...` + `from modules.deriver_layer import ImportanceScorer, ForgettingCurve` 추가. |
| `modules/vector_engine.py` | **신규** — bio_memory_engine.py에서 분리된 벡터 인덱스 계층. `TurboVecLight`, `VectorIndexManager`, `get_vector_backend`, `set_vector_backend`, `init_vector_index`, `_auto_bind_vim` 포함. |
| `modules/dreaming_v2.py` | Dreaming 결함 #5(PEMS 고착화) 수정. `random.random()` 기반 PEMS 제거 → 실제 미처리 이벤트 수 체크 + 1시간 최소 간격 체크로 교체. `_last_distill_time = 0` 인스턴스 변수 도입 (`pems_threshold` 제거). 검증 게이트 `0.85 > 0.80` → `len(distilled_knowledge.strip()) > 20` 교체. `_append_to_hot_md` 경로: `~/Applications/Mjobsidian/hot.md` → `~/Applications/Mjobsidian/wiki/00_Meta/01_hot.md` 수정. |
| `hermes_local.py` | PID 충돌 자동복구 추가: Conflict 에러 감지 시 `launchctl kickstart -k gui/501/com.hermes.bot` 자동 실행. |
| `handlers/_base.py` | 메모리 포화도 자동 압축 추가: `add_to_history()`에서 포화도 체크 후 자동 `compact_and_save()` 실행. |
| `modules/harness_verifier.py` | 경로 버그 수정: `02_스크립트 정보.md` (공백) → `02_스크립트_정보.md` (언더스코어). |

**연결 구조**:
- `bio_memory_engine.py` ← import → `vector_engine.py` (벡터 계층)
- `bio_memory_engine.py` ← import → `deriver_layer.py` (`ImportanceScorer`, `ForgettingCurve`)
- `dreaming_v2.py` → `01_hot.md` (경로 수정 완료)
- `handlers/_base.py` → `history_manager.compact_and_save()`

**에러 진단 포인트**:
- `vector_engine.py` import 실패 → `modules/vector_engine.py` 존재 여부 확인
- Dreaming 조기 종료 → `_last_distill_time` 초기화 여부 확인 (0 = 1시간 간격 체크 통과)
- PID 충돌 자동복구 미동작 → `hermes_local.py` 내 Conflict 에러 문자열 패턴 확인
- `harness_verifier.py` 파일 없음 오류 → `02_스크립트_정보.md` (언더스코어) 경로 확인

---

## 2026-06-23 fswatch-indexer.sh — wiki_auto_stamper 상시 연동

**프로젝트**: `/Users/bluesea/Applications/Mjauto/Scripts`

| 파일 | 변경 내용 |
|---|---|
| `fswatch-indexer.sh` | `WIKI_STAMPER` 변수 추가. `.md` 파일 감지 시 `wiki_auto_stamper.py` 자동 호출. 감시 범위 `wiki/00_Meta` 등 5폴더 → `wiki/` 전체로 확장. 제외 패턴 3개 추가 (`.obsidian/`, `.smart-env/`, `99_Archive/`). launchd 재시작 완료 (PID 75611). |

**효과**: Hermes·Claude Code가 wiki에 .md 파일 생성·수정 시 → 태그 최대8개·링크제거·타임스탬프 자동 적용. digest 때만 되던 규칙이 **상시 적용**으로 전환.

**에러 진단 포인트**: stamper 적용 여부 → `fswatch-indexer.log` 확인. `[AUTO-STAMP] ✅` 로그가 없으면 stamper 미적용.

---

## 2026-06-23 Lessons Learned 시스템 신설 — 01_hot.md + CLAUDE.md

**프로젝트**: `/Users/bluesea/Applications/Mjobsidian/wiki/00_Meta`

| 파일 | 변경 내용 |
|---|---|
| `01_hot.md` | `## 💡 Lessons Learned` 섹션 신설. 실시간 상태 바로 아래 배치. 기존 산발적 교훈 6개 소급 정리하여 테이블 통합. |
| `CLAUDE.md` | 메타 7종 트리거에 Lessons Learned 추가. 라우팅 규칙 신설 (06번 보고서 vs 01_hot.md 구분 기준). |

**설계**: 06번(버그 공식 분석) + 01_hot.md Lessons Learned(비버그 패턴·판단근거) — 상충 없음, 계층이 다른 보완 구조.

---

## 2026-06-23 MJstock screener 개선 — signal_tracker / batch_fill_returns / future_screeners

**프로젝트**: `/Users/bluesea/Applications/Mjstock/screener`

### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `screener/signal_tracker.py` | `SIGNAL_LOG_FIELDS`에 `score_delta` / `reversal_early` 컬럼 추가. `record_scan()` 파라미터에 `score_delta_map: dict = None` 추가. 루프 내 `reversal_early` = signals에 "REVERSAL_EARLY" 포함 여부 자동 판별 |
| `screener/batch_fill_returns.py` | `_days_elapsed()` → `_trading_days_elapsed()` 교체. `pd.bdate_range` 기반 영업일 계산으로 수익률 채움 조건 정확도 향상. 3개 호출 지점 전부 교체 |
| `future_screeners/` (신규 폴더) | 삼돌이/농사단타 미적용 검색기 4개 파일 보관: `screen_samdoli_kr.py`, `screen_nongsa_danta_kr.py`, `screen_nongsa_danta_us.py`, `samdoli_analysis_for_ai.md` |
| `docs/quant_logic_analysis.html` | 퀀트 컬럼 표 20개 → 22개로 업데이트 (score_delta / reversal_early 행 추가). batch_fill_returns 설명 캘린더일 → 거래일 기준으로 수정 |
| `docs/signals_entry_points.html` | signal_log.csv 신규 컬럼(score_delta / reversal_early) 설명 박스 추가 |

### 에러 진단 포인트
- `reversal_early` 항상 False → `signals` 타입이 `list[str]`인지 확인. `"REVERSAL_EARLY"` 정확 일치 필요
- `_trading_days_elapsed()` 오류 → `scan_dt_str`이 `None`이면 `fromisoformat` 실패. `row.get("scan_datetime", "")` 빈 문자열 폴백 적용됨
- `score_delta` 빈 값 → 직전 스캔 데이터 없을 때 정상. 스캔이 쌓이면 자동 채워짐

---

## 2026-06-23 Hermes1 버그 수정 3종 — scan_single / history_manager / permission_bridge

**프로젝트**: `/Users/bluesea/Applications/MJauto/Scripts` + `/Users/bluesea/Applications/Mjstock/screener`

### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `screener/scan_single.py` | `.env` 로딩 순서 수정 (screener/.env 먼저 → Mjstock/.env 폴백). `get_exchange_code()` → `resolve_ticker_universe()` 교체 (CSV 기반 자동 판별). 빈 DataFrame 감지 후 NAS/NYS/AMS 순차 재시도 로직 추가. 모든 거래소 실패 시 RuntimeError 출력 |
| `modules/history_manager.py` | `COMPACT_THRESHOLD`: 30 → 60 (컨텍스트 포화 주기 2배 연장). `KEEP_RECENT`: 10 → 15. `add_message()` 파일 저장: 최근 20개 → 10개 (시작 시 초기 포화 방지) |
| `modules/permission_bridge.py` | `RUN_CMD`를 `EXTERNAL_TOOLS`에서 `INTERNAL_TOOLS`로 이동 (터미널 명령어 자동 승인). 헤르메스 봇 재시작 (PID 71365) |

### 버그 수정 상세

| 버그 | 파일 | 증상 | 원인 | 수정 |
|---|---|---|---|---|
| 거래소 오판별 KeyError | `scan_single.py` | AAOI 등 nasdaq_full.csv 종목이 `데이터 조회 실패: 'close'` KeyError | `get_exchange_code()`가 NYS로 잘못 반환 → KIS API에 NYS로 조회 → 빈 DataFrame → close 컬럼 없음 | `resolve_ticker_universe()` + 빈 DataFrame 감지 후 거래소 순차 재시도 |
| 세션 시작 즉시 컨텍스트 포화 | `history_manager.py` | 봇 시작 직후부터 67% 포화 상태 | 파일에서 20개 메시지 로드 → COMPACT_THRESHOLD 30 기준 이미 포화 | 파일 저장 최대 10개 + COMPACT_THRESHOLD 60으로 상향 |
| bash 실행 권한 타임아웃 | `permission_bridge.py` | 터미널 명령어 실행 시 인라인 버튼 승인 필요 → 120초 타임아웃 후 "권한 없음" | `RUN_CMD`가 EXTERNAL_TOOLS(수동 승인 필요)에 분류됨 | `INTERNAL_TOOLS`로 이동 (MJ 개인 봇이므로 자동 승인 적절) |

### 에러 진단 포인트
- `scan_single.py` 거래소 재시도 실패 시: `RuntimeError("모든 거래소(NAS/NYS/AMS) 시도 실패 — ticker 확인 필요")` 출력됨
- `history_manager.py` 포화 경고: COMPACT_THRESHOLD=60 기준 메시지 수 모니터링
- `permission_bridge.py` 자동 승인 범위: INTERNAL_TOOLS에 RUN_CMD 포함됨 — 민감 명령어 보안 검토 시 이 목록 확인

---

## 2026-06-23 MJstock 스캔 유니버스 전면 확장 — 4개 유니버스 × 모닝/장중 분리

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `auto_scan_morning.py` | US: nasdaq100→나스닥500 + russell1000→러셀500 각각 별도 실행. KR: kospi200→코스피500 + kosdaq150→코스닥500 각각 별도 실행. 총 4유니버스 × 5검색기 = 20스캔 / 07:00 KST |
| `auto_scan_intraday.py` | US(22:30): 나스닥500 + 러셀500 각각 3검색기. KR(09:10): 코스피500 + 코스닥500 각각 3검색기. 타임아웃 300s→600s. 총 12스캔 |

### 전체 스캔 구성 (확정)

| 시간 (KST) | 유니버스 | 종목 수 | 검색기 | 텔레그램 레이블 |
|---|---|---|---|---|
| 07:00 | 나스닥 | 500 | US 5개 | 미국(나스닥500) |
| 07:00 | 러셀1000 | 500 | US 5개 | 미국(러셀500) |
| 07:00 | 코스피 | 500 | KR 5개 | 한국(코스피500) |
| 07:00 | 코스닥 | 500 | KR 5개 | 한국(코스닥500) |
| 22:30 | 나스닥 | 500 | US 3개 | 미국장중(나스닥) |
| 22:30 | 러셀1000 | 500 | US 3개 | 미국장중(러셀) |
| 09:10 | 코스피 | 500 | KR 3개 | 한국장중(코스피) |
| 09:10 | 코스닥 | 500 | KR 3개 | 한국장중(코스닥) |

### 에러 진단 포인트
- `run_screener()` 타임아웃: 600초. 500종목 × 다중 지표 계산 시 초과 가능 → top_n 줄이거나 타임아웃 늘릴 것
- russell1000 universe: `get_us_universe_tickers("russell1000", 500)` 호출. data_loader.py에서 지원 여부 확인 필요
- KR 09:10 crontab: `auto_scan_intraday.py --market kr` — 코드 내 early return 제거됨, 실제 스캔 실행됨

---

## 2026-06-23 MJstock 시간대별 자동 스캔 + 퀀트 DB 확장 + 차트 외부 접속 + HTML 문서 초보자 설명

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `auto_scan_morning.py` | 미국 morning 스캔(07:00 KST)만 활성화. KR 블록 주석 처리, `kr_results = {}` placeholder. subprocess에 `--scan-type morning` 인자 추가 |
| `auto_scan_intraday.py` | 미국 intraday 스캔(22:30 KST)만 활성화. KR: `if args.market == "kr": return 0` 즉시 종료. universe 하드코딩 "nasdaq100". subprocess에 `--scan-type intraday` 인자 추가 |
| `screener/run_scan.py` | `compute_quant_snapshot()` — `ret_30d/60d/90d` 컬럼 추가. `compute_intraday_signal_snapshot()` 신규 함수 (7컬럼, US/KR 자동 구분). `--scan-type` CLI 인자 추가. `run_one()` — `scan_type` 파라미터 추가, matched 딕셔너리에 자동 기록 |
| `screener/batch_fill_returns.py` | `for days in [5, 10, 20]` → `[5, 10, 20, 30, 60, 90]` 확장. 종목별 1회만 현재가 호출(`cur_price = None` 패턴)으로 API 최적화 |
| `handlers/_stock.py` | `_handle_mjstock_chart()` — 로컬 IP URL 방식 → `bot.send_document()` HTML 파일 첨부 방식. 외부망에서도 차트 접근 가능 |
| `docs/nongsa_danta_guide.html` | 농부 비유 설명박스 + SVG 4구간 흐름 차트 + STEP 1/2/3 카드 + 한줄 요약 (초보자 설명 신규) |
| `docs/samdoli_guide.html` | 시소 비유 + SVG 삼돌이 3타입 시각화 + 3가지 타입 카드 (초보자 설명 신규) |
| `docs/nongsa_signal_board.html` | 4단계 흐름 그리드 + ON/OFF 신호등 설명 (초보자 설명 신규) |
| `docs/korean_original_formulas.html` | 검색기 비유 + 6개 전략 그리드 + AND/OR 논리 설명 (초보자 설명 신규) |
| `docs/kr_to_us_conversion.html` | 한국-미국 비교 SVG 테이블 + 핵심 요약 배너 (초보자 설명 신규) |

### crontab 등록

| 스케줄 | 작업 |
|---|---|
| `0 7 * * 1-5` | morning US 스캔 (`auto_scan_morning.py`) |
| `30 22 * * 1-5` | intraday US 스캔 (`auto_scan_intraday.py`) |
| KR intraday | 주석 처리 (비활성) |

### compute_intraday_signal_snapshot() 신규 함수 — 7개 컬럼

| 컬럼 | 설명 |
|---|---|
| `intra_vwma78_above` | 5분봉 종가 > VWMA78 (KR: 66봉) |
| `intra_vwma390_above` | 5분봉 종가 > VWMA390 (KR: 330봉) |
| `intra_pink_above` | 핑크선 위 여부 |
| `intra_fifty_bok` | 50억봉 ($5M US / 5B KRW) 출현 여부 |
| `intra_fifty_bok_bars` | 50억봉 출현 횟수 |
| `intra_star_count` | BB 33,3.5σ 상단 돌파 횟수 (★ 시그널) |
| `intra_obv_bull` | OBV 강세 여부 |

### 에러 진단 포인트

- `compute_intraday_signal_snapshot()` 컬럼 누락 → `is_kr` 파라미터 전달 여부 확인 (KR: n_yellow=66/n_sky=330, US: 78/390)
- `batch_fill_returns.py` 30/60/90d 수익률 미채움 → `cur_price = None` 패턴 및 API 호출 로직 확인
- `_handle_mjstock_chart()` 파일 첨부 실패 → HTML 파일 경로 존재 여부 + `bot.send_document()` 파라미터 확인
- `--scan-type` 인자 미인식 → `run_scan.py` argparse `--scan-type` 등록 여부 확인

---

## 2026-06-23 MJstock HTML 문서 업데이트 — HTS 이미지 독해 + 원본 전략식 반영 + 초보자 섹션 신규

**프로젝트**: `/Users/bluesea/Applications/Mjstock/docs`

### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `docs/korean_original_formulas.html` | 5개 전략 섹션(세력포착/단타의신/주도주단기농사/세력주농사/우량주농사)에 "HTS 원본 전략식 (사진 직접 독해)" div 블록 추가. 기존 Python 변환 구현 로직 유지 |
| `docs/kr_to_us_conversion.html` | 5개 전략 섹션에 "HTS 원본 전략식 (사진 직접 독해)" pre 블록 추가. 기존 KR→US 변환 테이블 유지 |
| `docs/signals_entry_points.html` | 목차 다음, 섹션 1 전에 "초보자 빠른 이해" 섹션 신규 추가. 농사단타 3단계 흐름(검색기→추돌이→5분봉타점), SVG 5분봉 타점 차트, 50억봉/별1,2,3 설명 카드, 3번째별 규칙 테이블, 손절/익절 카드 포함 |
| `docs/quant_logic_analysis.html` | "초보자 빠른 이해" 섹션 신규 추가 (섹션 1 철학 전). 전체 흐름 SVG(검색기→시그널→점수→퀀트DB→분석), S/A/B/PASS 등급 카드, 퀀트DB 역할 설명 |

### HTS 이미지 독해 작업

- `/tmp/screener_imgs/` HEIC→JPEG 변환된 5종 검색기 HTS 사진 직접 독해
- 주도주단기농사 전략식 추출 완료: 25개 알파벳 조건 (AND/OR 논리 구조)
- 5개 전략 모두 원본 HTS 검색식 텍스트 형태로 문서 수록

### 에러 진단 포인트

- `korean_original_formulas.html` 원본 전략식 섹션 미표시 → div 블록 class 확인
- `signals_entry_points.html` 초보자 섹션 레이아웃 깨짐 → SVG viewBox 속성 확인
- `quant_logic_analysis.html` 등급 카드 미렌더링 → CSS 인라인 스타일 확인

---

## 2026-06-23 MJstock 검색기 개선 — 인지편향 프롬프트 + EMA 버그 수정 + 우량주농사 강화

**프로젝트**: `/Users/bluesea/Applications/Mjstock/screener`

### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `screener/ticker_analyze.py` | `format_analysis_message()` 끝에 "인지편향 체크 → Claude에게 물어보기" 섹션 자동 첨부. 분석 결과 텔레그램 메시지 하단에 Claude 프롬프트 템플릿 삽입 |
| `screener/screen_selyeok_pochak.py` | BUG FIX: `c_ema` 조건 `EMA_9 > EMA_5`(하락 배열) → `EMA_5 > EMA_9`(상승 배열) 수정. PDF 원본(지수5이평>지수9이평) 기준 복원 |
| `screener/screen_uryangju_nongsaju.py` | `prepare_daily()` EMA45 추가(`add_ema([1,45,200])`). `prepare_30min()` Ichimoku(9,26,52) 추가. K 조건 `EMA_1>=EMA_200` → `EMA_1>=EMA_45`(PDF 원본 복원). J 조건 신규(30분봉 close >= Ichimoku SenkouB). `full_pass`에 J 조건 포함 |
| `docs/nongsa_danta_guide.html` | v1.0 → v2.0. 3단계 프로세스 섹션 추가, 추돌이 설명 추가 (이전 세션) |
| `docs/nongsa_signal_board.html` | 3단계 프로세스 panel 추가, 5일신고가/50억봉 "신이 주신 기회" 설명 추가 (이전 세션) |

### 에러 진단 포인트

- `ticker_analyze.py` Claude 프롬프트 미출력 → `format_analysis_message()` 끝 섹션 누락 여부 확인
- `screen_selyeok_pochak.py` 여전히 하락 배열 통과 시 → `c_ema` 조건 `EMA_5 > EMA_9` 방향 재확인
- `screen_uryangju_nongsaju.py` J 조건 미동작 → `prepare_30min()` Ichimoku 컬럼 생성 여부 + `full_pass` J 조건 포함 확인
- Ichimoku SenkouB 컬럼명 불일치 → `df.columns` 출력으로 실제 컬럼명 대조

---

## 2026-06-23 Hermes 메모리 개선 + MJstock 티커 분析기

**프로젝트**: `Mjauto/Scripts/modules` + `Mjstock/screener`

### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `modules/memory_schema.py` | 신규. EpisodicEntry dataclass (저장 기준 내장), SemanticMemory 카테고리 dict, classify_category() 키워드 테이블 |
| `modules/memory_consolidator_v2.py` | 신규. L2→L3 증류 파이프라인. `~/.hermes/memory/` 경로 기반. 단방향 7단계 파이프라인. `--dry-run`/`--health` 지원 |
| `modules/context_assembler_v2.py` | 신규. 기존 assemble_context() 동일 인터페이스, 7블록→3~7블록. harness_agent.py 미전환 상태 |
| `screener/ticker_analyze.py` | 신규. `/mjstock {티커}` 전체 검색기 동시 분析. SCREENER_META 9종 US + 9종 KR. KIS API 1회 다운로드 재사용 |
| crontab | `0 4 * * *` memory_consolidator_v2 자동 실행 추가 |

### 에러 진단 포인트

- memory_consolidator_v2 실패 → `logs/consolidation.log` 확인
- L2 경로 오류 시 → `~/.hermes/memory/episodic_memory.json` 존재 여부 확인
- ticker_analyze.py KIS 오류 → `KIS_APP_KEY` env 확인 (`screener/.env` 또는 `~/.hermes/.env`)
- context_assembler_v2 미적용 상태 → harness_agent.py import 줄 확인 (아직 v1)

---

## 2026-06-23 MJstock 스캔 시스템 개편 v2.0

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `auto_scan_morning.py` | 신규. 아침 일봉 스캔 전용 (KST 07:00). US/KR 5종씩 (uryangju/judoju/danta/selyeok/chowuryang) |
| `auto_scan_intraday.py` | 신규. 장중 스캔 전용. `--market us` (22:30) / `--market kr` (09:10). pochak/shooting/chuddoli |
| `screener/send_scan_result.py` | 수정. 텔레그램 로컬 IP 링크 제거 → `sendDocument` API로 HTML 파일 직접 첨부. `get_chart_paths()` 함수 신규 |
| `auto_scan_nasdaq500.py` | `.bak` 백업 (비활성화) |
| crontab | 기존 auto_scan_nasdaq500 줄 제거. 아침·미국장중·한국장중 3개 신규 등록 |
| `docs/*.html` (5개) | Jun 18 → Jun 21 버전으로 교체 (사용설명서/signals_entry_points/quant_logic_analysis/korean_original_formulas/kr_to_us_conversion) |

### 핵심 변경 사유

| 문제 | 해결 |
|---|---|
| 텔레그램 차트 링크 `192.168.x.x:8765` → LTE에서 안 열림 | `sendDocument` API로 HTML 파일 직접 전송. 네트워크 무관 |
| 장중 전용 검색기(pochak/shooting/chuddoli)를 아침에 실행 → 항상 0 | 아침/장중 스캔 파일 분리 |
| cron 타이밍 `0 10 UTC` = KST 19시 = 미국장 4시간 전 | 실제 장 시작 시간에 맞게 수정 |

### crontab 최종 상태 (변경분)

```
# 제거: 0 10 * * 1-5 auto_scan_nasdaq500.py
# 추가:
0 7   * * 1-5  auto_scan_morning.py    >> logs/scan_morning.log
30 22 * * 1-5  auto_scan_intraday.py --market us >> logs/scan_intraday_us.log
10 9  * * 1-5  auto_scan_intraday.py --market kr >> logs/scan_intraday_kr.log
```

### 에러 진단 포인트

- 아침 스캔 실패 → `logs/scan_morning.log` 확인
- 장중 미국 스캔 실패 → `logs/scan_intraday_us.log`
- 장중 한국 스캔 실패 → `logs/scan_intraday_kr.log`
- 텔레그램 파일 전송 실패 → `send_scan_result.py`의 `send_document()` 함수, `sendDocument` API 응답 코드 확인

---

### ✅ 2026-06-18 | Claude Code | **MJstock — 텔레그램 /mjstock 단일 종목 온디맨드 분석 (v2.5)**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`, `/Users/bluesea/Applications/Mjauto/Scripts`

| 파일 | 변경 내용 |
|---|---|
| `Mjstock/screener/scan_single.py` | (신규) 단일 종목 × 검색식 분석 스크립트. KISClient로 일봉/분봉 데이터 조회 → 검색식 통과 여부·점수 계산 → `save_farming_chart()`로 차트 HTML 즉시 생성. JSON stdout 출력 |
| `Mjauto/Scripts/handlers/_stock.py` | `cmd_mjstock` 추가 — `/mjstock TICKER` 입력 시 8개 검색식 인라인 버튼 팝업. `callback_mjstock` 추가 — 버튼 탭 시 `scan_single.py` subprocess 실행 → 점수+조건표+차트URL 회신 |
| `Mjauto/Scripts/handlers/_callbacks.py` | `mjstock:` 접두사 콜백 라우팅 추가 |
| `Mjauto/Scripts/hermes_local.py` | `cmd_mjstock` CommandHandler 등록 |

**흐름**: `/mjstock RDW` → 검색식 버튼 → `scan_single.py` 실행 (30초) → 점수+차트URL → URL 탭 → 모바일 대시보드에서 차트 즉시 열림 (시간 제한 없음)

**에러 진단 포인트**:
- `scan_single.py` 실패 시: `KIS_APP_KEY`/`KIS_APP_SECRET` 환경변수 확인, 검색식 키 오타 확인
- 차트 URL 404 시: `charts/SCREENER/TICKER_farming_*.html` 파일 존재 여부 확인 (scan_single이 생성)

---

### ✅ 2026-06-18 | Claude Code | **MJstock — 단일 텔레그램 발송 + 버그 수정 완료 (v2.4 최종)**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

| 파일 | 변경 내용 |
|---|---|
| `screener/send_scan_result.py` | 개별 메시지 발송 제거 → 전체 통과 종목 집계 후 ONE 요약 메시지 + 대시보드 URL(`http://[IP]:8765`) 발송. `sent = 0` 초기화 버그 수정 |
| `app.py` | KR 검색식 키 오류 수정 (`kr_X` → `X_kr`), `st.switch_tab` AttributeError 제거 |
| `docs/signals_entry_points.html` | §12 추가 — 스캔 결과 해석법 (★ 유무 판단 기준, 버튼 아이콘 기준표, 3단계 흐름) |
| `docs/quant_logic_analysis.html` | §9 추가 — 스캔 결과 해석 원칙 (A/B/C/D 등급표, 시그널 신선도 기준표) |

**에러 진단 포인트**:
- 텔레그램 URL이 안 오면 `send_scan_result.py`의 `sent` 집계 확인, `TELEGRAM_BOT_TOKEN`/`CHAT_ID` 환경변수 확인
- KR 검색식 결과 누락 시 폴더명이 `X_kr` 접미사인지 확인

---

### ✅ 2026-06-18 | Claude Code | **MJstock — 헬스체크 + 설정 탭 + 사용설명서 업데이트 (v2.4 추가)**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

| 파일 | 변경 내용 |
|---|---|
| `health_check.py` | (신규) 일일 헬스체크 스크립트 — 오늘 스캔 실행 여부/로그 에러/수익률 채움/퀀트 데이터 누적 현황 체크. 평일 18:00 crontab 자동 실행 등록. 결과를 텔레그램 일일 리포트로 발송 |
| `app.py` | `tabs[5]` "설정" 탭 신규 추가 — 텔레그램 봇 토큰/채팅 ID 입력·저장, "내 채팅 ID 자동 조회" (getUpdates API), "테스트 메시지 발송" 버튼, 현재 연동 상태 요약 (텔레그램 봇/채팅ID/KIS API 3개 metric). 가족 배포용 — USB로 받은 가족이 자기 텔레그램 계정으로 알림 받을 수 있도록 설계 |
| `docs/MJstock_사용설명서.html` | 설정 탭 사용법 섹션(텔레그램 연동 설정) + 모바일 대시보드 접속 방법 섹션 추가 |

**에러 진단 포인트**:
- `health_check.py`: crontab 미실행 시 `crontab -l`로 등록 확인, 텔레그램 미발송 시 `TELEGRAM_TOKEN`/`CHAT_ID` 환경변수 확인
- `app.py` 설정 탭: 토큰 저장 경로 `~/.mjstock/telegram_config.json`, getUpdates API 실패 시 봇 토큰 유효 여부 확인

---

### ✅ 2026-06-18 | Claude Code | **MJstock — 더블 발송 버그 수정 + 모바일 대시보드 (v2.4)**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

| 파일 | 변경 내용 |
|---|---|
| `screener/run_scan.py` | `--no-telegram` 플래그 추가 — 외부 호출 시 텔레그램 자동 발송 억제 |
| `auto_scan_nasdaq500.py` | 서브프로세스 cmd에 `--no-telegram` 삽입 — 개별 스캔 8건 중복 발송 차단, 요약 1건만 발송 |
| `mobile_dashboard.py` | (신규) 모바일 웹 대시보드 — Python 내장 http.server 기반, 포트 8765, 같은 WiFi에서 폰 접근 가능 |

**버그 원인 (더블 발송)**:
- `auto_scan_nasdaq500.py`가 `run_scan.py` × 8회 subprocess 호출
- `run_scan.py`가 각 스캔 완료 시 텔레그램 1건 발송 (= 8건)
- `auto_scan_nasdaq500.py`가 요약 1건 추가 발송 → 총 9건 발송
- **수정**: `run_scan.py`에 `--no-telegram` 플래그, `auto_scan_nasdaq500.py` 호출 시 삽입 → 요약 1건만 발송

**모바일 대시보드**:
- 실행: `python3 mobile_dashboard.py`
- 접속: `http://[Mac IP]:8765` (같은 WiFi 폰 브라우저)
- 기능: 검색식별 최신 스캔 결과 카드, 상위 10종목, 점수바, 5분 자동 새로고침
- 추가 패키지 불필요 (Python 내장 http.server)

---

### ✅ 2026-06-18 | Claude Code | **MJstock — UI/UX 통합 + 나스닥 500 자동화 (v2.3)**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

| 파일 | 변경 내용 |
|---|---|
| `app.py` | "스캐너 실행" 탭: 스캔 완료 후 타임스탐프 표시 + 자동으로 "차트 보기" 탭 전환 |
| `app.py` | "차트 보기" 탭: 드롭다운 아래에 검색식별 최근 스캔 시각 표시 |
| `auto_scan_nasdaq500.py` | (신규) 나스닥 500 × 8개 검색식 자동 스캔 스크립트 (텔레그램 결과 발송 포함) |
| `Scripts/handlers/_stock.py` | `/mjscan` 명령어 신설 — 텔레그램에서 수동 또는 자동화된 나스닥 스캔 실행 |
| `crontab` | 매일 밤 11시(KST) = 미국동부 오전 10시에 `auto_scan_nasdaq500.py` 자동 실행 등록 |

**워크플로우**:
```
"스캐너 실행" 탭
├─ 검색식 선택 + 스캔 실행
├─ ✅ 통과 종목 N개 + 🕐 시각 표시
└─ 자동으로 "차트 보기" 탭 전환 (선택할 필요 없음)

"차트 보기" 탭
└─ 드롭다운에서 검색식 선택 → 🕐 최근 스캔 시각 함께 표시
```

**나스닥 500 자동화**:
- `crontab`에 매일 밤 11시 등록 → 8개 검색식(우량주/세력주/단타의신 등) 자동 스캔
- 결과 로그: `/Users/bluesea/Applications/Mjstock/results/auto_scan.log`
- 텔레그램 발송: `/mjscan` 명령으로도 수동 실행 가능

**데이터 축적 계획** (1개월):
- 일일 스캔 결과 → `scan_*.csv` 누적
- 5/10/20일 수익률 채움 (`batch_fill_returns.py` 자동)
- 한달 후: 검색식별 승률 + 진입 신호 품질 분석 → 로직 개선

---

### ✅ 2026-06-18 | Claude Code | **MJstock — 스캔 마커 전 검색식 확대 + 신호없음 이유 주석 (v2.2)**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

| 파일 | 변경 내용 |
|---|---|
| `screener/chart.py` | `_apply_scan_marker()` 공통 유틸 추출. `make_chart()` + `make_chowuryang_chart()` + `save_intraday_chart()`에 `scan_marker` 파라미터 추가 — 전 검색식 차트에 금색 수직선·진입가·손절가 표시 |
| `screener/chart.py` | `_no_signal_reason(df_sig)` 함수 신설 — 최근 봉 기준 BUY_URYANGJU/BUY_SELYEOK/NONGSSA 미충족 조건 분석. 최근 5봉 신호 없을 때 차트 좌상단에 "⚠️ 최근 매수★ 없음 — 미충족 조건" 주석 자동 표시 |
| `screener/run_scan.py` | 초우량주 차트에 scan_marker 전달 추가. SEPA/5분봉 차트도 scan_marker 전달 |

**신호없음 이유 출력 예시**:
```
[ 현재 종가 위치 ]
  C2(노란선): -0.9%  구름대S2: +0.1%  GG세력선: -8.7%
🔴 우량★ 미발생:
  종가(135.34) ≤ C2(136.58) [-0.9% 미달]
🟡 농싸★ 미발생:
  종가(135.34) ≤ C2(136.58) [C2 아래 = 추세 이탈]
```

**에러 진단 포인트**: 주석이 차트에 안 보이면 최근 5봉 중 하나라도 신호가 있는 것 — 정상 동작. `_no_signal_reason()` 오류는 silent catch라 스캔 중단 없음.

---

### ✅ 2026-06-18 | Claude Code | **MJstock — 십자선(Crosshair) + 매수신호 AND 조건 정렬 + 문서 전체 업데이트**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

#### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `screener/chart.py` | 4개 `update_layout()` 전부에 `hovermode="x unified"` + `showspikes=True` 추가 — 마우스 이동 시 날짜·가격 십자선 표시 (SEPA/초우량주/Farming/5분봉 차트 모두 적용) |
| `screener/run_scan.py` | `compute_buy_signal_snapshot(df_daily)` 함수 신설 — 최근 5거래일 이내 BUY_URYANGJU/BUY_SELYEOK/NONGSSA 체크 → `has_buy_signal`, `buy_signal_type`, `buy_signal_days_ago` 3개 컬럼 추가. US/KR 스캔 모두 적용. 결과 DataFrame을 `has_buy_signal` 우선 + `score` 내림차순으로 정렬 후 반환 |
| `app.py` | 차트 탭 정렬: score 단독 → `has_buy_signal` 우선 + score 내림차순. `buy_signal_map` 구성 후 🟢 버튼 아이콘 + "🟢오늘/🟢N일전" 레이블. 신호 종목 수 안내 메시지 추가 |
| `docs/signals_entry_points.html` | 섹션 11 신규 추가 — "검색식 vs 시그널 AND 조건" (검색식/시그널 역할 분리, 검색식별 해석, AND 조건 정렬 기준 테이블, has_buy_signal 컬럼 설명) |
| `docs/quant_logic_analysis.html` | 17개 → 20개 퀀트 컬럼으로 업데이트 (has_buy_signal, buy_signal_type, buy_signal_days_ago 3개 추가) |
| `docs/MJstock_사용설명서.html` | 6단계 차트 확인 설명에 🟢 버튼 형식 + 십자선 기능 안내 추가 |

#### 핵심 설계 결정
- 검색식 통과(점수) ≠ 차트 매수★ — 두 시스템은 독립적
- AND 조건 = 신호 있는 종목 먼저, 그 안에서 score 높은 순 정렬
- 신호 유효 기간 = 5거래일 이내 (0일=오늘이 가장 강력)

#### 에러 진단 포인트
- `has_buy_signal` 항상 False: `compute_farming_signals()`가 필요한 컬럼(C2_VWMA50, GG_LINE 등) 반환하는지 확인
- 🟢 버튼이 안 보임: 기존 스캔 CSV에는 컬럼 없음 — 재스캔 필요
- 차트 십자선 미작동: Plotly 버전 5.x 이상 필요, `pip show plotly`로 확인

---

### ✅ 2026-06-18 | Claude Code | **메타인지(Faithful Uncertainty) 규칙 추가 — harness_agent.py 시스템 프롬프트**
- **배경**: Google 논문 "Hallucinations Undermine Trust; Metacognition is a Way Forward" 기반. AI가 자신의 지식 한계를 인식하고 명시적으로 표현하면 "확신에 찬 오류(환각)" 감소. 에이전트의 [SEARCH] 판단 정확도 향상.
- **변경 파일**: `Scripts/harness_agent.py` — `_FIXED_SYS_PROMPT` Rule 12 신설, `_CAVEMAN_SYS_PROMPT` Rule 5 강화
- **적용 내용**: 사실·정보·분석 질문 시 3단계 접두어 사용 (일상 대화·명령 수행엔 미적용)
  - `[확실]` — 학습 데이터로 검증된 정보, [SEARCH] 없이 직접 답변
  - `[추론]` — 논리 추론 기반, 필요 시 [SEARCH] 제안
  - `[불확실]` — 자신 없음, 반드시 [SEARCH] 후 답변
- **기대 효과**: 불필요한 [SEARCH] 호출 감소(응답 속도↑) + 잘못된 확신 답변 감소(신뢰도↑). 기존 `memory_refinement.py` 아첨 필터와 시너지
- **에러 진단 포인트**: 봇이 모든 답변에 `[불확실]` 붙이면 과도하게 조심하는 것 → 프롬프트 범위 조정 필요. 일상 대화에 접두어 남용 시 동일하게 범위 재조정

### ✅ 2026-06-17 | Claude Code | **워치독 오탐 수정 — 하트비트 파일 기반 전환, 텔레그램 알람 제거**
- **문제**: 워치독이 로그 mtime 기준(10분)으로 체크하여 메시지가 없는 조용한 상태를 "행(hang)"으로 오판 → 정상 작동 중인 봇을 14분마다 불필요하게 재시작, 텔레그램에 🚨 알람 반복 발송
- **변경 파일 1** `hermes_local.py`: 폴링 루프(`while app.updater.running`) 안에 `_HEARTBEAT.touch()` 추가 — 15초마다 `~/.hermes/runtime/bot_heartbeat` 파일 갱신. 이 파일이 있으면 봇이 실제로 polling 루프를 돌고 있다는 증거
- **변경 파일 2** `check_bot_alive.sh`: 체크 기준을 로그 mtime → 하트비트 파일 mtime으로 교체, 임계값 10분→3분, 텔레그램 알람(`send_telegram_msg.py`) 완전 제거(내부 동작만), 알림 문자열에서 🚨 이모지 제거
- **에러 진단 포인트**: 봇이 재시작 없이 잘 돌다가 워치독이 트리거된다면 `~/.hermes/runtime/bot_heartbeat` mtime 확인 — 3분 이상 갱신 안 됐으면 진짜 행

### 🚨 2026-06-17 | Claude Code | **(최종 원인 정정) Hermes1 무응답 = ISP의 텔레그램 SNI 차단, 코드 문제 아님**
- 이전 두 건(워치독 도입, kill -9→SIGTERM 수정)을 진행하며 봇이 계속 재시작되길래 "크래시 루프"로 추정했으나, `hermes_local.py:483-485`의 `except Exception: pass`가 진짜 예외를 완전히 삼키고 있어 오판했음
- **수정**: 해당 줄을 `logger.error(f"[Polling 종료 원인] {type(e).__name__}: {e}")`로 변경 → 실제 원인이 `TimedOut`으로 드러남
- **네트워크 진단**: `curl https://www.google.com` → 200 (정상) / `curl https://api.telegram.org` → 000 (차단) / 일부 텔레그램 IP는 TCP 443 포트는 열리지만 TLS 핸드셰이크에서 멈춤 → ISP의 SNI 기반 선택적 차단으로 결론
- **현재 상태**: 봇 프로세스(`com.hermes.bot`)는 정상 대기 중, 네트워크 차단이 풀리면(VPN 연결 또는 ISP 차단 해제) 자동으로 정상화됨. 코드 측 추가 조치 불필요
- **에러 진단 포인트**: 향후 "텔레그램 안 됨" 신고 시 가장 먼저 `curl --max-time 6 https://api.telegram.org` 로 네트워크 차단 여부부터 확인할 것 — 프로세스 재시작 시도보다 우선

### ✅ 2026-06-17 | Claude Code | **Hermes1 봇 행(hang) 워치독 신설**
- **장애 진단**: 텔레그램 폴링 중 `httpx.ReadError`(네트워크 단절) 발생 후 PTB 내부 재시도 루프가 죽지도, 정상화되지도 않고 멈춤. `hermes_local.py:477-485`의 `while app.updater.running: sleep(15)` 구조상 행 상태에서는 예외가 안 던져져 `sys.exit(0)`까지 도달하지 못함 → launchd `KeepAlive`(프로세스 종료시만 재시작)가 무력화되어 3시간 30분간 무응답
- **신규 파일**: `Scripts/check_bot_alive.sh` — `hermes_launchd.log`/`.error.log` mtime 10분 이상 정지 + 프로세스 생존 시 `kill -9` 후 `launchctl kickstart -k gui/501/com.hermes.bot` 강제 재시작, 텔레그램 알림 발송
- **신규 LaunchAgent**: `~/Library/LaunchAgents/com.hermes.botwatch.plist` — `StartInterval=300`(5분마다), `RunAtLoad=true`. 로그: `~/.hermes/runtime/botwatch.log/.err`
- **에러 진단 포인트**: 워치독 자체가 안 도는지 확인할 때 `launchctl list | grep botwatch`, 동작 로그는 `~/.hermes/runtime/botwatch.log`
- **잔존 과제**: `hermes_local.py` 폴링 루프에 httpx `read_timeout`/`connect_timeout` 명시 + 하트비트 파일 기록 — 근본 원인(행 자체)을 줄이는 코드 수정, 잠금 정책상 신중 검토 후 별도 진행

### ✅ 2026-06-17 | Claude Code | **(후속) 워치독 kill -9 → SIGTERM 우선 방식으로 수정, 크래시 루프 해소**
- **장애**: 위 워치독이 `kill -9`로 강제종료 → 텔레그램 서버측 getUpdates long-poll 연결이 안 끊긴 채 잔류 → 5초 뒤 뜬 새 인스턴스가 `telegram.error.Conflict: terminated by other getUpdates request`로 즉시 죽음 → `ThrottleInterval=5`로 재시작 → 또 충돌. PID가 5~10초마다 계속 바뀌는 크래시 루프 발생, 실제 봇 무응답으로 MJ가 신고
- **복구**: `launchctl unload com.hermes.bot.plist` → `pkill -f Scripts/hermes_local.py` 완전 정지 → 60초 대기(텔레그램 서버측 연결 만료) → `launchctl load`로 1회 기동, Conflict 재발 없음 확인
- **변경 파일**: `Scripts/check_bot_alive.sh` — `kill -TERM` 후 최대 5초 대기, 그래도 살아있으면 `kill -9` fallback. `launchctl kickstart` 전 대기 2초→5초로 확대
- **에러 진단 포인트**: 향후 PID가 수 초 단위로 계속 바뀌면(`ps aux | grep hermes_local` 반복 확인) `hermes_launchd.error.log`에서 `Conflict` 키워드 grep — 잔류 연결 충돌 의심. 이 경우 워치독 재시작이 아니라 `unload`+`pkill`+대기 후 수동 `load` 필요

### ✅ 2026-06-11 | Claude Code | **legal-paper 스킬 규격 업데이트 — 실제 투고 양식 2종 + 학위논문 지침 반영**
- **변경 파일**: `~/.hermes/skills/legal-paper/SKILL.md`
- **추가 내용**:
  - 모드 A(학위논문): 국립한국해양대학교 대학원 학위논문 작성 지침(개정 2024.10.30) 전체 규격 내장 — 기본체계 순서, 편집 규격(여백/글자크기/줄간격), 목차 체계, 초록 형식, 참고문헌 형식, 인준지 인원, 제출 부수/방법
  - 모드 B(소논문): 한국해사법학회 「해사법연구」 투고 기준 — 실제 인용 형식(홍길동, "제목", 「해사법연구」 제○권...), 투고카드 필수 기재 사항, 투고 전 체크리스트
- **규격 원본**: `~/Downloads/논문투고양식.hwp`, `~/Downloads/한국해사법학회_논문_투고카드-1.hwp`, `~/Downloads/4. 국립한국해양대학교 대학원 학위논문 작성 지침.hwpx`
- **HWP 추출 방법**: HWPX는 unzip+hp:t 태그 파싱, HWP는 UTF-16-LE 스캔으로 텍스트 추출

### ✅ 2026-06-11 | Claude Code | **테스트 격리 + 비대화 감시 자동화 + 글쓰기 스킬 3종 신설**
- **변경 파일**:
  - `tests/test_bio_memory.py` — 6개→19개 테스트로 확장 (ImportanceScorer/ForgettingCurve/절차기억/save_important). `_make_engine()` 헬퍼로 L1/L2/L3 경로를 tmp로 격리
  - `check_file_sizes.sh` (신규) — 관제탑 500줄/일반 800줄 상한 감시, 초과 시 텔레그램 경고. `com.hermes.sizewatch.plist`로 매주 월 09:00 자동 실행
  - 스킬 3종 신설: `~/.hermes/skills/writing-pro` (글쓰기), `book-writing` (책 집필+정합성), `legal-paper` (법학논문, korean-law MCP 인용 검증 연동)
- **중대 발견 및 정리**: 기존 테스트가 운영 기억 파일(`~/.hermes/memory/episodic_memory.json` 등)을 직접 오염시키고 있었음 (에피소드 83건 중 70건이 테스트 산출물). 백업(`*.pre_cleanup`) 후 정리 — 상세는 06_오류보고서 참조
- **에러 진단 포인트**: sizewatch 로그는 `~/.hermes/runtime/sizewatch.log`. 테스트는 반드시 `_make_engine()` 경유 (직접 `BioMemoryEngine()` 생성 시 운영 파일 오염)

### ✅ 2026-06-11 | Claude Code | **Zombie Poller 근본 해결 — 봇 단일 인스턴스 자동 보장 (PID 교체 방식)**
- **변경 파일**: `hermes_local.py`
- **기존 방식**: `fcntl.flock` — launchd 즉시 재시작 시 타이밍 충돌 → 이중 기동 발생
- **신규 방식**: `_ensure_single_instance()` 함수 추가
  - `~/.hermes/hermes_local.pid` PID 파일 기록
  - 시작 시 기존 PID 존재 확인 → SIGTERM 전송 → 최대 5초 대기 → 자동 교체
  - 구 프로세스가 응답 없으면 SIGKILL로 강제 종료 후 신규 인스턴스 기동
  - atexit 등록: 종료 시 PID 파일 자동 삭제
- **효과**: launchd 재시작, 수동 킥스타트 어떤 상황에서도 항상 단일 인스턴스 유지. 출장 중 원격 무인 운영 가능
- **추가 수정**: `handlers/_meta.py` — `safe_reply` import 누락 수정 (`/claude_brief` 명령어 NameError 수정)
- **에러 진단 포인트**: `~/.hermes/hermes_local.pid` 확인 → 실행 중 PID와 일치하면 정상

### ✅ 2026-06-11 | Claude Code | **결함 #4 완전 해소 — safe_reply/safe_edit 전 핸들러 적용 + Hermes1 재시작**
- **변경 파일**: `handlers/_base.py`, `handlers/_stock.py` (이전 세션), 나머지 핸들러 14개 (이번 세션)
  - `_approval.py`, `_callbacks.py`, `_file.py`, `_grill.py`, `_kanban.py`, `_memory.py`, `_meta.py`, `_orchestrator.py`, `_paper.py`, `_research.py`, `_system.py`, `_system_ops.py`, `_ui.py`, `_vault.py`
- **적용 내용**: `update.message.reply_text()` → `safe_reply()`, `msg.edit_text()` → `safe_edit()` 일괄 전환
  - 총 **219곳** 안전 래퍼 적용 (결함 #4에서 집계된 137곳 + 추가 발견분)
  - `safe_reply`/`safe_edit` 함수: `parse_mode` 오류 시 `parse_mode=None`으로 자동 폴백 (BadRequest 크래시 방지)
- **봇 재시작**: `launchctl kickstart -k gui/.../com.hermes.bot` → PID 95296 (변경 활성화)
- **에러 진단 포인트**: `_`, `*`, `[` 포함 메시지 전송 시 BadRequest 대신 plain text로 자동 전환됨

### ✅ 2026-06-11 | Claude Code | **P2-④ semantic_index ↔ memory 검색 연동 완료**
- **변경 파일**: `modules/memory_refinement.py` — `hybrid_recall()` 3소스 통합
  - 기존: L2 에피소딕 + knowledge_indexer FTS5 (2소스)
  - 추가: `semantic_index.db` FTS5 MATCH (00_Meta 82개 위키 문서 전문검색) → **3소스 통합**
  - 검색 경로: `_SEMANTIC_DB`(`wiki/00_Meta/semantic_index.db`) → `fts_docs` 테이블 FTS5 OR 쿼리
- **검증**: `hybrid_recall('하네스 업그레이드 로드맵')` → 🗂 [시맨틱 인덱스] 섹션 정상 출력 확인
- **에러 진단 포인트**: `[MemRefine] semantic_index 검색 실패` 로그 → `_SEMANTIC_DB` 경로 또는 DB 스키마 확인

### ✅ 2026-06-11 | Claude Code | **_stock.py 잔여 28곳 safe wrapper 완전 적용**
- `handlers/_stock.py`: 기존 누락분 `update.message.reply_text` → `safe_reply`, `msg.edit_text` → `safe_edit` 전환
- 총 safe wrappers 36개 (handlers 전체: 255개)
- Hermes1 재시작: PID 95669

### ✅ 2026-06-11 | Claude Code | **하네스 업그레이드 로드맵 P1 전체 완료**
- **① L3 증류 자동화**: `auto_dream_trigger.py` 신규 생성 + `com.hermes.autodream.plist` launchd 주간(일 03:30)
  - L2 미처리 ≥ 20개 시 자동 Dreaming 실행. DeepSeek API 직접 호출(봇 스택 의존 없음)
  - `consolidator_state.json` 현재 83/83 (이전 세션에서 54건 처리 완료)
- **② 문서 표류 보정**: `claude_briefing.md` 갭 섹션 — 이미 해결된 3개 항목 ✅ 표시
- **③ Markdown 폴백**: `_base.py` safe_reply/safe_edit → 전 핸들러 219곳 적용 (이번 세션 완료)
- **⑤ 모듈 분할**: `ingest_engine.py` 642→459줄 (`ingest_text_utils.py`), `knowledge_indexer.py` 631→537줄 (`indexer_text_utils.py`)
- **신규 문서**: `하네스_업그레이드_로드맵_20260611.md` (P1~P3 전체 계획, 실행 현황 포함)

### ✅ 2026-06-10 | Claude Code | **Self-Harness 3단계 완성 — Proposal Validation + WeaknessMiner 전체 확장**
- **신규**: `modules/proposal_validator.py` — Self-Harness 논문 3단계 Proposal Validation 구현
  - `pre_check(target_file, desc)` → held-in(weakness DB 반복 실패) + held-out(정상 케이스) 체크리스트 자동 생성
  - `record_result(file, passed, detail)` → SQLite 이력 기록. 거부 시 `06_에러보고서.md` 자동 append
  - DB: `~/.hermes/runtime/proposal_validation.db`
  - 싱글턴: `get_validator()` 전역 사용
- **확장**: WeaknessMiner 핸들러 커버리지 6개 파일 14개 포인트 (`_vault`, `_file`, `_system`, `_grill`, `_research` 추가)
- **신규**: `wiki/00_Meta/hooks/wiki_autostage.py` — PostToolUse(Write|Edit) 시 wiki 경로 파일 자동 git 스테이징
- **업그레이드**: `wiki/00_Meta/hooks/stop_hook.py` v2 — mtime 30분 기반 변경 파일만 감지, 탐색세션 간단메시지
- **업그레이드**: `wiki/00_Meta/session_start_hook.sh` v2 — 컨텍스트 2591자→1297자 (50% 감소)
- **신규**: `Scripts/.git` — Scripts/ 독립 git 저장소 초기화 (201파일 초기커밋)
- **신규**: `~/.claude/agents/meta-updater.md`, `~/.claude/agents/hermes-debugger.md` — Layer 4 서브에이전트
- **신규**: `~/.claude/skills/mj-meta-update/SKILL.md`, `~/.claude/skills/mj-stock-analyze/SKILL.md` — Layer 2 스킬
- **수정**: `~/.claude/settings.json` — stock-scanner MCP 등록 (Layer 5 완성)
- **에러 진단**: `proposal_validator.py` DB 위치 `~/.hermes/runtime/`. weakness DB 없으면 held-in은 "최근 7일 반복 없음"으로 안전 처리

### ✅ 2026-06-10 | Claude Code | **주식 시스템 v1.4 — ADX 점수 + 4티어 스캔**
- **수정**: `modules/stock_signal_engine.py` — `score["adx_bonus"]` 추가 (ADX≥60:+0.5 / ADX≥40:+0.25). 최대 점수 8.5→9.0. `sepa_structure` 플래그 추가 (정배열+EMA우상향, 52W 무관)
- **수정**: `modules/stock_scanner.py` — 4티어 분류 (`buy_signals` / `watch_ready` / `strong_struct` / `sepa_list`). strong_struct = sepa_structure+ADX≥40 (SEPA 완전통과 아니어도 포함)
- **수정**: `handlers/_stock.py` — `/scan buy|watch|strong|sepa` 서브커맨드 추가. 기본 `/scan`은 4티어 요약
- **수정**: `handlers/_ui.py` — `/help` 주식 섹션 4티어 커맨드로 업데이트
- **배경**: RDW(ADX 71.8, SEPA 정배열이나 52W 미달) 같은 종목이 기존엔 어느 리스트에도 안 잡힘 → strong_struct 티어로 커버
- **에러 진단**: `sepa_structure`는 정배열+EMA우상향만. ADX 컬럼은 `cur.get("adx",0)`으로 안전 접근

### ✅ 2026-06-10 | Claude Code | **주식 시스템 v1.3 — 5가지 전략 개선**
- **수정**: `modules/stock_signal_engine.py` — REVERSAL 부분점수화(macd_cross 0.75+ema_cross 0.50+rsi_zone 0.25), 펀더멘털 필터, 실적발표 필터, 트레일링 스탑, 섹터 RS 보너스 +0.5. 최대 점수 7.5→8.5점
- **수정**: `modules/stock_fetcher.py` — `get_fundamentals()` 추가 (7일 캐시), `update_trailing_stop()` 추가, `fundamentals_cache` 테이블 추가, positions 테이블에 max_price/trailing_stop 컬럼 추가
- **수정**: `modules/stock_scanner.py` — `_get_top_sectors()` 추가 (섹터 ETF RS 순위), 스캔 루프에서 펀더멘털+트레일링 스탑 연동
- **문서**: `wiki/50_Invest/MJ 주식 분석 시스템 V_FINAL 전략 가이드.md` — v1.3 5레이어 구조도 전면 갱신, 수식 완전 재작성
- **에러 진단**: 펀더멘털 데이터 없으면 통과(None guard). yfinance .calendar가 DataFrame/dict 두 형태 — 모두 처리. 트레일링 스탑은 포지션 없으면 skip

### ✅ 2026-06-10 | Claude Code | **주식 시스템 v1.2 — 전략 완화 + 전체 명령어 테스트**
- **수정**: `modules/stock_signal_engine.py` — IS_TIGHT 필수게이트 제거, IS_LEADER 임계값 0.97→0.90, SCORE 4.5→3.0
- **수정**: `handlers/_stock.py` — `/scan TICKER` 리다이렉트 버그 수정, 포매팅 이중개행 수정
- **결과**: SEPA 36개 → 매수신호 6개 (AMAT/KLAC/NXPI/FTNT/GS/MS)
- **테스트**: 전체 11개 명령어/기능 터미널 검증 완료 (모두 ✅)
- **문서**: `wiki/50_Invest/MJ 주식 분석 시스템 V_FINAL 전략 가이드.md` — 4레이어 구조 전면 재작성, v1.2 파라미터 표, 점수별 의미, 테스트 결과 추가
- **에러 진단**: 전략 완화 전 TIGHT=0%(변동성 확대 구간), LEADER 3개(임계값 97% 과타이트) → 조합 0개. 완화 후 정상

### ✅ 2026-06-10 | Claude Code | **주식 시스템 v1.1 — /scan sepa 추가**
- **수정**: `modules/stock_scanner.py` — `sepa_list` 필드 추가 (SEPA 통과 전체 종목 결과에 포함, 알파점수 정렬)
- **수정**: `handlers/_stock.py` — `/scan` 기본 출력에 SEPA 상위 15개 자동 표시 + `/scan sepa` 서브커맨드 (전체 목록, 페이지 분할)
- **수정**: `handlers/_ui.py` — `/help`에 `/scan sepa` 설명 추가
- **수정**: `wiki/50_Invest/MJ 주식 분석 시스템 V_FINAL 전략 가이드.md` — v1.1 반영, `/scan sepa` 상세 사용법 추가
- **배경**: `/scan` 결과에서 SEPA 36개 목록이 매수신호 0개일 때 사라지는 문제 해결
- **에러 진단**: sepa_list는 scanner.run_scan() 결과에 포함됨. buy_signals/sell_watch와 독립적으로 수집

### ✅ 2026-06-10 | Claude Code | **주식 시스템 V_FINAL 완성 + MCP 서버 + 피드백 루프**
- **신규**: `Scripts/stock_mcp_server.py` — Claude Code용 MCP 서버 (6개 도구: analyze_stock, run_scan, market_status, record_result, backtest_summary, watchlist). Hermes 완전 독립 별개 프로세스
- **신규**: `Scripts/modules/stock_fetcher.py` — yfinance 데이터 소스, SQLite(positions/watchlist/scan_log/trade_results)
- **신규**: `Scripts/modules/stock_indicators.py` — 순수 pandas/numpy 45개 지표 (pandas-ta/numba 불필요)
- **신규**: `Scripts/modules/stock_signal_engine.py` — V1.8~V3.0 병합 V_FINAL 전략 (SEPA+TIGHT+LEADER+ALPHA_SCORE)
- **신규**: `Scripts/modules/stock_scanner.py` — NASDAQ 유니버스 스캔 오케스트레이터
- **신규**: `Scripts/handlers/_stock.py` — 텔레그램 핸들러 7개 (/stock /scan /market /watchlist /positions /result /backtest)
- **수정**: `Scripts/hermes_local.py` — stock 핸들러 7개 등록 (try/except graceful 처리)
- **MCP 등록**: `claude mcp add stock-scanner` → `~/.claude.json` project scope, ✓ Connected 확인
- **패키지 설치**: `mcp==1.27.2` (Python 3.14.4 환경)
- **설계 원칙**: 일봉 기준 스윙 트레이딩, yfinance 15분 지연 OK, 고확신 종목만 (ALPHA_SCORE≥4.5/7.5)
- **피드백 루프**: `/result CRWD 580 620 5` → SQLite 기록 → `/backtest` 승률/RR 통계 → V_FINAL 파라미터 개선 근거
- **에러 진단**: ① MCP 안 됨 → `claude mcp list | grep stock` 확인, `python3 Scripts/stock_mcp_server.py` 직접 실행 테스트 ② 텔레그램 핸들러 로드 실패 → handlers/__init__.py Python 3.14 호환 문제 (executor 패키지), try/except로 graceful 처리 중 ③ pandas-ta import 실패 → 설계상 사용 안 함(순수 pandas만)
- **사용 패턴**: 출장=텔레그램 /scan, 집=Claude Code MCP + Webull 차트 시각 확인

### ✅ 2026-06-10 | Claude Code | **아티클 3종 Loop Engineering 적용 분석**
- **분석**: Loop Engineering(addyosmani), HIVE 3티어(Boris Cherny), Buildroom(gkisokay) 3개 아티클 교차 분석
- **결론**: Hermes 시스템이 5 Pillars의 85% 구현 완료. 추가 설치보다 기존 모듈 트리거 등록이 우선
- **적용 불필요**: Buildroom JSON 계약 체인(솔로 운영 과도한 overhead), ant CLI(기존 Python으로 대체), swarm(개발팀용)
- **CLAUDE.md 증류 루프**: 매주 일요일 오전 세션 보정 내역 채굴 → CLAUDE.md 개선안 제안. CronCreate 등록(세션 한정, 영구화 불필요 결정)
- **에러 진단**: 없음 (코드 변경 없음, 분석 + 의사결정만)
- **관련 문서**: `hermes_harness_skill_모음.md` (미등록 모듈 스킬 목록 유지)

### ✅ 2026-06-10 | Claude Code | **/ship 병렬 서브에이전트 팀 설정**
- **신규**: `~/.claude/agents/writer.md` — 코드 작성 전담 (Sonnet, Write/Edit/Bash)
- **신규**: `~/.claude/agents/reviewer.md` — 리뷰 전담, 수정 안 함 (Sonnet, Read/Grep만)
- **신규**: `~/.claude/agents/tester.md` — 테스트 작성 전담 (Sonnet, Read/Write/Bash)
- **신규**: `~/.claude/commands/ship.md` — `/ship <작업>` 슬래시 커맨드 오케스트레이터 (Opus)
- **환경변수**: `~/.zshrc`에 `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 추가
- **동작**: `/ship <작업설명>` → Opus가 브리핑 작성 → Writer+Tester 병렬 실행 → Writer 완료 시 Reviewer → 최종 리포트 1개
- **에러 진단**: 병렬 실행 안 됨 → `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 환경변수 확인 (`echo $CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`). Task 도구가 비활성화 상태면 환경변수 미적용.
- **적합한 작업**: 파일 2개 이상 수정 + 테스트 필요한 중간 규모 작업. 단순 수정은 일반 Claude Code가 빠름.
- **관련 파일**: `~/.claude/agents/*.md`, `~/.claude/commands/ship.md`, `~/.zshrc`

### ✅ 2026-06-09 | Claude Code | **meta-update 스킬 생성 + Hermes Harness Skill 관리 체계 수립**
- **신규**: `~/.hermes/skills/meta-update/SKILL.md` — "메타 업데이트해줘" 자연어로 호출 시 세션 작업 분석 → 관련 메타 7종 + 00_Meta 하위 파일 자동 업데이트
- **신규**: `wiki/00_Meta/hermes_harness_skill_모음.md` — 커스텀 스킬 목록, 호출 방법, 동작 설명 관리 문서
- **변경**: PostToolUse 훅 제거 (`~/.claude/settings.json`) — "리마인더 주입"에 불과해 실질 자동화 아님. 대신 meta-update 스킬로 대체
- **에러 진단**: 스킬 동작 안 함 → `~/.hermes/skills/meta-update/SKILL.md` 존재 확인. Hermes 텔레그램에서만 자동 로딩됨. Claude Code는 MCP 통해 접근 가능.
- **관련 파일**: `~/.hermes/skills/meta-update/SKILL.md`, `wiki/00_Meta/hermes_harness_skill_모음.md`

### ✅ 2026-06-09 | Claude Code | **harness_agent.py 3차 분할 — command_router + response_handler 이관**
- **배경**: 2차 분할 후 494줄 잔존. `handle_message()` 내 명령어 라우팅(L239~345)과 LLM 응답 파이프라인(L347~443)을 추가 분리.
- **결과**: **494줄 → 319줄** (35% 추가 감소). 전체 887→319줄 (64% 총 감소). `handle_message()` 본체 ~35줄의 얇은 글루로 완성.
- **신규 모듈 2개**:
  1. `modules/command_router.py` (~135줄) — `/confirm`, `/cancel`, 🔄 모드전환, `@검색/@상태/@정리/@ingest`, Logic Engine 우선처리. `route_command()` 단일 진입점, 처리됐으면 True 반환.
  2. `modules/response_handler.py` (~130줄) — 진행 애니메이션, assemble_context→get_llm_response→run_agentic_loop 파이프라인, SAVE 태그 처리, 히스토리 저장, 최종 응답 전송. `handle_llm_response()` 단일 진입점.
- **수정**: `harness_agent.py` — `handle_message()` 전체를 `route_command()` + `handle_llm_response()` 2줄 위임으로 교체.
- **연결 구조**:
  - `harness_agent.handle_message()` → `modules/command_router.route_command()` (T/F 반환)
  - `harness_agent.handle_message()` → `modules/response_handler.handle_llm_response()`
  - `response_handler` → `modules/context_assembler`, `modules/agentic_loop`, `modules/permission_bridge`
  - `command_router` → `modules/file_ops_agent`, `handlers/_base._reply_long`
- **에러 진단 포인트**:
  - `/confirm` `/cancel` 안 됨 → `command_router.route_command()` — `file_op_pending` dict 참조 확인
  - `@검색` 응답 없음 → `command_router` — `get_semantic()` None 여부 확인
  - 🔄 모드전환 키보드 안 뜸 → `command_router` — `ALL_MODES`, `MODE_LABELS` 인자 전달 확인
  - LLM 응답 없음 (anim만 뜸) → `response_handler.handle_llm_response()` — `get_llm_response` 예외 트레이스 확인
  - [SAVE] 태그 처리 안 됨 → `response_handler` — `permission_bridge.guard_external_tool` import 확인
- **정리**: `harness_agent.py.bak_20260609` — 작업 완료 후 삭제 예정 (현재 유지 중)
- **관련 파일**: `modules/command_router.py`, `modules/response_handler.py`, `harness_agent.py`

### ✅ 2026-06-09 | Claude Code | **harness_agent.py 2차 분할 — context_assembler + agentic_loop 이관**
- **배경**: harness_agent.py `handle_message()` 함수 하나가 609줄(전체 887줄)짜리 God Function. 메타 7종 자동 업데이트 훅 설정 병행.
- **결과**: **887줄 → 494줄** (44% 감소). `handle_message()` 내 핵심 로직 2블록이 전용 모듈로 이관됨.
- **신규 모듈 2개**:
  1. `modules/context_assembler.py` (254줄) — wiki ctx 캐시, style_profile, 히스토리, L2/L3 메모리, Resolver, 시스템 팩트(briefing.md), 날씨 스크래핑을 조립해 `messages` 리스트 반환. `assemble_context()` 단일 진입점.
  2. `modules/agentic_loop.py` (276줄) — 에이전틱 루프(최대 3회), 도구 태그 파싱·실행(LIST/READ/WEB_READ/SEARCH/RUN_CMD/CREATE/DELETE/MOVE/COPY/RENAME), ToolResult 샌드박스 래핑, Mayor 루프 감독, CoVe 파일시스템 자기검증. `run_agentic_loop()` 단일 진입점.
- **수정**: `harness_agent.py` — L372~500 컨텍스트 블록 → `assemble_context()` 호출로 교체. L503~694 에이전틱 루프 → `run_agentic_loop()` 호출로 교체. L696~788 복원용 주석 코드 91줄 삭제.
- **connection 구조**:
  - `harness_agent.py` → `modules/context_assembler.py` (import: `assemble_context`)
  - `harness_agent.py` → `modules/agentic_loop.py` (import: `run_agentic_loop`)
  - `agentic_loop.py` → `modules/tool_result.py`, `modules/mayor_agent.py`, `modules/file_ops_agent.py`, `modules/permission_bridge.py`, `cove_engine.py`
  - `context_assembler.py` → `modules/memory_refinement.py`, `modules/hermes_context_builder.py`, `handlers/_base.py` (style_profile)
- **에러 시 진단 포인트**:
  - `ImportError: modules.context_assembler` → `Scripts/modules/context_assembler.py` 파일 존재 확인
  - `ImportError: modules.agentic_loop` → `Scripts/modules/agentic_loop.py` 파일 존재 확인
  - 날씨 응답 없음 → `context_assembler._inject_weather_context()` — BS4/urllib 정상 여부 확인
  - 도구 태그 실행 안 됨 → `agentic_loop.run_agentic_loop()` — Mayor/ToolResult import 확인
  - 메모리/wiki 컨텍스트 누락 → `context_assembler.assemble_context()` 내 각 try/except 블록 확인
- **관련 파일**: `modules/context_assembler.py`, `modules/agentic_loop.py`, `harness_agent.py`, `harness_agent.py.bak_20260609`

### ✅ 2026-06-09 | Claude Code | **Phase 2 세밀한 메모리 (Memory Refinement) 구현**
- **신규**: `modules/memory_refinement.py` — bio_memory_engine(Lock Stack)을 건드리지 않는 4대 갭 해결 래퍼.
- **기능**: auto_forget(에빙하우스 기반 자동 정리), check_conflict(충돌 감지), should_store(저장 판단), hybrid_recall(L2+FTS5 병합 검색).
- **수정**: `handlers/_memory.py` — `/memory forget [confirm]`, `/memory health` 서브커맨드 추가.
- **수정**: `harness_agent.py` — LLM 컨텍스트에 hybrid_recall 자동 주입 (기존 pre_query_context 보완).
- **수정**: `handlers/_ui.py` — `/help`에 새 서브커맨드 반영.
- **관련 파일**: `modules/memory_refinement.py`, `handlers/_memory.py`, `harness_agent.py`, `handlers/_ui.py`

### ✅ 2026-06-09 | Claude Code | **Phase 1 낙관적 응답 엔진 (Optimistic Response) 구현**
- **신규**: `modules/optimistic_response.py` — Linear 아키텍처 기반 즉시 피드백 엔진. 사용자 명령 시 즉시 "⏳ 진행 중" → 백그라운드 처리 → 메시지 편집으로 결과 표시.
- **기능**: 3회 자동 재시도(2초 간격), 에러 유형별 복구 힌트(권한/네트워크/파일/메모리), `get_engine()` 모듈 싱글턴.
- **수정**: `handlers/_file.py` — `/ingest` 3분기(기본/scan/interrogate) 전부 낙관적 엔진으로 전환.
- **수정**: `handlers/_callbacks.py` — `/retry` 콜백 핸들러 추가 (마지막 실패 작업 정보 표시).
- **수정**: `harness_agent.py` — `/retry` 텍스트 인터셉트 → `handle_retry_callback` 위임.
- **관련 파일**: `modules/optimistic_response.py`, `handlers/_file.py`, `handlers/_callbacks.py`, `harness_agent.py`

### ✅ 2026-06-09 | Claude Code | **Round 2 대규모 분할 완료 — handlers 13→17개, modules 54→57개**
- **배경**: verify_harness 2차 진단(67/100) 후 잔존 🟡 600줄+ 파일 4개 추가 분할. 👻 ghost 문서 5개도 수정.
- **분할 완료 (Round 2)**:
  1. `handlers/_research.py` 831줄 → `_paper.py` (신규 582줄) + `_research.py` (254줄) — 논문/연구 분리
  2. `handlers/_system.py` 705줄 → `_system.py` (521줄) + `_system_ops.py` (신규 191줄) — cmd_handoff/search/restart_bot 분리
  3. `handlers/_base.py` 663줄 → `_base.py` (425줄) + `_callbacks.py` (신규 259줄) — InlineKeyboard 라우터 분리
  4. `modules/core_reducer.py` 809줄 → `core_agents.py` (신규 505줄) + `core_reducer.py` (313줄) — 에이전트 클래스 6개 분리
- **👻 Ghost 문서 수정**: `harness_verifier.py` `glob("*.py")` → `rglob("*.py")` 변경 — `dialectic_layer/` 서브디렉터리 파일 감지 정상화
- **메타 업데이트**: `02_스크립트 정보.md`, `03_시스템 인벤토리.md` handlers/modules 카운트 및 설명 업데이트
- **sudo purge 안전 분석**: `vm_stat` 기준 File-backed 1.8GB만 정리, Anonymous(앱) 18.1GB 완전 미영향 — 반복 사용 안전
- **관련 파일**: `handlers/_paper.py`, `handlers/_callbacks.py`, `handlers/_system_ops.py`, `modules/core_agents.py`

### ✅ 2026-06-09 | Claude Code | **harness_agent.py + skill_evolver.py 책임 분할 완료**
- **배경**: verify_harness 첫 진단에서 B등급(60/100) — harness_agent.py(1243줄), skill_evolver.py(918줄) 분할 필요로 지적됨
- **분할 완료**:
  1. **harness_agent.py**: 1243줄 → **882줄** (-361줄) — LLM 엔진/파일 작업 분리 후 에이전틱 루프 핵심만 유지
  2. **skill_evolver.py**: 918줄 → **351줄** (-567줄) — 큐레이션 기능 분리 후 SkillEvolver 클래스 + re-export만 유지
  3. **llm_engines.py**: 신규 267줄 — harness_agent에서 분리된 LLM 모드/Circuit Breaker/엔진 호출
  4. **file_ops_agent.py**: 신규 126줄 — harness_agent에서 분리된 파일 작업 확인/경로 검증/실행
  5. **skill_curator_ext.py**: 신규 564줄 — skill_evolver에서 분리된 큐레이션/통계/검증 (순환import 방지: 공유 상수 중복 정의)
- **의존성 확인**: 
  - `modules/subconscious_layer.py`의 `from modules.skill_evolver import prune_old_skills` 유지 (skill_evolver.py에서 re-export)
  - `_llm_eng` 모듈로 fallback_notify_bot 주입 방식 변경 (`harness_agent.py` L233)
  - `_file_ops_agent` import 추가 (`harness_agent.py` L221-222)
- **테스트**: 모든 import 정상 (harness_agent, llm_engines, file_ops_agent, skill_evolver, skill_curator_ext)
- **UI 개선**: `/verify_harness` 명령어에 InlineKeyboard 버튼 추가 — "✅ 진단 실행" / "❌ 취소" 버튼 클릭 시 실행
  - 콜백: `handlers/_ui.py` `callback_verify_harness_run()` / `callback_verify_harness_cancel()` + `_base.py` handle_button_callback 연결
- **문서 업데이트**: `02_스크립트 정보.md`에 3개 신규 모듈 추가 + 모듈 개수 업데이트
- **다음 목표**: `/verify_harness` 재실행 → 75+ (A등급) 달성
- **관련 파일**: `harness_agent.py`, `skill_evolver.py`, `modules/llm_engines.py`, `modules/file_ops_agent.py`, `modules/skill_curator_ext.py`, `handlers/_ui.py`, `handlers/_base.py`

### ✅ 2026-06-09 | Claude Code | **/verify_harness 구조 진단 명령어 신규 구현**
- **배경**: AI한테 "내 하네스 좋아보여?" 물으면 아첨 편향 + 컨텍스트 없는 평가 → 신뢰도 낮음. 수치 기반 자동 측정 도입.
- **구현**:
  - `modules/harness_verifier.py` (신규): 6개 항목 측정 → 100점 점수화
  - `handlers/_ui.py` `cmd_verify_harness()`: 텔레그램 `/verify_harness` 핸들러
  - `hermes_local.py` CommandHandler 등록
  - `/help` 텍스트 업데이트
- **문서 표류 자동 감지**: 항목 3(문서 동기화)에서 `02_스크립트 정보.md` ↔ 실제 파일 자동 비교. 문서에만 있는 파일(👻 표류), 파일에만 있는 것(📄 미문서화) 모두 출력.
- **첫 실행 결과 (2026-06-09 00:48)**: **60/100 B등급**
  - 🔴 harness_agent.py 1243줄, skill_evolver.py 918줄 — 분할 필요
  - 👻 문서 표류 5개: `_deriver.py`, `_dialectic.py`, `_dreamer.py`, `_fts.py`, `_handoff.py`
  - ✅ 서비스 3개 정상, ✅ L1/L2/L3 메모리 정상, ✅ 임시 파일 없음
- **관련 파일**: `modules/harness_verifier.py`, `handlers/_ui.py`, `hermes_local.py`

### ✅ 2026-06-09 | Claude Code | **Stage 5 Mayor 에이전트 + 토큰 예산 가드레일 + 루프 자기검증 구현**
- **Mayor 에이전트** (`modules/mayor_agent.py` 신규):
  - `MayorAgent` 싱글톤 — 모든 에이전틱 루프의 생애주기 추적 (등록→틱→종료)
  - `register(name, max_iters, token_budget)` → loop_id 반환
  - `tick(loop_id, tokens_used)` → 예산/반복 초과 시 False 반환 (루프 중단 신호)
  - `finish(loop_id, success)` + `should_stop()` + `dashboard()` + `prune_old()` 메서드
  - 정체 감지: 5분 이상 tick 없으면 `stale` 상태 자동 전환
  - `/orchestrate mayor` → 대시보드 출력 (활성+완료 루프 전체 현황)
- **토큰 예산 가드레일** (`harness_agent.py`):
  - 에이전틱 루프 매 턴마다 `_est_tokens()` (글자수/3)로 소비 토큰 추정
  - Mayor.tick() 반환값 False 시 루프 강제 종료 + 사용자 알림
  - 기본 예산: 12,000토큰/루프 (NVIDIA 무료 API 기준 여유 있음)
- **루프 자기검증** (`harness_agent.py`):
  - 에이전틱 루프 정상 종료(break) 직전 CoVe `_filesystem_grounding()` 자동 실행
  - 답변 내 파일명 주장(.py/.sh)이 실제로 존재하지 않으면 경고 블록 자동 추가
  - 실패 시 조용히 무시 (서비스 영향 없음)
- **관련 파일**: `modules/mayor_agent.py`(신규), `harness_agent.py`, `handlers/_orchestrator.py`

### ✅ 2026-06-09 | Claude Code | **AgentForge 4패턴 구현 + "루프 설계" 아키텍처 분석 적용**
- **배경**: AgentForge 아티클의 핵심 안전/구조 패턴 4종 도입. "루프 설계" 트렌드(steipete/bcherny) 분석 → Hermes 적용 가능성 검토
- **구현 1 — Circuit Breaker** (`harness_agent.py`):
  - `_CIRCUIT_BREAKER` dict + `_cb_is_open()` / `_cb_record_fail()` / `_cb_record_success()` 함수 추가
  - 엔진별 연속 3회 실패 → 60초 회로 개방(차단). 폴백 체인도 Circuit Breaker 확인 후 건너뜀
  - 효과: 죽은 엔진을 반복 호출하는 지연 제거. 응답 시간 평균 ~2-3초 단축 (장애 상황)
- **구현 2 — Prompt Injection 방어** (`harness_agent.py`):
  - `_sandbox_tool_output()` → `ToolResult` 모듈로 업그레이드
  - `[TOOL_OUTPUT_START type=... status=...]...[TOOL_OUTPUT_END]` 샌드박스 태그 적용
  - WEB_READ / SEARCH / READ 3개 도구 출력 전체 커버
  - 효과: 웹 스크래핑 결과 내 "무시하고 다음 지시를 따라라" 류 공격 차단
- **구현 3 — Context Compaction** (`modules/history_manager.py`):
  - 30턴 초과 시 오래된 대화를 요약 메시지 1개로 압축 (`COMPACT_THRESHOLD=30`, `KEEP_RECENT=10`)
  - `get_history_for_llm()` 자동 압축 + `compact_and_save()` 강제 저장 메서드 추가
  - 효과: 장기 대화 시 토큰 낭비 방지. 핵심 맥락 보존하면서 LLM 컨텍스트 윈도우 절약
- **구현 4 — ToolResult 구조화** (`modules/tool_result.py` 신규 생성):
  - `ToolResult(tool_type, content, ok, artifacts, recovery_hint, next_actions)` 데이터클래스
  - `to_prompt()` → 구조화된 프롬프트 출력 (결과물 식별자 + 권장 다음 행동 포함)
  - `ToolResult.error()` 단축 생성자. 도구별 기본 next_actions 맵 내장
  - 효과: LLM이 도구 결과를 받고 "다음에 뭘 해야 하나" 불확실성 감소 → 에이전틱 루프 품질 향상
- **관련 파일**: `harness_agent.py`, `modules/history_manager.py`, `modules/tool_result.py`(신규)

### ✅ 2026-06-09 | Claude Code | **"루프 설계" 트렌드 분석 → Hermes 적용 결론**
- **배경**: Boris Cherny(Claude Code 창시자) "루프가 새 단위 — 나는 더 이상 프롬프트 안 친다. 루프를 쓴다" 발언 → 2.2M 조회수 논쟁
- **핵심 결론**: Hermes는 이미 "루프 설계" 시스템. 텔레그램이 루프 인프라, 스킬이 재사용 단위.
- **단계별 매핑**:
  - Stage 1 (ReAct while-loop) → ✅ 구현됨: `harness_agent.py` 에이전틱 루프 (최대 3회)
  - Stage 2 (자기 프롬프트) → ✅ 구현됨: `/kanban` + `/handoff` + `/delegate`
  - Stage 3 (ralph loop) → ✅ 구현됨: `/loop` 명령어 (CronCreate 기반)
  - Stage 4 (/goal) → ✅ 구현됨: `/caveman` 모드 (계획 후 실행)
  - Stage 5 (루프가 루프를 감독) → ⚠️ 부분 구현: `/orchestrate` 병렬 서브에이전트는 있으나 "루프가 루프를 스케줄" 계층은 미구현
- **즉각 적용 가능 항목**: 비용 관리 3대 가드레일 (max_iterations, no-progress 감지, 토큰 예산)
- **중장기 고려 항목**: Stage 5 멀티루프 오케스트레이션 (Hermes v9.4 로드맵에 포함 권장)
- **관련 문서**: `01_hot.md` 루프 설계 섹션 참조

### ✅ 2026-06-08 | Claude Code | **LLM 환각 방지 3계층 구조 구현**
- **배경**: GPT OSS 120B가 Hermes 스크립트에 ffmpeg가 없음에도 "사용 중"이라고 환각 → 잘못된 보안 분석 생성
- **원인 분석**: LLM이 실제 파일 확인 없이 "자동화 시스템 → 미디어 처리 → ffmpeg" 추론 패턴으로 환각 생성. CoVe가 위키 검색만 하고 파일시스템 검증 없음.
- **구현된 3계층**:
  1. **방법 1** — `claude_briefing.md`에 "❌ 미사용 기술 스택" 섹션 추가 (ffmpeg/Docker/RTSP/Redis 등 7종). `/claude_brief` 갱신 시 자동 포함됨
  2. **방법 2** — `harness_agent.py` handle_message()에 시스템 컨텍스트 자동 주입 추가. "내 하네스/내 시스템/하네스에" 등 10개 키워드 감지 시 claude_briefing.md 핵심 섹션을 LLM system 메시지로 자동 주입
  3. **방법 3** — `cove_engine.py` Step 5 파일시스템 검증 추가. CoVe 4단계 완료 후 답변 내 파일명(.py/.sh) 주장을 grep으로 실존 확인. 미존재 시 "⚠️ [CoVe 파일시스템 검증]" 경고 추가
- **테스트**: `fake_module.py에서 ffmpeg 사용` → Step5에서 즉시 감지 확인
- **관련 파일**: `claude_briefing.md`, `harness_agent.py`, `cove_engine.py`

### ✅ 2026-06-08 | Claude Code | **FFmpeg 보안 업그레이드 + pixel-art 스킬 절대경로 수정**
- **변경 사항**:
  1. `brew upgrade ffmpeg` — 8.1 → 8.1.1 (보안 패치 포함)
  2. `venu/.hermes2/skills/creative/pixel-art/scripts/pixel_art_video.py` 절대경로 수정
     - `FFMPEG_BIN = "/opt/homebrew/bin/ffmpeg"` 상수 추가
     - `_ensure_ffmpeg()` → 절대경로 우선, 없으면 PATH fallback
     - `subprocess.run(["ffmpeg", ...])` 2곳 → `[_ffmpeg, ...]` 로 교체
- **확인 사항**: Hermes1/2 핵심 스크립트(Scripts/*.py, *.sh)에는 ffmpeg 미사용 — 외부 분석의 "Hermes가 ffmpeg를 사용한다"는 전제 자체가 오류였음
- **실제 ffmpeg 사용 위치**: Hermes2 creative 스킬 3종(pixel-art, manim-video, p5js)만 — 모두 로컬 파일 변환용, RTSP/네트워크 입력 없음
- **관련 파일**: `/opt/homebrew/bin/ffmpeg`, `venu/.hermes2/skills/creative/pixel-art/scripts/pixel_art_video.py`

### ✅ 2026-06-08 | Claude Code | **curator.py 확장 + .bak 정리 + CLAUDE.md 편집자 원칙 추가**
- **변경 사항**:
  1. `modules/curator.py` 확장 — 스킬 관리 전용 → 코드베이스 편집자로 업그레이드
     - `scan_large_files()`: 300줄+ Python 파일 탐지, 🔴위험(900줄+) / 🟡경고(600줄+) 등급 분류
     - `scan_bak_files()`: .bak/_old/_test 임시 파일 탐지 (_backup/ 폴더 제외)
     - `full_audit()`: 스킬감사 + 대형파일 + .bak 통합 리포트 (텔레그램 출력용)
  2. .bak 파일 6개 삭제 (코드베이스 오염 정리):
     - `hermes_local.py.bak2`, `modules/dreaming_v2.py.bak.*`, `handlers/_memory.py.bak.*`
     - `hermes/memory_engine/consolidator_state.json.bak_*` (2개), `_archive/cove_engine.py.bak`
  3. `CLAUDE.md` 행동 원칙에 "편집자 원칙" 섹션 추가 — 제거 우선, .bak 즉시 삭제, 300줄 초과 Write 금지
- **현재 코드베이스 상태**: 🔴위험 2개(harness_agent.py 1115줄, skill_evolver.py 918줄), 🟡경고 7개
- **curator 사용법**: 텔레그램 🧹 큐레이터 버튼 또는 `python3 -c "from modules.curator import SkillCurator; print(SkillCurator().full_audit())"`
- **배경**: 기사 "취향(Taste)이 새로운 10x" 인사이트 — 편집자형 코드베이스 관리 적용

### ✅ 2026-06-08 | Claude Code | **Claude Code ↔ Hermes 통합 아키텍처 문서 생성 + 시스템 성장 구조 확정**
- **핵심 결론 확정**:
  1. 어떤 작업 경로(Claude Code / WebUI / 텔레그램)를 써도 시스템은 성장함
  2. bio_memory_engine.dream()은 로컬 LLM 불필요 — 현재 NVIDIA GPT OSS 120B로 작동
  3. WebUI 하단 모델 버튼(Hermes2) ≠ 텔레그램 LLM 모드(~/.hermes/llm_mode.txt) — 완전 별개
  4. 텔레그램 🌙 Dreaming 버튼 = dream() 직접 호출 = L3 장기 기억 성장 트리거
  5. 자동 Dreaming 스케줄러 미채택 — 수동이 더 품질 높음 (대화 충분히 쌓인 시점에 누름)
- **생성 문서**: `Claude_Code_Hermes_통합_아키텍처.md`
- **관련 파일**: `bio_memory_engine.py`, `harness_agent.py`, `handlers/_memory.py`

### ✅ 2026-06-08 | Claude Code | **하네스 스크립트 4종 정비 + CLAUDE.md 생성 + 메타 문서 업데이트**
- **변경 사항**:
  1. `check_services.sh` 전면 재작성 — 구 LM Studio/harness_v2.py 체계 → 현 아키텍처(5개 launchd + llama-server port 8080 + DeepSeek/NVIDIA API + WebUI 캐시 나이)
  2. `model_endpoint_check.sh` 신규 생성 — 실제 HTTP POST로 모델 엔드포인트 생사 확인 (NVIDIA GPT OSS 120B / DeepSeek / 로컬 llama-server), 404=dead 감지
  3. `clear_webui_cache.sh` 수정 — `ai.hermes.gateway` restart 추가 (누락되어 있었음)
  4. `check_venv_patches.sh` 전면 재작성 — 기존 파일 경로 오류·한글 깨짐 완전 수정, venv 실제 경로 기반으로 재작성
  5. `wiki/00_Meta/CLAUDE.md` 신규 생성 — Claude Code 세션 초기화 포인터 파일, `~/.claude/CLAUDE.md` 심볼릭 링크 연결
  6. `~/.claude/settings.json` 수정 — `"permissions": {"ask": ["Edit", "Write"]}` 추가 (Claude Code 파일 수정 시 허락 요구)
  7. `비서_시스템_메타_문서_비교_정의.md` 업데이트 — Section 4 "Claude Code 연동 문서 체계" 추가
  8. `00_Meta_지도.md` 업데이트 — Section 1에 `CLAUDE` 항목 추가
- **관련 파일**: `Mjauto/Scripts/check_services.sh`, `model_endpoint_check.sh`, `clear_webui_cache.sh`, `check_venv_patches.sh`, `wiki/00_Meta/CLAUDE.md`, `~/.claude/settings.json`
- **스크립트 위치**: `/Users/bluesea/Applications/Mjauto/Scripts/`

### ✅ 2026-06-07 22:00 | Hermes | **WebUI 멀티모델 통합 완료 + NIM 70B → GPT OSS 120B 마이그레이션**
- **변경 사항**:
  1. WebUI 드롭다운 Qwen-14B / GPT OSS 120B / Minimax M2.7 3종 완전 동작
  2. `hermes-webui/api/config.py` `_whitelist_keywords`에 qwen / gpt-oss-120b / minimax-m2.7 추가
  3. `gateway/platforms/api_server.py` `@custom:<slug>:` prefix strip 로직 추가 + `context_length` TypeError 패치
  4. `gateway/run.py` `_resolve_custom_provider_by_model()` context_length 반환 수정
  5. `~/.hermes/webui/models_cache.json` 캐시 삭제 (stale cache 제거)
  6. `harness_agent.py` + `llm_mode.txt`: NVIDIA NIM 70B(meta/llama3-70b-instruct) → GPT OSS 120B(openai/gpt-oss-120b)
  7. `~/.hermes/config.yaml`: `disabled_toolsets: [browser, vision]` + `skills.disabled` 115개 전체 비활성화
- **효과**: Qwen 첫 응답 120s → 77s (skills 3,348 tokens 제거), KV 캐시 후 1-3s
- **M4 스펙 확정**: Mac Studio M4 / 36GB RAM (구 문서 M2 Max/Ultra 오기 수정 완료)
- **신규 문서 생성**: `HERMES_HOME 과 환경 변수의 진짜 물리적 지도 (Fact Check) 20260607.md`, `WebUI_멀티모델_통합_장기화_원인분석_20260607.md`
- **llama-server 종료 가능 여부**: Telegram(GPT OSS 120B 모드)·WebUI(Qwen 미선택 시) 모두 영향 없음 — Qwen 선택 시에만 필요
- **관련 파일**: `~/.hermes/config.yaml`, `hermes-webui/api/config.py`, `gateway/platforms/api_server.py`, `gateway/run.py`, `Mjauto/Scripts/harness_agent.py`, `~/.hermes/llm_mode.txt`
- **장애 기록**: #024

### ✅ 2026-06-07 02:33 | Hermes | **Qwen2.5-14B-Instruct → Qwen2.5-14B-Instruct-A3B 모델 교체**
- **변경 사항**:
  1. 로컬 LLM 모델 교체: Qwen2.5-14B-Instruct (gemma-4-26b-a4b-it) → Qwen2.5-14B-Instruct-A3B (Qwen2.5-14B-Instruct-A3B-UD-Q4_K_M.gguf)
  2. `~/.hermes/config.yaml` custom_providers: Gemma (localhost:1234/v1) → Qwen (localhost:8080/v1, context_length 32768→65536 override)
  3. `harness_agent.py`: 모든 UI 문자열 Gemma4→Qwen3.6 교체 (MODE_GEMMA4 값, 라벨, 출력 메시지, 오류 메시지)
  4. `llm_interface.py`: `_call_gemma4()` docstring에 Qwen3.6 명시
  5. llama-server: PID 84469, Qwen2.5-14B-Instruct-A3B-UD-Q4_K_M.gguf, port 8080, -c 32768 -ngl 99
- **미해결 이슈**: WebUI 하단 modelSelect 드롭다운에 Qwen 미표시 (API에는 등록됨); Qwen 호출 시 1분 40초+ thinking 후 응답 없음
- **관련 파일**: ~/.hermes/config.yaml, ~/Applications/Mjauto/Scripts/harness_agent.py, ~/Applications/Mjauto/Scripts/modules/llm_interface.py
- **업데이트된 메타 문서**: 7종 메타 + HERMES3_MASTER_DEVELOPMENT_GUIDE + 장애 기록

||| ||| **🚀 2026-06-07 11:52** | **Hermes** | **cove_engine.py Scripts/로 이전**: `~/.hermes/governance/skills/brain/cognitive/scripts/cove_engine.py` → `Scripts/cove_engine.py`. 내부 하드코딩된 `sys.path.append` 2줄 제거, `wiki_manager` import를 `modules.wiki_manager`로 변경. handlers 7개 모듈 영향 없음. 원본 백업 후 삭제 완료 (`_archive/cove_engine.py.bak`). | cove_engine.py, handlers/_base.py | 완료 | 스크립트 경로 일원화 + 하드코딩 경로 제거 |
||| ||| **🚀 2026-06-06 09:15** | **Hermes** | **memory_engine 디렉터리 및 빈 consolidator_state.json 파일 생성**
- 효과: 메모리 엔진 경로 오류 방지, 초기화 시 파일 존재 검증 통과, `FileNotFoundError` 및 `JSONDecodeError` 사전 차단.
- 관련 파일: ~/Applications/Mjauto/Scripts/hermes/memory_engine/, consolidator_state.json
- 업데이트된 메타 문서: 01_hot.md, 04_주요 시스템 가이드 및 FAQ.md, 05_시스템 상태.md, 06_에이전트 오류 및 재발방지 보고서.md

## 🕒 최근 작업 이력

### 🛠️ 현재 엔진 구성
| 컴포넌트 | 기본 엔진 | 역할 | 상태 |
| :--- | :--- | :--- | :--- |
|| **헤르메스 봇 (Hermes1)** | **Harness V2.5 (llama.cpp + Qwen2.5-14B-Instruct)** | **CoVe 팩트체크 및 자율 복구형 Bash 실행 관제탑 + Polling Watchdog** | **Active (Running, v9.2 — Qwen2.5-14B-Instruct 로컬, DeepSeek API 폴백)** |
| **하네스 (Harness)** | **미사용** | 메인 연구 및 자동화 에이전트 | *Disabled (Idle)* |
| **안티그래비티 봇** | **미사용** | 고수준 로직 및 모니터링 | *Disabled (Idle)* |
| **맥봇 (MacBot)** | **미사용** | 규칙 기반 OS 자동화 | *Disabled (Idle)* |
|| **헤르메스2 봇** | **클라우드 전용 (DeepSeek + NVIDIA)** | **클라우드 전용 LLM (DeepSeek 기본 + GPT OSS 120B 폴백)** | **✅ Active (클라우드 전용, 게이트웨이 불필요)** |

||| 날짜/시간 | 작업자 | 작업 내용 | 관련 파일 | 상태 | 이유 |
|||:---|:---|:---|:---|:---|:---|
||| **🚀 2026-06-05 12:00** | **Hermes** | **Bio-Memory Engine v9.3 — 3파일 구조 개선 완료**: bio_memory_engine.py `_get_l2_bytes()` + L2 KB/MB 보고. dreamer_layer.py `offline_consolidation_forced()` 강제 증류. dreaming_v2.py 용량 위기 강제 증류 호출 + atomic write 적용. import 검증 통과. | modules/bio_memory_engine.py, modules/dreamer_layer.py, modules/dreaming_v2.py | 완료 | L2 1.7MB/83건 급증 시 Early Exit 정체 해결 및 메모리 블로트 리스크 대응 |
| ||| **🚀 2026-06-07 10:30** | **Hermes** | **Gemma4 internal token fallback 해결 + Qwen→Gemma4 복귀**: Qwen→Gemma4 전환 후 Gemma4 모드가 항상 DeepSeek으로 fallback되는 현상 수정. `_call_local()`에 Gemma4 internal token(`<|channel>`) 제거 로직 추가. 모든 "Qwen3.6" 문자열 "Gemma4"로 복원 (8곳). llama-server gemma-4-26B 재기동. `qwen-gemma4-switch` 스킬 등록. | harness_agent.py, ~/.hermes/config.yaml, 장애 기록 #021 | 완료 | Gemma4 completions 응답에 `<|channel>` special token 포함되어 fallback 발동 — re.sub() 제거로 해결 |
|||| **🚀 2026-06-05 08:37** | **Antigravity** | **Llama-Server(로컬 LLM) 멀티 슬롯 컨텍스트 불일치 수정**: `com.bluesea.llama_server2.plist`의 병렬 슬롯 개수 옵션을 `-np 2`에서 **`-np 1`**로 수정하여 단일 슬롯에 65,536 컨텍스트를 온전히 할당. `config.yaml`의 `context_length: 65536`과 일치시켜 대화가 길어질 때 이전 생성 파일 정보 등 대화 앞부분이 서버단에서 강제 유실(망각)되던 장애를 해결하고 SWA 캐시 보존율 최적화. | com.bluesea.llama_server2.plist, 주요 6종 Meta 문서 | 완료 | 멀티 슬롯 설정에 따른 슬롯 분할(32,768 한계) 및 서버단 강제 Truncation으로 인한 에이전트의 이전 작업 망각 장애 원천 해결 |
||| **🚀 2026-06-04 22:20** | **Hermes** | **에이전트 세션 초기화 프로토콜 강제화**: WebUI 재시작 등 세션 리셋 시 에이전트가 헌법(타임스탬프, 태그 규칙 등)을 망각하고 작업을 시작하는 현상 해결을 위해, `USER.md`에 첫 턴 필수 동작으로 `constitution.local.md`와 `01_hot.md` 스캔을 강제화. | USER.md, 06_에이전트_오류_및_재발방지_보고서.md, 주요 7종 Meta 문서 | 완료 | 세션 단절 후 규칙 망각에 따른 파일 생성 포맷 오류(타임스탬프 누락 등) 원천 차단 |
||| **🚀 2026-06-04 21:50** | **Hermes** | **세션 워크스페이스 망각 장애 해결 및 다중 경로 바인딩 적용**: 세션 컴팩션 시 Mjobsidian 밖의 임시 외부 프로젝트(`MarineOS-XR Project`)를 스캔하지 않고 파일이 지워졌다고 허위 추측 답변을 하는 장애 발생. 중복 생성된 `Script/` 디렉토리 정리 완료. `constitution.local.md`에 §X.2(물리적 실재 확인 의무화) 및 §X.4(다중 작업 경로 바인딩) 신설. `01_hot.md`에 보조 작업 경로 기록. `06_에이전트_오류_및_재발방지_보고서.md` 발행. | constitution.local.md, 01_hot.md, 06_에이전트_오류_및_재발방지_보고서.md | 완료 | 세션 단절 후 워크스페이스 외부 경로에 대한 AI의 거짓/추측성 판단 방지 및 팩트 기반 스캔 강제화 |
||| **🚀 2026-06-03 17:55** | **Hermes** | **Ingest TagLinker vault_path 오류 수정**: `ingest_engine.py:20` `TagLinker(vault_path=...)` → `TagLinker()` 수정. TagLinker.__init__()는 db_path만 받음. Ingest 정상 동작 확인 (11 Clippings + 7 root files). | modules/ingest_engine.py | 완료 | TypeError — 호출부/callee 시그니처 불일치 |\n|| **🚀 2026-06-03 11:40** | **Hermes** | **PKM_2 Knowledge Mesh 전면 구현 완료**: Private Knowledge Mesh 2차 설계 10단계 구현. 신규 모듈 4개 생성 (knowledge_mesh_orchestrator.py 15,640자, timeline_builder.py 2,721자, cross_reference_analyzer.py 4,525자, auto_topic_manager.py 6,798자). 기존 모듈 3개 확장 (knowledge_indexer.py search_similar() 추가, paper_bundle_manager.py formal_date 지원, handlers/_research.py cmd_research 전면 재작성). Pytest 69/69 ✅ 전부 통과. 핫토픽 업데이트 완료. | modules/knowledge_mesh_orchestrator.py, modules/timeline_builder.py, modules/cross_reference_analyzer.py, modules/auto_topic_manager.py, modules/knowledge_indexer.py, modules/paper_bundle_manager.py, handlers/_research.py, 01_hot.md | 완료 | 연구 시간 90% 단축 목표 — LanceDB 불필요, 기존 TF-IDF 벡터 검색 확장, JSON 레시피 파이프라인 |\n|| **🚀 2026-06-02 22:15** | **Hermes** | **Hermes 2 WebUI 맥락 단절 원인 분석 및 SWA/Truncation 설정 최적화**: WebUI 대화 단절 및 `~/.hermes` 오염 문제 해결. (1) `llama-server -np 2` 병렬 처리 시 Gemma 4 SWA 캐시 무효화 원인 파악 → `-np 1`, `--cache-reuse 256` 수정. (2) 조기 압축 유발하는 `compressor` 엔진 대신 `.hermes2/config.yaml`의 `engine: truncation`, `threshold: 0.85` 상향. (3) Hermes 1 / 2 폴더 격리 정책 재확인 및 `.hermes2/skills/devops/llama-context-fix/SKILL.md` 저장. | start_llama.sh, .hermes2/config.yaml, SKILL.md, 주요 6종 Meta 문서 | 완료 | Hermes 2의 심각한 기억 상실(건망증) 해결 및 두 시스템 간 경로 혼선 완전 차단 |
||||||||| **🚀 2026-06-01 08:00** | **Hermes** | **`/reduce` 2가지 버그 수정**: (1) `_execute_ask` `except ImportError`→`except Exception` — `cmd_ask_logic` 내 예외 발생 시 빈 결과 방지. (2) `_execute_web`을 `hermes_web_search_plus`(미설치) → `duckduckgo_search`(DDGS)로 교체, 웹 검색 정상화. 미사용 `_WEB_SEARCH_ENV`/`WEB_CACHE_TTL` 제거. | modules/core_reducer.py | 완료 | /reduce 안녕하세요! → 빈 결과. /reduce 오늘 날씨 → 위키 fallback |
|||||||| **🚀 2026-06-01 09:00** | **Hermes** | **PDF→MD 파이프라인 구현**: `ingest_engine.py`에 `_read_file_content()` 메서드 추가 — PDF(PyMuPDF/fitz) 텍스트 추출 + 일반 텍스트 확장자 분기. Clippings/ + 루트 파일 모두 `.pdf` ingest 지원. 빈 PDF 방어 처리. `/ingest` 한 번으로 PDF→.md 자동 변환 + LLM 분류 + FTS5 검색 가능. | modules/ingest_engine.py | 완료 | 기존 .md 전용 ingest에서 PDF 미지원 → 텍스트 추출 불가. NotebookLM MCP(비공식 API)만으로는 영구 저장/검색 불가
|||||||| **🚀 2026-06-01 19:10** | **Hermes** | **`/reduce` 보안 감사 + 3계층 방어 + 캐싱 최적화 + 테스트 격리 + 문서화**: (1) `_approval.py` `cmd_tag_logic` — `shlex.quote()` 인자 escape, action 화이트리스트, 한글 경로 검증. (2) `core_reducer.py` `_execute_exec` — 14개 위험 명령어 블록리스트 + 한글 차단 + 30초 타임아웃. (3) `_execute_file` 신규 — 허용 경로 `/Users/bluesea/Applications/` 제한, symlink 검증, 100MB/50KB 제한. (4) 메모리 LRU 캐시 (Dict, 128개, 60s TTL) — Context Hash 1차 조회 + SQLite 폴백. (5) `test_core_reducer.py` 파이프라인 테스트 격리 DB(tempfile.mktemp)로 캐시 오염 방지. (6) `v9_0_완성_요약.md` — v9.0 개요/액션 현황/v9.1 계획 정리 문서 신규 생성. | handlers/_approval.py, modules/core_reducer.py, tests/test_core_reducer.py, v9_0_완성_요약.md | 완료 | 태그/실행/파일읽기 인젝션 취약점 사전 차단, SQLite 캐시가 테스트 결과를 오염시키는 문제 해결, v9.0 상태 문서로 정리
||||||||||| **🚀 2026-06-01 21:30** | **Hermes** | **ingest 키워드 5-8개 향상**: `_build_classify_prompt` keywords 지시를 "3-5개"→"5-8개"로 확장, "mix of general categories and specific terms" 문구 추가. 복잡도 0으로 태그 풍부도 향상. | modules/ingest_engine.py | 완료 | 더 정교한 태그 분류로 검색성 및 메타데이터 품질 향상 |
||||||||||| **🚀 2026-06-01 22:03** | **Hermes** | **논문_워크플로우_가이드 section 2.5 + HERMES3_ENCYCLOPEDIA.md 업데이트**: 분할 명령어 체계 철학 문서화, 워크플로우 흐름도/결정 포인트 표/통합 파이프라인 비교표 추가. | wiki/00_Meta/논문_워크플로우_가이드.md, wiki/00_Meta/HERMES3_ENCYCLOPEDIA.md | 완료 | 연구 워크플로우 결정 중심 설계 명문화 |
연구 워크플로우 결정 중심 설계 명문화 |
|| **🚀 2026-06-01 22:10** | **Hermes** | **arXiv 논문 4편 ingest → 40_Thesis 저장**: SemaClaw(하네스 엔지니어링), Selective QA(충돌 메모리), Control Arch(메모리 제어), AEvo(에이전틱 진화) — PDF 다운로드→텍스트 추출→분류→40_Thesis/.md 저장+Archive 백업. 각 문서에 tags/description/questions/brief frontmatter 포함. | wiki/40_Thesis/ | 완료 | 40_Thesis 최초 문서 4편 등록 |
|| **🚀 2026-06-01 22:30** | **Hermes** | **PermissionBridge 2-tier 도구 권한 게이트웨이 + Ingest 저신뢰 Inbox Deferral + ENCYCLOPEDIA.md 2항목 + GUIDE.md 9.2**: (1) `modules/permission_bridge.py` — 2-tier tool authorization. (2) `harness_agent.py` + `_base.py` — RUN_CMD/SAVE/파일작업 PermissionBridge 인가 경유. (3) `ingest_engine.py` — 저신뢰 deferral → `Inbox/*_pending.md` + `_process_inbox()`. (4) `GUIDE.md` changelog + 9.2. (5) `ENCYCLOPEDIA.md` 2항목. (6) meta 7종 업데이트. | modules/permission_bridge.py, handlers/_base.py, harness_agent.py, modules/ingest_engine.py | 완료 | 도구 사용 권한 계층화 + 저신뢰 콘텐츠 안전 분류 파이프라인 도입 |
||||| **🚀 2026-05-31 10:17** | **Hermes** | **harness_agent.py 버그 3종 핫픽스**: (1) `await executor.execute_bash_command()` 누락 수정 (→ coroutine 객체 방지), (2) NVIDIA(45s)/Gemma4(60s)/DeepSeek(45s) `asyncio.wait_for` 타임아웃 적용 (→ 무한 대기 방지), (3) LLM 호출 전 "🤔 생각 중..." 진행 피드백 메시지 전송 후 완료 시 삭제 (→ 2시간 침묵 해결). | harness_agent.py | 완료 | 2시간 침묵 + coroutine 객체 반환 문제 근본 해결 |
|||||| **🛠️ 2026-05-31 11:29** | **Hermes** | **_base.py try-except 폴백 추가**: `_reply_long()`/`_edit_or_send_long()`에 `parse_mode` 실패 시 자동 `None` 폴백 구현. Telegram "Can't parse entities" 에러 근본 해결. | handlers/_base.py | 완료 | `_call_local`, `__init__` 등 언더스코어 포함 AI 응답에서 Telegram Markdown 파서 깨짐 방지 |
|||||||| **🚀 2026-05-31 13:30** | **Hermes** | **/restart_bot import 버그 수정 + /run 제거**: `handlers/__init__.py`에 `cmd_restart_bot` import 누락 복구 (다른 AI가 추가한 코드 배포 누락). 제한적 화이트리스트 기반 `/run` 명령어 제거 → 자율 에러 복구형 `/exec`로 통일. | handlers/__init__.py, handlers/_system.py, hermes_local.py | 완료 | /exec가 이미 임의 명령 실행 지원, /run은 불필요한 중복 + 제한적이라 부적합 |
||||||| **🚀 2026-06-01 14:00** | **Hermes** | **Hermes2 클라우드 전용 개편 — gemma4 모드 제거 + NVIDIA NIM 70B 폴백 + 게이트웨이 의존 제거**: DeepSeek 기본, NVIDIA 폴백 체계로 단순화. `GATEWAY_URL` 상수 제거, NVIDIA_API_KEY `.env`+plist 등록. 하이브리드 라우터 불필요. Hermes2 CPU 0%, RSS 73MB로 안정화. 관련 메타 문서 4종 업데이트. | hermes2_bot.py, ai.hermes2.bot.plist, .env | 완료 | 로컬 의존 제거로 재시작 루프 가능성 차단, 모드 전환 로직 단순화 |
||||||||| **🚀 2026-06-01 14:02** | **Hermes** | **post_init 콜백 stdout 리다이렉션 문제 수정**: `post_init()` 콜백 함수 제거, `main()` 내 `start_polling()` 직후 직접 print 2줄 (`🚀 시작 완료` + `📁 Vault 경로`)로 이동. 로그 정상 출력 확인 (Hermes2 PID 93376). | hermes2_bot.py | 완료 | Telegram PTB post_init 콜백 stdout이 launchd 로그로 리다이렉션되지 않는 문제 근본 해결 |
||||||| **🚀 2026-06-01 14:02** | **Hermes** | **HERMES3_GUIDE v9.1.5 업데이트 + V9.0 추가 2 제안 검토**: Phase 6(Hermes2 Cloud-Only) 추가, v9.4+ 중장기 계획 섹션 신설. V9.0 추가 2.md(NVIDIA SkillSpector/Anthropic CyberAuditor/OpenSRE SRE Investigator — 설계 제안 단계, 미구현) 검토 완료. 00_Meta_지도/스크립트 정보/주요 시스템 가이드/시스템 인벤토리/시스템 상태 5종 메타 문서 일괄 업데이트. | HERMES3_MASTER_DEVELOPMENT_GUIDE.md, V9.0 프로그램 추가 2.md, 5종 메타 문서 | 완료 | V9.0 추가 2 제안 반영 — 미래 모듈 설계는 기록하되 코드 미구현 상태 명시 |
||||| **🚀 2026-05-27 17:40** | **Hermes** | **memory.json 사망 확인 + harness_memory.json 문서 보강**...
|||| **🚀 2026-05-28 15:00** | **Hermes** | **Dreaming/Ingest/Recent/DummyRouter 버그픽스**: `_memory.py` Dreaming 3종 (MemoryEngine import 누락, DummyRouter 우회, effective_message 버튼 콜백), `_file.py` Ingest 2종 (IngestEngine 인자+process_gardening) + Recent 1종 (get_recent_context), `hermes_local.py` DummyRouter 전 서브모듈 패치. 모든 핸들러 복구 완료, 봇 재시작 (PID 81010). | handlers/_memory.py, handlers/_file.py, hermes_local.py | 완료 | - |
|||| **🚀 2026-05-27 12:56** | **Hermes** | **/ingest v2.2 — 모드 중립 분류 라우팅**: `_file.py` `cmd_ingest`가 DeepSeek 고정 `_call_llm` 대신 사용자 현재 모드(Gemma4/DeepSeek/NVIDIA)를 따르는 `_ingest_llm_wrapper`를 사용하도록 변경. `ingest_engine.py` 중복 `_build_classify_prompt` 제거(코드 정리). | handlers/_base.py, handlers/_file.py, modules/ingest_engine.py | 완료 | - |
|||| **🚀 2026-05-27 12:50** | **Hermes** | **/ingest v2.1 — 루트 파일 분류 이동 완료**: `_process_root_files()` 전면 개편: LLM 분류(DeepSeek) 후 4개 폴더(10_AI_Automation/20_Research/30_Journal/Unsorted)로 이동 + frontmatter(category 태그) 업데이트. `_build_tag_prompt` 제거(dead code), `_build_classify_prompt` 활용. 루트 26개 .md 파일 전량 처리 완료. | ingest_engine.py, handlers/_file.py | 완료 | - |
|||| **🚀 2026-05-26 17:10** | **Hermes** | **#018 max_tokens underflow (harness_agent.py 1500→3500)**: Gemma4 응답이 1500토큰에서 강제 종료되는 버그 발견. `harness_agent.py:248` max_tokens=1500→3500. Hermes1 재시작 (PID 88574). 장애 이력 #018 등록. hot.md/시스템 상태.md 업데이트 완료. | harness_agent.py, 장애 기록/헤르메스_초기통합_장애_이력.md, hot.md | 완료 | - |
|||| **🚀 2026-05-26 15:00** | **Hermes** | **#7 ~/.hermes/ 재정비 완료 — governance/ + runtime/ 이분화 + 중첩 버그 수정**: `governance/`(헌법/SOUL/스킬) + `runtime/`(DB/로그/캐시/크론/훅) 구조로 분리. 모든 원래 경로에 하위호환 symlink 유지. `mv` 시 `runtime/x/x/` 중첩 생성 버그 7건(cron/logs/hooks/plugins/cache/audio_cache/image_cache) 수정. 23개 항목 전부 symlink 검증 완료. `스크립트 정보.md` 심볼릭 테이블 전면 갱신. Hermes1 정상 동작 (PID 83522). | ~/.hermes/governance/, ~/.hermes/runtime/, 스크립트 정보.md | 완료 | - |
||| **2026-05-24 15:00** | **Antigravity** | **파일 통합 및 하이브리드 라우팅 개편**: 분산되어 있던 `hermes_local.py` 등 모든 핵심 스크립트를 `Mjauto/Scripts/`로 일원화. `com.hermes.bot` plist로 자동재시작 등록 및 `hybrid_router.py`(DeepSeek/로컬 라우팅), True Lazy Loading 적용. LM Studio 삭제 → llama.cpp Qwen2.5-14B-Instruct GGUF(Q4_K_S)로 전환. | Scripts/, com.hermes.bot.plist | 완료 | - |
||| **2026-05-19 12:50** | **Antigravity** | **자율 파일 관리 및 인라인 승인 UI 탑재**: LLM Tool Calling(`create`, `move`, `delete`, `list_directory`) 추가 및 파괴적 작업을 위한 텔레그램 버튼 승인 시스템(CallbackQueryHandler) 구축. | hermes_local.py, verification_engine.py | 완료 | - |
||| **2026-05-19 09:45** | **Antigravity** | **Harness V2.5 전격 기동 및 아키텍처 확정**: Meta CoVe(검증사슬) RAG Runtimes, 자율 에러 복구형 Bash Executor(최대 3회), 프리미엄 터치 메뉴 키보드 UI 배포. | hermes_local.py, verification_engine.py, executor.py | 완료 | - |
||| **2026-05-19 09:27** | **Antigravity** | **헤르메스 단일 관제 체제 전환**: Mjobsidian 및 sys.path 연동, llama.cpp + Qwen2.5-14B-Instruct Q4 적용, 타 에이전트 비활성화 | wiki/00_Meta/, hermes_local.py | 완료 | - |
||| **2026-05-26 09:49** | **Antigravity** | **Gateway → Hermes2 Bot 비교실험 전환 완료**: Gateway Telegram 비활성화로 충돌 해결, Gateway → Bot tool calling 파이프라인 복원 | config.yaml, hermes2_bot.py | 완료 | - |
||| **🚀 2026-05-26 14:20** | **Hermes** | ~~Dreaming → Skill Evolver 자동 연결~~ **[Dreaming 폐기됨]** | dream_scheduler.py, bio_memory_engine.py, skill_evolver.py | 완료 | - |
||| **2026-05-26 17:45** | **Hermes** | **[Phase 3] 4개 연구 SKILL.md 생성 + /paper humanize 구현 완료**: literature-review, citation-formatter, abstract-writer, argument-mapper SKILL.md 생성. /paper humanize (법률/학술 문체 변환, DeepSeek LLM) 명령어 추가. ✍️ 논문 버튼 하단 키보드 추가. `/chart`는 MJ님 요청으로 삭제. hot.md/스크립트 정보.md 업데이트. | handlers/_research.py, hermes_local.py, ~/.hermes/skills/research/*/SKILL.md | 완료 | - |
||| **🚀 2026-05-28 15:30** | **Hermes** | **#10 /paper draft (논문 초안 생성) 구현 완료**: 주제 입력 → 개요 생성 → 본문 초안 → 문체 변환(humanize) 3단계 LLM 파이프라인 구현. 4096자 초과 시 개요/본문 분할 전송. `/paper [subcmd]` 사용법 업데이트, 도움말 텍스트 갱신. | handlers/_research.py, 스크립트 정보.md, 시스템 상태.md, 시스템 인벤토리.md | 완료 | - |
||| **2026-05-26 09:54** | **Hermes** | **`hermes_handlers.py` → `handlers/` 패키지 리팩토링 완료**: 단일 60KB 파일(20+개 함수)을 기능별 7개 서브모듈로 분리. `_base.py` lazy import로 순환참조 방지. 기존 동작 100% 유지 검증 완료. hermes_local.py import 1줄만 변경. | handlers/, hermes_local.py | 완료 | - |
|||| **🚀 2026-05-28 18:00** | **Hermes** | **#12~#14 완료**: ~~#14 dream_scheduler ExecPlan (phase 격리+요약)~~ **[Dreaming 폐기됨]** | constitution.local.md, .git/hooks/pre-commit, dream_scheduler.py | 완료 | - |
|||| **🚀 2026-05-26 17:19** | **Hermes** | **#020 constitution.local v1.2 인라인 전환 완료**: 포인터 패턴(§2/§3/§6/나의 비서 가이드.md 참조) → 모든 참조를 실제 내용으로 인라인 대체. 98→204줄. constitution.md §4(오류복구)/§5(메모리)/§7(에이전트패턴)도 통합 병합. Gemma4가 단일 문서만 읽어도 모든 규칙 파악 가능. | constitution.local.md | 완료 | - |
|||| **🚀 2026-05-28 19:30** | **Hermes** | ~~#14 보완 — style_profile → dreaming 연결 가시화~~ **[Dreaming 폐기됨]** | dream_scheduler.py | 완료 | - |
|||| **🚀 2026-05-28 20:15** | **Hermes** | ~~#14 보완 — Phase 3 style_profile 감사 포함~~ **[Dreaming 폐기됨]** | dream_scheduler.py | 완료 | - |
|| **2026-05-26 10:07** | **Hermes** | **레거시 파일 _archive/ 이동 + `devops/single-file-to-package-refactor` 스킬 저장** | _archive/, ~/.hermes/skills/devops/ | 완료 | `mac_bot_v2.py`(Scripts/MacBot/), `start_bots.sh`, `hermes_handlers_backup_20260526.py`(원본 .bak) → `Scripts/_archive/`로 이관. 영향도 0% 확인 완료 (LaunchAgents 미참조, 모든 Bot Disabled).
||| **2026.05.26** | **Antigravity** | **자율 터미널 진단 지능 부여 및 OOM 복구**: `harness_agent.py` 프롬프트에 `[RUN_CMD:]` 태그를 부여하여 사용자가 지시 시 수동 답변 대신 봇 스스로 진단하도록 업그레이드. 390MB OOM으로 인한 딥시크 폴백 원인을 캐시 삭제 및 재시동으로 복구. | harness_agent.py | 완료 | - |
||| **2026.05.26** | **Antigravity** | **파일 조작 절대 경로 인식 버그(404) 수정**: `hermes_local.py`의 `secure_path` 함수에서 절대경로 입력 시 Base 경로가 중복 결합되는 오류 완전 해결. | hermes_local.py | 완료 | - |
||| **2026.05.25** | **Antigravity** | ~~/goal 명령어 및 야간 스케줄러(dream_scheduler.py) 헌법 감사 자동화 기능 탑재~~ **[Dreaming 폐기됨]**, LLM 400 포맷 불일치 오류 수정, Git 동기화 오작동 수정. | goal_manager.py, dream_scheduler.py | 완료 | - |
||| **2026.05.25** | **Antigravity** | **인라인 UI 기반의 /kanban (보드) 기능 구현 완료** 및 hot.md 통합, 텔레그램 연동 상호작용 강화. | kanban_manager.py, hot.md | 완료 | - |
||| **2026.05.25** | **Antigravity** | **타임존 불일치 문제 해결(KST 적용) 및 시스템 상태 메타 문서 체계 정비**. | file_manager.py, config.py | 완료 | - |
||| **2026-05-26 08:52** | **Hermes** | **[Phase 2] [7]~[10] 구현 완료**: cmd_delegate(Task 분리), cmd_audit(포트/SSH/키 감사), cmd_cove devil(반론 생성), cmd_secreview(보안 코드 리뷰), 검색 백엔드 교체(DDGS/SearXNG+Firecrawl) | hermes_handlers.py, hermes_local.py, audit_engine.py, web_agent_module.py | 완료 | - |
||| **2026-05-26 11:10** | **Hermes** | **brew upgrade 경로 불일치 수정 + executor import 누락 패치**: llama-server plist 바이너리 경로를 `/opt/homebrew/bin/llama-server`(symlink)로 변경하여 brew upgrade 시 깨지지 않도록 근본 해결. `harness_agent.py`에 `import executor` 추가하여 `[RUN_CMD:]` NameError 해결. Hermes1 봇 재시작 완료 (PID 78042). | com.bluesea.llama_server2.plist, harness_agent.py | 완료 | - |
|||| **2026-05-27 09:10** | **Hermes** | **Python 3.10 호환성 수정 + 테스트 인프라 구축 (87 passed)**: `dialectic_layer/_dreamer.py` + `hermes_context_builder.py` 상단에 `from __future__ import annotations` 추가 → Python 3.9.6에서도 `str|None` 문법 정상 동작. handlers/ 9개 서브모듈 import 성공. pytest 8.4.2 설치, tests/ 디렉토리 및 6개 테스트 파일 생성 (imports/bio/skill/kanban/router). **87 passed, 0 skipped, 0 failed**. 기존 handler version skip 조건 제거. | dialectic_layer/_dreamer.py, hermes_context_builder.py, tests/*.py, hot.md | 완료 | - |
|||| **🚀 2026-05-27 11:20** | **Hermes** | **/ingest 전면 개편 + 태그 기반 전환 + 링크 폐지 + 루트 정리**: 텔레그램 ingest 버튼 하나로 Clippings(분류 이동+태그+description) + 루트 파일(태그만, 이동 없음) 동시 처리 통합. WikiLinker.link_text() 비활성화 (링크 사용 폐지, 태그 기반 시스템 전환). `ingest_engine.py` 전면 재작성 — one-shot 예시 LLM 프롬프트, 엄격한 JSON 파싱(Gemma4 대응), 기존 frontmatter 병합, scanned 중복 skip, 제외 로직 불필요(이미 메타폴더로 이동 완료). `file_manager.write_file(update_timestamp=False)` 옵션 추가 (루트 scan 타임스탬프 보존용). 동시에 루트 방치 파일 정리: SCHEMA.md→wiki/00_Meta/, **Deepseek**.md→wiki/00_Meta/Deepseek_API_Keys.md, 리다이렉트 3개→wiki/99_Archive/Backup_20260520/ | ingest_engine.py, file_manager.py, wiki_linker.py, SCHEMA.md, Deepseek_API_Keys.md, 3개 리다이렉트 | 완료 | - |
|| **2026-05-28 21:00** | **Hermes** | **[Group 1] 4개 구현 완료 — /status KEY=VALUE, 시스템 상태.md 이유 컬럼, /paper review, ADR 템플릿**: (1) `/status KEY=VALUE` — KEY=VALUE 파싱 + hot.md KV 섹션 쓰기/읽기/리셋 기능, (2) 시스템 상태.md 변경 이력 테이블에 `| 이유 |` 컬럼 추가 (기존 5→6컬럼, 과거 행 `-`), (3) `/paper review [문서/텍스트]` — 구조/논증/용어/인용/개선 6축 학술 검토 파이프라인, (4) ADR 템플릿 `docs/adr/ADR-0000-template.md` 생성 (배경/결정/이유/대안/영향). `handlers/_system.py`, `handlers/_research.py`, `시스템 상태.md` | handlers/_system.py, handlers/_research.py, 시스템 상태.md, docs/adr/ADR-0000-template.md | 완료 | 핫키-값 설정 필요성, 결정 투명성, 논문 검토 워크플로, 구조적 의사결정 기록 |
||||| **🚀 2026-05-28 21:30** | **Hermes** | **[Group 2] 2개 구현 완료 — /vault check, /grill-with-docs**: (1) `/vault check` — Vault 정합성 종합 진단 핸들러 (`_vault.py`). 캐시/찌꺼기 검사, 프론트매터 스키마 검증, 중복 문서 TF-IDF 스캔 (기존 vault_scanner 연동). `🔍 보관함 진단` 버튼 추가 (ℹ️ 도움말 대체). (2) `/grill [문서] [질문]` — Vault 문서 읽고 LLM Q&A (`_grill.py`). 키워드/FUZZY 검색, 대용량 문서 15K자 청킹. `handlers/_vault.py`, `handlers/_grill.py` 신규 생성 (handlers/ 9개 서브모듈). | handlers/_vault.py, handlers/_grill.py, hermes_local.py, handlers/_base.py, modules/vault_scanner.py | 완료 | Vault 건강 모니터링, 문서 기반 대화 필요성 |
||||| ~~**🚀 2026-05-27 17:27**~~ | ~~**Hermes**~~ | ~~**[A] 경고 정리 + [C] 주간 펄스 리포트 + [B] style_profile 일반 명령어 주입 완료**~~: ~~A: `hermes_local.py` warnings.filterwarnings (DeprecationWarning, UserWarning 무시). C: `weekly_pulse.py` 생성 (칸반/로그/Git/리소스 수집→ `주간_펄스_리포트.md`), `com.bluesea.weekly_pulse.plist` launchd 등록 (매주 일요일 23:00). B: `_base.py`에 `_get_style_profile()` 캐시 함수 추가 → `_call_llm()` + `harness_agent.py handle_message()` 시스템 프롬프트에 stop-slop+문체 규칙 주입. 메타 문서 5종 전면 업데이트.~~ **[2026-05-27 weekly_pulse 폐기됨]** | hermes_local.py, handlers/_base.py, harness_agent.py, weekly_pulse.py, com.bluesea.weekly_pulse.plist | 완료 | AI 냄새 제거, 주간 모니터링, 출력 스타일 일관성 |
|||||| ~~**🚀 2026-05-27 17:50**~~ | ~~**Hermes**~~ | ~~**Memory_시스템_설계 → 메모리_파일_명세서 통합 + archive/ 폴더 신설**~~: ~~MJ님의 구 설계 문서 `(Hot.md파일관련)Memory_시스템_설계.md`(3단계 구조·Dreaming 프로세스·핵심 원칙)를 신규 `메모리_파일_명세서.md`(JSON 전수조사 명세)에 병합 → 하나의 통합 문서 완성. 구 파일은 `wiki/00_Meta/archive/`로 이동. `00_Meta_지도.md` v6.9: 구 링크 제거·중복줄 제거·archive/ 폴더 섹션 추가·푸터 갱신.~~ **[2026-05-27 메모리_파일_명세서 archive/99_Archive/로 이관 — Dreaming 폐기로 정적화]** |
|||||| **🚀 2026-06-02 22:00** | **Hermes** | **메모리_파일_명세서.md + memory.md + 비서_시스템_메타_문서_비교_정의.md 정리**: 메모리_파일_명세서.md(99_Archive) 내 memory.md 참조 10곳 전부 취소선 처리 + 상단 아카이브 배너 추가. 비서_시스템_메타_문서_비교_정의.md memory.md 행 취소선 + 99_Archive 이관 명시. memory.md(99_Archive) 상단 아카이브 배너 추가. | 메모리_파일_명세서.md, 비서_시스템_메타_문서_비교_정의.md, memory.md | 완료 | Dreaming 폐기 후속 정리 — memory.md 동결 확인 |
|||||| **🚀 2026-05-27 18:55** | **Hermes** | **메모리_파일_명세서.md 99_Archive→00_Meta 원복 (현역 복귀)**: 사용자 지시로 메모리_파일_명세서.md는 살리고 memory.md만 archive 보낸다는 의도 확인. 메모리_파일_명세서.md 99_Archive→00_Meta 복사, 상단 아카이브 배너 제거 (memory.md 참고 문구로 대체). 00_Meta_지도.md 취소선 해제 + 푸터 갱신. memory.md 파일은 99_Archive 유지. | 메모리_파일_명세서.md, 00_Meta_지도.md | 완료 | 메모리_파일_명세서는 현역 문서 — memory.md만 archive |
||||||| **🚀 2026-05-27 22:40** | **Hermes** | **구조적 결함 3종 수정 + 재발 방지 인프라**: (1) Zombie Poller — `app.run_polling()` → 직접 `updater.running` 모니터링 + `sys.exit(0)` → launchd 재시작. (2) 에러 침묵 — asyncio ERROR 레벨 격상, bare except→로깅 교체, PYTHONUNBUFFERED + `-u`. (3) Gemma4 GGUF chat template — `chat.completions.create()` → `completions.create()` + 수동 포맷. asyncio 예외 핸들러 등록, 60s watchdog. 분석 문서 `시스템_구조적_결함_분석.md` 신규 생성. | hermes_local.py, harness_agent.py, com.hermes.bot.plist, 스킬 ref, wiki/00_Meta/시스템_구조적_결함_분석.md | 완료 | 7분 주기 zombie poller 3회 재현 — 근본 원인 3중 결함 발견 |
||||||| **🚀 2026-05-28 21:58** | **Hermes** | **Hermes v8.1 전면 구현 완료 — Self-Healing Loop + Live Sync + Cron 등록 + 전면 코드 정리**: (1) `modules/dream_scheduler.py` v1.0 4-phase ExecPlan (진단→수정→검증→보고) 구현, 매일 새벽 3시 cron 등록 완료 (job_id: cb8d3090df6b), (2) `fswatch_daemon.py` + `update_index.py` 기반 Live Sync 실시간 인덱싱 (5개 핵심 디렉터리 감시 → hermes_index.db 갱신), (3) pytest 89/89 전부 통과 (conftest.py `telegram.constants` mock 보강), (4) `handlers/_base.py` `import time` 누락 수정 (런타임 NameError 방지), (5) dream_scheduler/fswatch/update_index 하드코딩 절대경로 전부 `Path.home()` 기반으로 개선. (6) stray `hermes_index.db` (0바이트) 삭제 + DB 테스트 엔트리 정리. | modules/dream_scheduler.py, fswatch_daemon.py, update_index.py, tests/conftest.py, handlers/_base.py, 스크립트 정보.md, 시스템 상태.md, 시스템 인벤토리.md, 주요 시스템 가이드 및 FAQ.md | 완료 | 자율 진단→수정→검증 루프, 실시간 파일 감시, 파이프라인 전면 자동화 |\n    64|    61|||||||| **🚀 2026-06-01 19:50** | **Hermes** | **v9.2 SIA 피드백 학습 + 모니터링 엔진 + 멀티 모델 로드 밸런싱**: (1) `modules/sia_engine.py` SelfImprovingAgent — 피드백 기록(평점/맥락) → 추세 분석 → LLM 개선 제안. (2) `modules/monitoring_engine.py` MonitoringEngine — 액션별 메트릭 기록, 에러율 추적, 3축 임계 경보. (3) `modules/load_balancer.py` ModelLoadBalancer — 성능 가중치 기반 모델 선택/기록/재조정. (4) `core_reducer.py` SIA/Monitoring 통합. (5) `hybrid_router.py` 로드밸런서 연동. (6) 신규 테스트 3파일 (34개) — 격리 DB. **전체 pytest 150/150 ✅ 전면 통과**. | modules/sia_engine.py, modules/monitoring_engine.py, modules/load_balancer.py, core_reducer.py, hybrid_router.py, tests/test_sia_engine.py, tests/test_monitoring_engine.py, tests/test_load_balancer.py | 완료 | Self-Improving Architecture(SIA) 3축 — 학습→모니터링→밸런싱 피드백 루프 구축 |
|---

## 🔬 아키텍처 중대 결정: Harness V2.5 vs V3.0 (Codex-Maxxing)
박사님이 제안하신 Codex-Maxxing 및 Self-Evolving Skills(자율 스킬 진화) 루프의 실현 가능성을 전격적으로 기술 검토하여 장기 안전 표준을 수립했습니다.

### ❌ V3.0 (자율 진화 루프) 보류/폐기 사유
1. **로컬 모델(Qwen2.5-14B-Instruct Q4)의 한계와 프롬프트 표류(Prompt Drift):** 자율 스킬북(`SKILL.md`) 작성 시 문법 파싱 에러나 지시 왜곡이 누적되어 장기적인 지능 저하 초래.
2. **무한 자가 오염 루프:** 잘못된 해결책이 영구 스킬북에 잘못 기재될 시, 인간(박사님)의 수동 수선 없이 스스로 오류에 갇힘.
3. **물리적 레이턴시 병목:** 전처리/Manifest 매칭 로직으로 인해 텔레그램 응답 시간이 지나치게 지연됨.

### ✅ Harness V2.5 (통제된 고성능 아키텍처) 채택 사유 (현 시스템 상태)
- **결정적 스킬 안정성:** 개발자인 박사님과 Antigravity가 설계한 철저한 정적 코드(`verification_engine.py`, `executor.py`)만을 수행하여 100% 예측 가능하고 안전함.
- **실시간 단기 복구:** 에러 대처를 시스템 설정 영구 변조가 아닌 **'단기 실행(Runtime) 중 최대 3회 재수행'**만으로 격리하여 프롬프트 정화성 유지.
- **최상의 응답 속도:** 최상의 다이어트 상태로 Qwen2.5-14B-Instruct(Q4_K_S)의 추론 성능을 있는 그대로 전개.

---

## 💡 주요 시스템 가이드 및 문제 해결 (FAQ & Changelog)
시스템 오류 해결 내역, 팁 및 자세한 운영 가이드는 아래 문서를 참고하십시오.
👉 **주요 시스템 가이드 및 FAQ** 문서 바로가기

---

## 📦 현재 설치된 주요 프로그램/환경

### AI 에이전트
- **Hermes Bot (헤르메스)**: **Running** (LaunchAgent: `com.hermes.bot`)
 - **위치**: `Applications/Mjauto/Scripts/` (v5.0 통합 디렉토리)
 - **메인**: `hermes_local.py` 및 `harness_agent.py` → **llama.cpp (Qwen2.5-14B-Instruct Q4_K_S, port 8080) / DeepSeek API** (Hybrid Router 연동)
 - **스킬**: `~/.hermes/skills/` (brain/cognitive, knowledge/wiki, devops/executor, devops/single-file-to-package-refactor, cove_verifier, **research/** — literature-review, citation-formatter, abstract-writer, argument-mapper)
 - **보안 설정**: `~/.hermes/.env` 분리 격리 연동
 - **기능**: `handlers/` 패키지 (9개 서브모듈: `_base`/`_memory`/`_system`/`_file`/`_kanban`/`_research`/`_ui`/`_vault`/`_grill`) 기반 명령어 처리, 자율 파일 관리(Tool Calling 및 인라인 승인 버튼), `/ask` (CoVe 팩트 RAG), `/exec` (자율 에러 복구형 Bash), `/restart_bot` (봇 재시작), `/recent` 및 `/status` 신규 연동, `/delegate` (Task 분리), `/audit` (인프라 감사), `/secreview` (보안 코드 리뷰), `/cove devil` (반론 생성), **`/paper humanize/draft/review` (법률/학술 문체 변환·초안·검토), `/vault check`, `/grill`**
- **Harness Bot / Antigravity Bot / MacBot**: **Disabled (당분간 미사용)**

### 로컬 LLM
- **llama.cpp (llama-server)**: Running (Port 8080)
 - **탑재 모델**: **Qwen2.5-14B-Instruct Q4_K_S (GGUF)**
 - **Launchd 레이블**: `com.bluesea.llama_server2`

---

## Profile Switching 및 명령어 요약 (헤르메스 V2.5 프리미엄 UI)

| 버튼 / 명령어 | 동작 | 설명 |
| :--- | :--- | :--- |
| **🛡️ CoVe 질문하기** (`/ask`) | 위키 교차 팩트체크 기반 답변 | 1. Baseline 작성 ➔ 2. 질문 분할 ➔ 3. 위키 RAG ➔ 4. 최종 정합성 보정 |
| **📁 보관소 최근 문서** (`/recent`) | `Mjobsidian` 최근 수정 5개 조회 | 최근 위키 요약 및 업데이트 상황 즉시 모니터링 |
|| **⚙️ 시스템 상태** (`/status [KEY=VALUE...]`) | 시스템 건강 + 핫토픽 표시 | `KEY=VALUE`로 hot.md 실시간 상태 기록, `reset`으로 초기화 |
| **ℹ️ 명령어 도움말** (`/help`) | 전체 명령어 가이드 확인 | 파일 제어(`/create`, `/read`, `/move`, `/copy`) 등 관리 매뉴얼 |
| **자율 Bash 명령어** (`/exec`) | 복구 retries가 가동되는 bash 실행 | 에러(stderr) 발생 시 Gemma4가 최대 3회까지 자율 수정 후 재도전 |
||| **✍️ 논문 작업** (`/paper humanize`, `/paper draft`, `/paper review`) | 법률/학술 문체 변환 + 논문 초안 생성 + 학술 검토 (DeepSeek LLM) | humanize: 학술지 스타일 변환 / draft: 개요→초안→문체 3단계 / review: 구조·논증·용어·인용 6축 검토 |
|| **📦 보관함 진단** (`/vault check`, `/vault duplicates`) | Vault 정합성 + 중복 문서 스캔 | 캐시/프론트매터/중복 TF-IDF 분석. 하단 버튼 `🔍 보관함 진단`으로 실행 |
|| **🔥 문서 기반 대화** (`/grill [문서] [질문]`) | Vault 문서 읽고 LLM Q&A | 키워드 검색, 대용량 문서 청킹, DeepSeek LLM 기반 답변 |

---

*최종 업데이트: 2026-06-24 22:17*
*작성자: Hermes*

### [2026-06-02] GUIDE.md 정리 + Gemma4 최적화 방안 + ENCYCLOPEDIA.md Graphify 문서화 + hot.md 업데이트

1. **GUIDE.md 삭제 대상 목록 정리 완료**: GUIDE.md의 `# 삭제할 것` 섹션에 있던 10개 파일 목록 전부 존재하지 않음 확인 (이미 삭제 완료). 해당 섹션을 "삭제 완료" 상태 메모로 대체.
2. **GUIDE.md MEDIUM 3 (해야할 일) 제거**: "v9.0 삭제 대상 파일들" 3개 행 제거. 해당 파일들이 이미 존재하지 않으므로 작업 필요 없음.
3. **GUIDE.md Qwen2.5-14B-Instruct 성능 최적화 방안 섹션 신설**: v9.4+ 섹션 이전에 배치. Qwen2.5-14B-Instruct에 특화된 4가지 최적화 방안 제시 (KV 캐시 공유, Speculative Decoding, Quantization Profile, Attention Sink Tuning).
4. **GUIDE.md 업데이트 이력 추가**: 2026-06-02 항목 — Guide 청소 + Gemma4 최적화 방안.
5. **graphifyy 패키지 설치 및 검증 완료**: `pip install graphifyy` (version 0.8.28). `import graphify` 확인. API: `extract()`, `build_from_json()`, `god_nodes()`, `to_json()`, `to_html()`. 테스트 파일 생성→삭제 완료.
6. **불필요 파일 정리 완료**: `_test_graphify.py`, `_check_graphify.py` 삭제 완료. vault 루트 graphify 출력 파일(graph.json, graph.html, GRAPH_REPORT.md, `.graphify_*`, `_graphify_chunk_*`)은 기존 출력물로 확인, 미삭제.
7. **ingest_engine.py Unsorted Deferral 경고 확인**: `_process_inbox()`에 `ingest_report["total"] > 0 and ingest_report["unsorted_ratio"] >= 0.30` 조건의 경고 메시지 로직 이미 존재. 자체 확인 완료 (추가 구현 불필요).
8. **_vault.py graphify 연동 확인**: `/vault graph` 명령어(`_vault_graph()` 함수)에 graphify 연동 완료 — `g_extract()`, `build_from_json()`, `god_nodes()`, `surprising_connections()` 모두 import되어 있음. 별도 구현 불필요.
9. **ENCYCLOPEDIA.md Graphify 항목 문서화 완료**: Graphify(지식 그래프) 섹션 신설 — `/vault graph` 명령어 설명, 동작 파이프라인, 특징, 의존성 명시. 업데이트 이력에도 2026-06-02 Graphify 연동 추가 등록.

---

## 2026-06-11 논문 5편 기반 Hermes 강화 (2606.11686/88/10949/11680/11671)

### 수행된 작업
| 항목 | 파일 | 논문 |
|------|------|------|
| Goal-Autopilot 검증 게이트 | `modules/agentic_loop.py` | 2606.11688 |
| Sycophancy 아첨 차단 필터 | `modules/memory_refinement.py` | 2606.10949 |
| HORMA 계층 클러스터 검색 | `modules/memory_refinement.py` | 2606.11680 |
| Layer-Isolated 하네스 (19개 테스트) | `tests/test_layer_harness.py` (신규) | 2606.11686 |
| Runtime Skill Auditor | `modules/skill_auditor.py` (신규) | 2606.11671 |

### 연결 구조
```
agentic_loop.py
  └─ _verify_gate() ← RUN_CMD/CREATE 실행 후 호출
       └─ 실패 감지 시 VerificationGate 경고 → 거짓 완료 보고 차단

memory_refinement.py
  ├─ should_store() ← Sycophancy 필터 추가 (아첨 패턴 80자 미만 차단)
  └─ hybrid_recall() ← HORMA 계층 검색 (context_tags 클러스터 → 세부 검색)

tests/test_layer_harness.py ← LLM 없이 1.31초, 19개 결정론적 테스트
modules/skill_auditor.py    ← ~/.hermes/skills/ 동적 감사, 위험도 3단계 분류
```

### 에러 진단 포인트
- VerificationGate 오탐 → `_FAILURE_SIGNALS` 리스트에서 해당 키워드 제거
- HORMA 클러스터 미매칭 → `context_tags` 없는 에피소드는 전체 fallback으로 자동 처리
- SkillAudit HIGH 감지 → `skill_audit.log`에서 상세 패턴 확인

---

## 2026-06-11 Architect Loop 워크플로우 도입

### 수행된 작업
| 항목 | 상태 | 비고 |
|------|------|------|
| `wiki/00_Meta/HANDOFF.md` 신규 생성 | ✅ 완료 | Architect↔Builder 상태 공유 파일. 슬라이스 스펙·결과·이견·결정 로그 포함 |
| `harness_agent.py` 응답 품질 원칙 추가 | ✅ 완료 | 규칙 #11 — 핵심 먼저·증거 기반·질문 시 분석만·재논의 금지 |
| `CLAUDE.md` 응답 품질 원칙 추가 | ✅ 완료 | Claude Code 세션에 자동 적용 |
| `자동화_시스템_사용법.md` §13 신규 | ✅ 완료 | Architect Loop 역할 분담·세션 흐름·5대 원칙·프롬프트 템플릿 |

### 연결 구조
```
Claude Code (Architect) ←→ HANDOFF.md ←→ DeepSeek WebUI (Builder)
                                ↑
                        Gemini (Reviewer, 선택적)
                        Perplexity (Research)
```

### 에러 진단 포인트
- Builder가 HANDOFF.md "현재 슬라이스"를 읽지 않고 시작 → 세션 시작 프롬프트에 `docs/HANDOFF.md를 읽어라` 명시
- 결과에 해석·주관 포함 → "raw 결과·숫자만" 규칙 재주지

---

## 2026-06-10 Self-Harness 3개 아티클 분석 및 시스템 강화

### 수행된 작업
| 항목 | 상태 | 비고 |
|------|------|------|
| Wiki Git 초기화 | ✅ 완료 | `wiki/.git` — 306개 파일 최초 커밋, `.gitignore` 포함 |
| SessionStart Hook 설정 | ✅ 완료 | `~/.claude/settings.json` — 세션 시작 시 `01_hot.md` 자동 주입 |
| Wiki 자동 lint 스케줄 | ✅ 완료 | 매일 새벽 3:17 — 미커밋/임시파일 감지 → 텔레그램 알림 |
| WeaknessMiner 모듈 신설 | ✅ 완료 | `modules/weakness_miner.py` — 반복 실패 패턴 자동 감지 + 06_에러보고서 자동 기록 |
| `_stock.py` Weakness Mining 연결 | ✅ 완료 | cmd_stock/cmd_scan/cmd_chart except 블록 → record_failure() 자동 호출 |

### 이론적 근거
- **Self-Harness 논문** (Shanghai AI Lab, 2026.06): Weakness Mining → Harness Proposal → Validation 3단계
- **Lucas Obsidian Second Brain**: Git 버전관리, SessionStart Hook, 자동 lint
- **Self-Correction Loops**: External Verifier (선택적 적용), Memory (기존 파일로 커버)

### 신규 파일
- `Scripts/modules/weakness_miner.py` (모듈 80개)
- `wiki/00_Meta/session_start_hook.sh`
- `wiki/.gitignore`

### 에러 진단 포인트
- WeaknessMiner DB: `~/.hermes/runtime/weakness.db`
- SessionStart Hook 미동작: `~/.claude/settings.json` → hooks.SessionStart 확인
- GitHub Remote 미연결: `git -C wiki remote -v` 확인

---

## 2026-06-03 시스템 상태 업데이트

### 수행된 작업
| 항목 | 상태 | 비고 |
|------|------|------|
| WebUI 초기화 프로토콜(Initialization Protocol) 신설 | ✅ 완료 | `USER.md` 및 FAQ에 시작 시 필수 헌법 스캔 규칙 문서화 |
| 세션 워크스페이스 망각 장애 해결 및 헌법 1.7 개정 | ✅ 완료 | `06_에이전트_오류_및_재발방지_보고서.md` 작성 및 다중 경로 바인딩(`constitution.local.md`) 추가 |
| `wiki_auto_stamper.py` 기능 확장 | ✅ 완료 | 태그 최대 8개 자동 조율 및 링크 텍스트화 |
| `지식 베이스 사용 가이드` 깨진 링크 정리 | ✅ 완료 | `00_Meta_지도.md`, `INDEX.md` 링크 제거 |
| `하네스 컨트롤` 분석 및 가이드 작성 | ✅ 완료 | `하네스_컨트롤_가이드.md` 신규 저장 |
| `/memory` 명령어 복구 | ✅ 완료 | `cmd_memory` 정상화, L2 자동 백업 추가 |
| Dreaming V2 PEMS 버그 수정 | ✅ 완료 | `offline_consolidation` 항상 실행 |
| HELP_TEXT 완전판 (33개 명령어) | ✅ 완료 | `/research`, `/goal`, `/model` 등 추가 |
| wiki 타임스탬프 59개 일괄 복구 | ✅ 완료 | `wiki_auto_stamper.py` 신규 작성 |
| `constitution.md` §9 규칙 제정 | ✅ 완료 | 타임스탬프 의무 갱신 절대 규칙 |
| `Bio_Memory_Engine_가이드.md` 개정 | ✅ 완료 | 현행 시스템 반영 전면 재작성 |
| `규칙_통합_결과보고서.md` Rule 6 추가 | ✅ 완료 | 문서 무결성 원칙 신설 |
| hermes_local.py 봇 재시작 | ✅ 완료 | pkill → launchd 자동 재시작 |

### 현재 시스템 상태
| SERVICE | 상태 |
|--------|------|
| Hermes1 텔레그램 봇 (`com.hermes.bot`) | 🟢 정상 |
| Hermes WebUI (`com.hermes.webui`) | 🟢 정상 |
| llama-server (Gemma4 로컬 LLM) | 🟢 정상 |
| Bio-Memory Engine (L1/L2/L3) | 🟢 복구 완료 |
| Dreaming V2 (PEMS+offline_consolidation) | 🟢 정상화 |
| 태그 자동화(최대 8개) 인프라 | 🟢 정상화 (자동/실시간) |

---

## 📋 AI Agent Memory 개념 분석 — 문제 인식 저장 (2026-06-05)

**상태**: 🔵 문제 인식 저장 (향후 수정 로드맵 확정 시 반영 예정)

블로그 "AI Agent Memory" 4계층 메모리 스택 + 4대 연산 vs 현재 시스템 대비 분석 완료.

**핵심 갭**:
- Forget 정책 부재 — 메모리 블로트 리스크
- Update 자동 충돌 감지 부재
- Writer 저장 전 self-question 부재
- semantic_index.db ↔ memory 검색 미연동

**참조**: 01_hot.md `📋 AI Agent Memory 개념 분석` 섹션 상세.

*최종 업데이트: 2026-06-24 22:17*

|*최종 업데이트: 2026-06-24 22:17*
|- **스크립트**: `~/Applications/venu/scripts/switch_model.sh` → `/usr/local/bin/switch-model` 심링크
|- **용도**: Hermes Agent 메인 모델 전환 (got-oss-120b / DeepSeek Chat)
||- **사용법**: `switch-model got` (GPT-OSS-120B NVIDIA) / `switch-model deepseek` (DeepSeek Chat)

||- **새로운 업데이트**: `~/.hermes/config.yaml`와 `~/Applications/venu/.hermes2/config.yaml`에 전체 toolsets를 포함하도록 2026-06-06에 적용되었습니다. 이제 두 Hermes 인스턴스 모두 모든 도구를 사용할 수 있습니다.
|- **적용 대상**: ~/.hermes/config.yaml (WebUI+Gateway) + venu/.hermes2/config.yaml (Telegram Bot)
|- **특징**: 설정 삭제 없이 `# [SWITCHED to ...]` 주석 보존, 전환 후 `hermes gateway restart` 필요
|- **배경**: DeepSeek API 대신 NVIDIA 무료 GPT-OSS-120B 메인 사용. Qwen2.5-14B-Instruct(로컬)는 별도 API 영향 없음.
- **상태**: NVIDIA 70B 모드 및 DeepSeek 날씨 검색 기능 모두 안정화됨.
- **주요 변경점**: `harness_agent.py`의 비동기 애니메이션 적용 및 모델명 수정, `web_agent_module.py` 네이버 날씨 스크래퍼 통합.

---

## 📋 2026-06-12 논문 기반 하네스 대규모 업그레이드

**상태**: ✅ 완료 (5개 파일 수정, 1개 신규 문서)

### 수행된 작업

| 파일 | 변경 내용 | 적용 논문 |
|---|---|---|
| `modules/bio_memory_engine.py` | ① Model Collapse 방어: assistant 점수 L2 상한 강제<br>② 인라인 임베딩 제거 + turbovec 이관 (`_strip_inline_embeddings`)<br>③ L2 eviction `if` → `while` 수정<br>④ source 태깅 (`human`/`llm`) | 2305.17493, 2605.18747, 2606.01444 |
| `modules/memory_refinement.py` | SAGE 신선도 게이트: `novelty_score()`, `is_novel_enough()`, `diversity_check()`, `get_diversity_report()` 추가 | 2605.30711 |
| `modules/context_assembler.py` | ClawTrojan 방어: `_sanitize_wiki_content()` — wiki 주입 전 의심 패턴 라인 제거 | 2605.31042 |
| `modules/skill_auditor.py` | COLLEAGUE.SKILL + Aging: `SkillLifecycle` 클래스 추가 (lifecycle DB, stale 탐지, 보고서) | 2605.31264, 2026-05-27 |
| `modules/agentic_loop.py` | TraceGraph: `_trace()` 함수 + `trace_log.jsonl` 롤링 로그 (200엔트리, turn/tool/path/result/repaired) | 2605.31308 |
| `wiki/00_Meta/하네스_논문_기반_개선_로그.md` | 전체 적용 내역 상세 기록 (신규 생성) | — |

### 핵심 성과

- **`episodic_memory.json` 비대 해결**: 2.3MB → ~150KB 예상 (임베딩 인라인 제거)
- **Model Collapse 차단**: LLM 응답이 L2 장기기억으로 올라가는 경로 완전 차단
- **ClawTrojan 방어**: wiki 파일 오염 통한 하네스 탈취 시도 탐지·차단
- **스킬 노화 감지**: 30일 미사용·성공률 저조 스킬 자동 stale 마킹
- **도구 실행 궤적 기록**: `~/.hermes/runtime/trace_log.jsonl`

### 미적용 후속 항목
- AdaCoM context hot/cold 분리 (context_assembler 리팩토링 필요)
- Governed Harness Mutation (skill_evolver.py 분석 후)
- Deep Telemetry 파이프라인 확장

*최종 업데이트: 2026-06-24 22:17*

---

## 📋 2026-06-12 협업 규칙 7개 → 하네스 코드 적용

| 파일 | 변경 내용 | 규칙 |
|---|---|---|
| `modules/history_manager.py` | `get_context_pressure()` 추가 — 포화도 {turns, ratio, warn, critical} 반환 | 규칙 5 |
| `modules/response_handler.py` | 응답 말미 컨텍스트 포화도 경고 삽입 (75%+/100%+) | 규칙 5 |
| `modules/skill_auditor.py` | `SkillLifecycle.record_feedback()` 추가 — MJ 정정 신호로 성공률 소급 보정 | 규칙 6 |
| `handlers/_base.py` | `_detect_correction()` + `emit_skill_feedback()` — 정정 패턴 자동 감지 훅 | 규칙 6 |
| `modules/context_assembler.py` | `_input_clarity_score()` + Input Clarity Gate — 모호 입력 시 LLM에 힌트 주입 | 규칙 7 |

*최종 업데이트: 2026-06-24 22:17*

---

## 2026-06-18 MJstock 실험 필터 토글 시스템 (v1.0)

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `screener/run_scan.py` | 실험 필터 토글 3개 추가 + `compute_exp_filters()` 함수 신설 + US/KR 스캔 양쪽 exp 필터 삽입 |
| `screener/signal_tracker.py` | `ACCOUNT_EQUITY`, `RISK_PCT` 상수 추가 + `_calc_position_size()` 함수 신설 + `record_scan()` 파라미터 확장 |
| `app.py` | 차트 탭에 실험 필터 체크박스 UI 자동 표시 + 즉시 필터링 + 필터된 종목 수 캡션 |
| `screener/screen_uryangju_nongsaju.py` | 중복 실험 필터 코드 제거 (run_scan.py로 통합) |
| `docs/signals_entry_points.html` | MAGNET_ZONE C2 기준 정정, 보조지표 독법(9), 과거 이력 누적법(10) 섹션 추가 |
| `docs/quant_logic_analysis.html` | 실험 필터 토글 시스템 섹션(9) 추가 — signal_tracker 연결 구조 도식화 |
| `docs/korean_original_formulas.html` | SEPA/GROK V1.8~V3.0 진화 아카이브 추가 |
| `docs/MJstock_사용설명서.html` | 우량주농사 분할매수 3단계 프레임 추가 (Blue-Chip Scaling V2.0) |

### 핵심 변경 상세

**`screener/run_scan.py` — 실험 필터 토글**
- `EXP_USE_FRESH_TREND = False` — 200일선 기울기 필터 (GROK V2.3)
- `EXP_USE_IS_LEADER = False` — RSI>55 + 장기상승 강세 근사
- `EXP_USE_IS_TIGHT = False` — ATR 이중 압축 필터 (GROK V3.0)
- `compute_exp_filters(df_daily)` — US/KR 12개 검색식 통과 종목에 공통 적용
- 스캔 결과 CSV에 항상 `exp_fresh_trend`, `exp_is_leader`, `exp_is_tight` 컬럼 기록

**`screener/signal_tracker.py` — 포지션 사이징**
- `_calc_position_size()` — ATR 기반 권장 매수 수량 계산 (GROK V3.0 Adaptive 공식)
- `record_scan()` 신규 컬럼: `atr14`, `suggested_shares`, `exp_fresh_trend`, `exp_is_leader`, `exp_is_tight`

**에러 진단 포인트**:
- `exp_` 컬럼 없는 구 CSV 로드 시 app.py 체크박스 미표시 (정상 동작)
- `_calc_position_size()` ATR=0 케이스: 0 반환하여 ZeroDivisionError 방지 필요 확인

---

### ✅ 2026-06-18 | Claude Code | **MJstock 유니버스 확장 + 티커 검색 + 슬라이더 UI**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

#### 변경 파일 요약

| 파일 | 변경 내용 |
|---|---|
| `data/nasdaq1000.csv` | 나스닥 상장 시총 상위 1000종목 (신규) |
| `data/kospi_full.csv` | 코스피 전체 946종목 시총 정렬 (신규, FinanceDataReader) |
| `data/kosdaq_full.csv` | 코스닥 전체 시총 상위 1000종목 (신규) |
| `data/dow30.csv`, `data/russell1000.csv`, `data/nasdaq100.csv`, `data/kosdaq150.csv` | 기존 유지 |
| `screener/data_loader.py` | `get_us_universe_tickers()`, `get_kr_universe_tickers()` 신규 추가. 각 유니버스 자체 1000개 보유, 보완 로직 없음 |
| `screener/run_scan.py` | `--universe`, `--top-n`, `--single-chart` argparse 파라미터 추가. `compute_exp_filters()` FRESH_TREND EMA50 fallback 수정 |
| `app.py` (스캐너 탭) | 미국/한국 그룹 → 유니버스 버튼(최대 4개) → 슬라이더 0~1000 통일 → 검색식 카드형 박스 |
| `app.py` (차트 탭) | 티커 직접 검색 expander 추가 (`--single-chart` subprocess 호출) |
| `docs/signals_entry_points.html` | GONGJOONG 설명에 "급등 말미 공중★ = 에너지 소진 경고, 분할 익절 신호" 추가 |

#### 유니버스별 종목 수 (슬라이더 0=전체)
```
나스닥  → nasdaq1000.csv  → 1,000개
S&P500  → 위키피디아 실시간 → 503개
다우30  → dow30 + russell1000 합산 → 최대 1,000개
러셀1000 → russell1000.csv → ~1,000개
코스피  → kospi_full.csv  → 946개
코스닥  → kosdaq_full.csv → 1,000개
```

#### 연결 구조
```
app.py (universe_sel + top_n_slider)
  → subprocess: run_scan.py --universe nasdaq100 --top-n 500
  → data_loader.get_us_universe_tickers('nasdaq100', 500)
  → nasdaq1000.csv 상위 500개 반환
```

**에러 진단 포인트**:
- `kospi_full.csv` / `kosdaq_full.csv` 없으면 `kospi200.csv` / `kosdaq150.csv` fallback 자동 적용
- `--single-chart` 모드: stdout에서 `CHART_PATH:` 라인 파싱 → 없으면 차트 생성 실패
- FRESH_TREND: 221봉 미만이면 EMA50 기울기로 fallback (KIS API 100봉 제한 대응)

### ✅ 2026-06-18 | Claude Code | **MJstock UI 개선 — 스캐너탭 정리, 점수 정렬, Best Signal 문구 수정**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

#### 변경 내용

| 파일 | 변경 내용 |
|---|---|
| `app.py` (스캐너 탭) | 스캔 결과 차트 버튼 그리드 제거 → "통과 종목 N개 — 차트 보기 탭으로 이동" 안내 메시지로 교체 (화면 중복 제거) |
| `app.py` (차트 탭) | 스캔 결과 점수(`score`) 기준 내림차순 자동 정렬 + 버튼 라벨에 "N점" 표시 |
| `app.py` (Best Signal) | 하드코딩 문구에 `*(수동 설정값 — 스캔 데이터 30일 누적 후 실제 승률로 자동 변경됩니다)*` 추기 |
| `run_scan.py` (US/KR) | 스캔 통과 종목에 farming signals + `compute_score()` 직접 계산 → CSV에 `score` 컬럼 포함 |

#### 연결 구조
```
run_scan.py → compute_farming_signals() + compute_score() → score 컬럼 포함 CSV
app.py (차트탭) → CSV load → score 정렬 → 버튼 라벨에 "N점" 표시
```

**에러 진단 포인트**:
- 버튼에 점수가 안 뜨면: CSV에 `score` 컬럼 있는지 확인, `pd.to_numeric` 변환 실패 여부 체크
- 실험 필터 체크박스 안 뜨면: CSV에 `exp_fresh_trend` 컬럼 존재 여부 확인 (재스캔 필요)

*최종 업데이트: 2026-06-24 22:17*

### ✅ 2026-06-18 | Claude Code | **MJstock 유니버스 CSV 업데이트 시스템 + 점수 계산 수정**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

#### 변경 내용

| 파일 | 변경 내용 |
|---|---|
| `screener/data_loader.py` | `_UNIVERSE_CSV` 딕셔너리 추가 — 유니버스-파일명 매핑 통일 |
| `screener/data_loader.py` | `get_universe_csv_mtime(universe)` 신설 — CSV 마지막 수정 시각 반환 |
| `screener/data_loader.py` | `update_universe_csv(universe)` 신설 — 유니버스별 최신 데이터 갱신 (S&P/나스닥/다우→위키, 코스피/코스닥→KRX OpenAPI, 러셀→Downloads 반자동) |
| `screener/data_loader.py` | `get_sp500_tickers()` — 매번 위키 크롤링 → CSV 우선 읽기로 변경 (스캔 속도 개선) |
| `app.py` (유니버스 섹션) | 버튼 레이블 간소화: "나스닥100 (100종목)" → "나스닥" 등 숫자 제거 |
| `app.py` (유니버스 섹션) | 슬라이더 눈금 HTML 추가 (0, 100, 200 ... 1000) |
| `app.py` (유니버스 섹션) | 유니버스 캡션에 CSV 마지막 업데이트 날짜 표시 |
| `app.py` (유니버스 섹션) | "유니버스 CSV 업데이트" 접이식 섹션 추가: 체크박스 선택 + 업데이트 버튼 + 러셀 다운로드 링크 |
| `screener/run_scan.py` (US) | score 계산 수정 — farming signals(항상 0) → bool 조건 컬럼 True 개수 / 전체 × 100 |
| `screener/run_scan.py` (KR) | 동일 score 계산 수정 (KR 검색식 공통 적용) |
| `app.py` (차트 탭) | 종목 버튼에 순위 번호 추가: "📈 AMAT 70점" → "📈 #1 AMAT 70점" |
| `app.py` (차트 탭) | 기존 결과 CSV score=0 시 bool 컬럼으로 자동 재계산 (과거 스캔 소급 표시) |

#### 유니버스 업데이트 소스 정리
```
S&P500    → Wikipedia (sp500.csv)
나스닥    → Wikipedia Nasdaq-100 표 (nasdaq1000.csv)
다우      → Wikipedia DJIA 표 (dow30.csv)
러셀1000  → ~/Downloads에서 russell*.csv/xlsx 탐색 → data/russell1000.csv 복사 (반자동)
코스피    → KRX OpenAPI POST (kospi_full.csv, 시총 정렬)
코스닥    → KRX OpenAPI POST (kosdaq_full.csv, 시총 정렬)
```

#### score 계산 로직 (v2.0)
```
score = round(True 조건 개수 / 전체 bool 조건 개수 × 100)
예) 우량주농사 10개 조건 중 7개 True → 70점
```

#### 연결 구조
```
app.py "유니버스 CSV 업데이트" expander
  → update_universe_csv(universe) in data_loader.py
  → 소스별 크롤링/API → data/*.csv 갱신
  → get_universe_csv_mtime() → 캡션에 날짜 표시

run_scan.py run_screen_us/kr
  → result["details"] bool 컬럼 True 개수 계산
  → row["score"] 저장 → CSV 기록

app.py 차트탭
  → score=0 감지 시 bool 컬럼 재계산 적용
  → 내림차순 정렬 → "#순위 티커 N점" 버튼 표시
```

**에러 진단 포인트**:
- 유니버스 업데이트 실패 → 위키피디아 테이블 구조 변경 or KRX API 응답 형식 변경 확인
- 러셀 업데이트 안 됨 → `~/Downloads`에 russell/ru1000 포함 파일명 확인
- score 여전히 0 → `result["details"]`에 bool 컬럼이 없는 검색식 (check_fn 반환값 확인)
- KRX API `OutBlock_1` 빈 배열 → 장 마감 후 당일 날짜로 조회 시 발생, 전일 날짜로 재시도

*최종 업데이트: 2026-06-24 22:17*

### ✅ 2026-06-18 | Claude Code | **MJstock 퀀트 DB 기초공사 — 스냅샷 + 퀀트 로그 탭**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

#### 변경 내용

| 파일 | 변경 내용 |
|---|---|
| `screener/run_scan.py` | `compute_quant_snapshot(df_daily)` 함수 신설 — 17개 퀀트 컬럼 계산 |
| `screener/run_scan.py` | US `run_screen_us` + KR `run_screen_kr` 양쪽에 `compute_quant_snapshot()` 호출 추가 |
| `app.py` (탭4) | "최근 결과" → "퀀트 로그" 전면 개편 — 검색식별 서브탭 + 요약 지표 + 전체 테이블 + CSV 다운로드 |

#### 퀀트 스냅샷 컬럼 (17개)

| 컬럼 | 설명 | 퀀트 활용 |
|---|---|---|
| `entry_price` | 스캔 당일 종가 | 진입가 기준, 수익률 계산 |
| `entry_volume` | 스캔 당일 거래량 | 거래량 폭발 여부 확인 |
| `atr14` | ATR(14) | 손절폭 = entry_price - 2×ATR |
| `atr_pct` | ATR14/entry_price×100 | 변동성 % — 포지션 사이즈 계산 |
| `rsi14` | RSI(14) | 과매수(>70) / 과매도(<30) 진단 |
| `macd_hist` | 표준 MACD(12,26,9) 히스트 | 추세 전환 신호 — 양전환 = 매수 신호 |
| `bb20_width` | BB(20) 밴드폭/EMA20 | VCP/수축 패턴 포착 — 낮을수록 폭발 전 |
| `bb20_pct` | 종가의 BB 내 위치(0~1) | 1에 가까울수록 상단 돌파 임박 |
| `ema20_dist_pct` | (close-EMA20)/EMA20×100 | 단기 이격 — 너무 크면 과열 |
| `ema50_dist_pct` | (close-EMA50)/EMA50×100 | 중기 이격 |
| `ema200_dist_pct` | (close-EMA200)/EMA200×100 | 장기 이격 — 음수면 장기 하락 추세 |
| `w52_high` | 52주(250봉) 최고가 | 신고가 돌파 여부 판단 기준 |
| `w52_high_dist_pct` | (close-52주고가)/고가×100 | 음수 = 고가 대비 하락폭 |
| `volume_ratio_20` | 당일거래량/20일평균 | >2 = 거래량 폭발, 세력 진입 신호 |
| `ret_5d` | 5일 후 수익률 | 스캔 시 빈칸 → 추후 채움 |
| `ret_10d` | 10일 후 수익률 | 동일 |
| `ret_20d` | 20일 후 수익률 | 동일 |

#### 퀀트 로그 탭 구성
```
탭4 "퀀트 로그"
  ├─ 검색식별 서브탭 (우량주농사 / 단타의신 / 주도주 / ...)
  │    ├─ 요약 지표: 총 통과 기록 / 평균 점수 / 평균 5일수익 / 평균 RSI14
  │    ├─ 전체 테이블 (스캔일시 + 17개 퀀트 컬럼, 점수 내림차순)
  │    └─ CSV 다운로드 버튼
  └─ 최근 30회 스캔 누적 로드
```

#### 향후 확장 로드맵 (퀀트 검증)
- `batch_fill_returns.py` (미구현): 매일 실행 → ret_5d/10d/20d 자동 채움
- 퀀트 대시보드: 점수 구간별 수익률 분포, 최적 진입 타이밍 분석
- 추가 예정 컬럼: `sector`, `market_phase`(강세/약세 국면), `day_of_week`

**에러 진단 포인트**:
- `compute_quant_snapshot()` 실패 시 None 값으로 저장되고 스캔 계속 진행 (try/except 보호)
- BB20_WIDTH 계산 실패 → `add_bbands_width()` 내 EMA20 계산 봉 수 부족 (최소 30봉 필요)
- 퀀트 로그 탭 빈 화면 → results/{screener}/ 폴더에 scan_*.csv 없음

*최종 업데이트: 2026-06-24 22:17*

### ✅ 2026-06-18 | Claude Code | **MJstock 퀀트 데이터 자동 축적 — 수익률 배치 + 헬스체크**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

#### 변경 내용

| 파일 | 변경 내용 |
|---|---|
| `screener/batch_fill_returns.py` | 신규 — 스캔 CSV의 ret_5d/10d/20d 자동 채우기 배치. KIS API로 현재가 조회 후 수익률 계산 저장 |
| `screener/batch_fill_returns.py` | `health_check()` 함수 — 검색식별 퀀트 채움률 + 수익률 채움률 반환 |
| `app.py` (퀀트 로그 탭) | "데이터 적재 상태 확인" 접이식 섹션 추가 — health_check() 호출, 80% 미만 경고 |
| crontab | 매주 월~금 17:30 자동 실행 등록: `batch_fill_returns.py` |

#### 자동화 구조
```
매일 17:30 (장 마감 후) — crontab
  → batch_fill_returns.py 실행
  → results/*/scan_*.csv 순회
  → entry_price 있고 ret_Nd = None + 스캔일 N일 경과 → KIS API 현재가 조회
  → ret_5d / ret_10d / ret_20d 계산 후 CSV 덮어쓰기
  → results/fill_returns.log 기록
```

#### 헬스체크 판단 기준
- 퀀트 채움% ≥ 80% → ✅ 정상
- 퀀트 채움% < 80% → ⚠️ 경고 (오래된 CSV는 스냅샷 없음 — 정상)
- 수익률 채움% = 5일 경과 레코드 중 ret_5d 채워진 비율

**에러 진단 포인트**:
- `fill_returns.log` 에서 `[ERROR]` 라인 확인
- KIS 토큰 만료 시 `get_access_token()` 재발급 — 24h 캐시 자동 갱신
- `ret_Nd` 채워지지 않으면: 스캔일 경과 확인 (`scan_datetime` 컬럼) + crontab 동작 확인

---

### ✅ 2026-06-18 | Opus 4.8 코드리뷰 | **MJstock 버그 5건 수정**

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

#### 변경 내용

| 파일 | 변경 내용 |
|---|---|
| `app.py` | SCREENERS_KR dict 키 6개 `kr_X` → `X_kr` 형식으로 통일 (치명적) |
| `run_scan.py` | `run_one()` 함수에 `no_telegram: bool = False` 파라미터 추가, `args.no_telegram` NameError 수정 (치명적) |
| crontab | 07:30/17:30/18:00 작업 3개 `/usr/bin/python3` → `.venv/bin/python` 교체 (치명적) |
| `auto_scan_nasdaq500.py` | `sys.executable` → `PYTHON` 변수 (venv 우선, 없으면 sys.executable fallback) |
| `app.py:617` | `st.switch_tab("📊 차트 보기")` 제거 → 안내 메시지로 대체 (Streamlit 1.58 미지원) |
| `send_scan_result.py:52` | `load_latest_csv` 반환 타입 `pd.DataFrame | None` → `tuple | None` |

#### 버그 요약

| 번호 | 심각도 | 파일 | 증상 |
|---|---|---|---|
| BUG-1 | 치명적 | `app.py` | KR 검색식 키 불일치 → KR 스캔 argparse 검증 실패, 결과 폴더 미존재 경로 참조 |
| BUG-2 | 치명적 | `run_scan.py` | `run_one()`에서 `args` NameError — `args.no_telegram` 미전달 |
| BUG-3 | 치명적 | crontab | venv 미사용 → pandas/dotenv/requests/plotly import 실패 |
| BUG-4 | 경미 | `app.py` | `st.switch_tab` Streamlit 1.58 미지원 API 호출 오류 |
| BUG-5 | 경미 | `send_scan_result.py` | 반환 타입힌트 오류 (`DataFrame` → `tuple`) |

**에러 진단 포인트**:
- KR 스캔 argparse error → SCREENERS_KR 키 형식 확인 (`X_kr` 패턴)
- `run_one()` NameError → 호출부 3곳에 `no_telegram=args.no_telegram` 전달 여부 확인
- crontab 스캔 실패 시 `which python` → `.venv/bin/python` 확인

---

### ✅ 2026-06-23 | handlers/_system_ops.py — /restart_bot 레이스 컨디션 수정

**프로젝트**: `/Users/bluesea/Applications/Mjauto/Scripts`

**변경 파일**: `handlers/_system_ops.py` — `_restart_routine()`

**변경 내용**:
- 기존: NEW 프로세스 Popen 먼저 → 2초 sleep → OLD SIGTERM (두 인스턴스 2초간 동시 폴링 → Telegram 409 Conflict → 양쪽 사망)
- 수정: old_pid 저장 → NEW를 `sleep 1 && python3 hermes_local.py` shell 방식으로 예약 → 즉시 OLD SIGTERM (겹치는 구간 제로)

**연결 구조**: `handlers/_system_ops.py` → `hermes_local.py` (새 인스턴스) → botwatch 자동 감지 불필요

**에러 진단 포인트**:
- 재시작 후 봇 무응답: `ps aux | grep hermes_local` 로 인스턴스 수 확인 (1개여야 정상)
- Telegram 409 Conflict 발생 시 → 두 프로세스 동시 실행 중. 수동으로 하나 종료 후 안정화

---

### ✅ 2026-06-23 | MJstock 삼돌이/농사단타 현지화 + HTML 5종 통합

**프로젝트**: `/Users/bluesea/Applications/Mjstock`

#### 신규 스크립트 (4개)

| 파일 | 내용 |
|---|---|
| `screener/screen_samdoli_kr.py` | 삼돌이 KR — 일봉 DD/C2/EMA45 돌파 후보 선별 + 5분봉 BB/ENV 장중 진입 신호 |
| `screener/screen_samdoli_us.py` | 삼돌이 US — 시총 $1B↑, 거래량변화 150%↑, SMA200 추가, USD 기준 현지화 |
| `screener/screen_nongsa_danta_kr.py` | 농사단타 KR — 삼돌이 후보 대상, 50억봉◆+3매수신호+OBV 3박자, Phase 7단계 |
| `screener/screen_nongsa_danta_us.py` | 농사단타 US — US 현지화, 30분봉 미사용(needs_30min=False), $5M봉 기준 |

#### run_scan.py 등록

| 레지스트리 | 추가 항목 |
|---|---|
| `KR_SCREENERS` | `samdoli_kr` (needs_30min: True), `nongsa_danta_kr` (needs_30min: True) |
| `US_SCREENERS` | `samdoli` (needs_30min: True), `nongsa_danta` (needs_30min: False) |

**검증**: `python3 -c "from screen_samdoli_kr...; from screen_nongsa_danta_us...; print('성공')"` → 4개 임포트 성공 확인

#### HTML 문서 정리 (8개 → 5개)

| 변경 | 파일 |
|---|---|
| 내용 흡수 후 삭제 | `samdoli_guide.html`, `nongsa_danta_guide.html`, `nongsa_signal_board.html` |
| 삼돌이/농사단타 섹션 추가 | `MJstock_사용설명서.html` (섹션3 카드, 타이밍표, 파라미터표, 선택가이드) |
| 삼돌이/농사단타 섹션 추가 | `signals_entry_points.html` (섹션13 Phase 가이드, 타점표) |
| 삼돌이KR/농사단타KR 수식 추가 | `korean_original_formulas.html` (S9/S10) |
| 삼돌이US/농사단타US 변환표 추가 | `kr_to_us_conversion.html` (S8/S9) |

**에러 진단 포인트**:
- `nongsa_danta_us` import 시 `prepare_30min` 없음 주의 → `needs_30min: False` 유지
- 함수명 차이: US 버전은 `prepare_daily_us()`, `prepare_5min_us()` (KR과 달리 `_us` 접미사)

---

### ✅ 2026-06-24 | arXiv 6편 논문 하네스 적용 + 봇 안정화

#### arXiv 논문 하네스 적용 (6편)

| 논문 | 적용 내용 | 수정 파일 |
|---|---|---|
| 2606.22528 Governance Decay | `GOVERNANCE_ANCHOR` 상수 — `compact_and_save()` / `get_history_for_llm()` 양쪽에 삽입, 압축 후에도 Lock Stack 4개 + 핵심 규칙 항상 LLM 컨텍스트 맨 앞에 보존 | `Scripts/modules/history_manager.py` |
| 2606.22737 GroundEval | `verify_file_changed()`, `verify_db_row_exists()`, `snapshot_file_hash()` 3개 검증 함수 추가 | `~/.hermes/governance/skills/brain/cognitive/scripts/verification_engine.py` |
| 2606.20659 Skill Coverage | `record_skill_invocation()`, `get_skill_coverage()` 추가 + `skill_invocations` DB 테이블 신설. `/status`에서 미호출 스킬 목록 확인 가능 | `Scripts/modules/weakness_miner.py` |
| 2606.20636 SkillHarness | `validate_skill_safety()` — `SKILL.md` 안전 키워드 검사, 안전 제약 없는 스킬 목록 반환 | `Scripts/modules/weakness_miner.py` |
| 2606.23195 Memory Contagion | L2 에피소드에 `confidence` / `observation_count` / `provenance` 필드 추가. L3에 `confidence` / `success_count` 추가. confidence < 0.6 불확실 기억 격리 | `Scripts/modules/bio_memory_engine.py` |
| 2606.20718 Sycophancy | `GOVERNANCE_ANCHOR` 6~9번 반아첨 규칙 추가 + `detect_sycophancy_risk()` 메서드 추가 | `history_manager.py` + `verification_engine.py` |

**연결 구조**: `harness_agent.py` → `history_manager.py`(GOVERNANCE_ANCHOR) → LLM 컨텍스트 / `weakness_miner.py`(skill coverage) → `weakness.db` / `bio_memory_engine.py`(L2/L3 confidence) → `~/.hermes/memory/`

**에러 진단 포인트**:
- GOVERNANCE_ANCHOR 미작동 의심 시: `python3 -c "from modules.history_manager import GOVERNANCE_ANCHOR; print(len(GOVERNANCE_ANCHOR))"`
- skill_coverage 미집계 시: `python3 -c "from modules.weakness_miner import get_weakness_miner; print(get_weakness_miner().get_skill_coverage())"`
- L2 confidence 필드 누락 시: `~/.hermes/memory/episodic.db` 스키마 확인 (`PRAGMA table_info(episodes)`)

---

#### 봇 안정화 수정

**변경 파일**: `handlers/_base.py`

**변경 내용**:
- `executor` 패키지 Python 3.14 SyntaxError 제거 (`async` 예약어 충돌) → `asyncio.create_subprocess_shell`로 교체
- 원인: 단일 import 실패가 handlers 패키지 전체(16개)를 무너뜨리는 구조. `/help` 포함 모든 명령어 먹통

**에러 진단 포인트**:
- 핸들러 전체 무응답 시: `python3 -c "from handlers._base import add_to_history; print('OK')"` — SyntaxError 발생하면 패키지 호환성 문제

---

**변경 파일**: `handlers/_system_ops.py`

**변경 내용**:
- `/restart_bot` 레이스 컨디션 수정 — OLD 프로세스 먼저 SIGTERM, `sleep 1 && python3`로 NEW 시작 (겹치는 구간 제로)
- 기존: NEW Popen 먼저 → 2초 sleep → OLD SIGTERM (2초간 두 인스턴스 동시 폴링 → Telegram 409 Conflict → 양쪽 사망)

**에러 진단 포인트**:
- 재시작 후 봇 무응답: `ps aux | grep hermes_local` 인스턴스 수 확인 (1개여야 정상)

---

**변경 파일**: `handlers/_vault.py`

**변경 내용**:
- `/vault graph` 노드 5298 > 5000 오버플로우 → env var + try/except graceful degradation

---

#### 신규 스킬 파일 (3개)

| 파일 | 내용 |
|---|---|
| `~/.hermes/skills/grill-me/SKILL.md` | 신규 |
| `~/.hermes/skills/grilling/SKILL.md` | 신규 |
| `~/.hermes/skills/diagnosing-bugs/SKILL.md` | 신규 |

**에러 진단 포인트**:
- 스킬 안전성 검사: `python3 -c "from modules.weakness_miner import get_weakness_miner; print(get_weakness_miner().validate_skill_safety())"`
