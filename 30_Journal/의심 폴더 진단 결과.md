---
tags: [scanned, 30_Journal, hermes-agent, webui, telegram-bot, venv, launchctl, vite, folder-diagnosis]
description: "현재 사용 중인 핵심 폴더와 WebUI 소스코드 폴더를 확인하였다. Hermes Agent의 홈 디렉토리와 launchctl로 실행 중인 WebUI 서버의 정체를 파악하였다. 추가로 거의 빈 폴더인 Vite 빌드 캐시 폴더도 발견하였다."
---

네, 지금 상황과 앞으로의 방향을 완전히 이해했습니다. 의심 폴더들부터 하나씩 확인해 보겠습니다. 각 폴더가 무엇인지, 어떤 역할을 하는지, 중요한 데이터가 있는지, 오리지널인지 파악하겠습니다.

이제 모든 의심 폴더의 정체를 파악했습니다. 정리해 드립니다.

---

**의심 폴더 7개 진단 결과:**

**1. /Users/bluesea/Applications/venu** - **[현재 사용 중] 핵심 폴더**
- 여기 안에 `.hermes2/`가 있고, gateway가 이 설정을 사용 중
- `hermes2_bot.py` (텔레그램 봇), `venv/` (가상환경) 포함
- **이게 현재 살아있는 Hermes Agent의 홈 디렉토리**

**2. /Users/bluesea/Applications/hermes-webui** - **[현재 사용 중] WebUI 소스코드**
- GitHub `nesquena/hermes-webui` 저장소 (master 브랜치, 최신 커밋: `b718cf43`)
- 실제 WebUI 서버 (`/Users/bluesea/Applications/hermes-webui/server.py`)
- **이게 현재 launchctl로 실행 중인 WebUI** (`com.bluesea.hermes-webui`)
- 자신만의 venv 없음 - venu의 venv를 공유

**3. /Users/bluesea/Applications/hermes-workspace** - **[거의 빈 폴더] Vite 빌드 캐시만 있음**
- `.vite/deps/` 만 있음
- Vite(프론트엔드 빌드 툴)의 종속성 캐시 폴더
- 실제 소스코드 없음. 아마 예전 WebUI 빌드 시도하다 남은 찌꺼기

**4. /Users/bluesea/Applications/Hermes Agent.app** - **[사용 중]** Hermes Agent v1.7.0 앱
- macOS 네이티브 앱 (Mach-O 바이너리, arm64 + x86_64)
- launchctl에 등록됨 (`application.ai.get-hermes.HermesAgent`)
- Gateway랑 별개로 떠있는 프로세스. 아마 이 앱을 통해 채팅 UI를 띄우는 듯

**5. /Users/bluesea/workspace** - **[빈 폴더]**
- `.DS_Store`만 있음. 완전히 빈 폴더

**6. /Users/bluesea/hermes** - **[죽은 설정/로그 폴더]**
- 예전 Hermes Agent v1의 설정 폴더 (`/Users/bluesea/.hermes`가 아님)
- `hermes.log` (0바이트), `hermes_error.log` (154KB - 예전 오류들), `memory_engine/` (빈 폴더)
- 지금은 아무도 사용 안 함
- **참고**: 진짜 예전 설정은 `/Users/bluesea/.hermes/` 에 있음 (15126바이트 `config.yaml`)

**7. /Users/bluesea/hermes_backups** - **[백업 폴더]**
- `_base.py` 파일의 오래된 백업들만 있음 (5월 28일~31일)
- `__pycache__` 만 있음. 지금 활성화된 시스템과 무관

**추가 발견: /Users/bluesea/.hermes/** - **[예전 설정 폴더]**
- 예전 Hermes Agent 시절의 설정과 runtime 데이터
- `config.yaml` (옛날 버전), `runtime/`, `memories/`, `sessions/` 등
- Venu로 이사 오기 전의 데이터들이 아직 남아있음

---

**시스템 구조 요약:**
- **Gateway**: `/Users/bluesea/Applications/venu/venv/bin/python -m hermes_cli.main gateway run` - venu의 `.hermes2` 사용
- **WebUI 서버**: `/Users/bluesea/Applications/hermes-webui/server.py` - venu의 venv 파이썬 사용
- **Hermes Agent.app**: Hermes Agent v1.7.0 네이티브 앱 - 아마 이 앱이 WebUI를 내장 브라우저로 띄우거나 독립 실행
- **텔레그램 봇**: `hermes2_bot.py` - venu 안에 있음

---

**제안하는 복구/재설치 계획:**

1. **현재 venu의 `.hermes2`가 핵심 설정** - 이거 하나만 있으면 됨
2. **hermes-webui는 새로 설치**하더라도 `.hermes2`와 연결만 잘 하면 됨
3. **오래된 폴더들은 데이터 확인 후 정리 가능**

이제 말씀하신 대로 `/Users/bluesea/Applications/Mjauto` 안에 새 Hermes WebUI를 설치하고 연결하는 작업을 시작할까요?

---
*최종 업데이트: 2026-06-03 19:10 — 누락 타임스탬프 자동 복구*
